# EngramNote 项目图解

> 以图为主的项目全貌梳理，帮助你快速建立整体认知。所有图均为 Mermaid 语法，可在 VS Code（安装 Mermaid 插件）、Typora 或 GitHub 中直接渲染。

---

## 1. 一句话理解

**EngramNote 是一个「AI 驱动的学习笔记管理与知识库工具」**：把原始资料（PDF/图片/音视频/Office/Markdown）通过「转换 → 清洗 → 理解 → 复习」流水线变成个人的结构化知识库，并用 SM-2 间隔重复帮你记住它。

```
输入资料 ──▶ 转换(MinerU/ASR) ──▶ AI清洗 ──▶ AI理解(摘要/卡片/题目) ──▶ 间隔复习(SM-2)
              PDF→Markdown          去噪去重        知识图谱 / RAG问答        薄弱点优先
```

---

## 2. 系统总体架构

```mermaid
graph TB
    subgraph "前端"
        UI["页面层：仪表盘 / 笔记 / 上传 / 卡片 / 图谱 / 问答 / 复习 / 项目 / 目标（18 个页面文件）"]
    end

    subgraph "后端 FastAPI + SQLAlchemy async"
        API["API 路由层（14 个模块）"]
        SVC["业务服务层（22 个 service）"]
        MODEL["数据模型层（14 张表）"]
        API --> SVC
        SVC --> MODEL
    end

    subgraph "Celery 异步任务"
        CONV["convert_tasks 文档转换"]
        CLEAN["clean_tasks 清洗"]
        UNDER["understand_tasks 理解"]
        EMB["embedding_tasks 向量嵌入"]
        REMIND["reminder_tasks 提醒 + Beat 定时"]
    end

    subgraph "存储"
        SQLITE[("SQLite 数据库")]
        VAULT[("Vault 文件目录")]
        CHROMA[("Chroma 向量库")]
    end

    UI -->|HTTP / SSE| API
    API -->|投递耗时任务| CONV
    SVC --> SQLITE
    SVC --> VAULT
    SVC --> CHROMA
    EMB --> CHROMA
    CONV --> VAULT
    CONV --> MODEL
```

### 技术栈速览

| 层次 | 技术 | 说明 |
|------|------|------|
| 后端 | FastAPI + SQL Alchemy 2.0 (async) + SQLite | 零外部数据库依赖 |
| 任务队列 | Celery + 文件系统 broker | 无需 Redis，`task_acks_late=True` |
| 文档解析 | MinerU（云端 API / 本地 Pipeline 可选） | PDF → Markdown，保留公式表格 |
| AI 模型 | DeepSeek / GLM（OpenAI 兼容接口） | `httpx` 直调，无 LangChain |
| 嵌入模型 | BGE-M3 | ModelScope 下载，隔离到 Celery Worker 进程 |
| 向量库 | Chroma | 嵌入式，持久化 `data/chroma/` |
| 前端 | React 18 + TypeScript + Vite | 学术优雅视觉 |
| 图谱 | react-force-graph-2d | 力导向图 |

---

## 3. 核心学习闭环（端到端流程）

```mermaid
flowchart TD
    A["上传资料<br/>(PDF/图片/Office/音视频/MD)"] -->|POST /api/upload| B["转换 convert_tasks<br/>MinerU/ASR → 原始 Markdown"]
    B -->|自动触发| C["AI 清洗 clean_tasks<br/>规则去噪 + BGE-M3 向量去重"]
    C -->|用户确认| D["AI 理解 understand_tasks<br/>章节摘要 + 4类知识卡片 + 自动出题"]
    D --> E["知识图谱<br/>嵌入相似度 + LLM 双机制建关系"]
    D --> F["RAG 智能问答<br/>向量+BM25+n-gram 三路混合 + SSE 流式"]
    D --> G["间隔重复复习<br/>SM-2 算法 + 薄弱点优先"]
    G --> H["学习评估 / 报告<br/>笔记比对 + 盲点检测 + 7天趋势"]
    G -->|V2.0| I["学习目标 + 每日推荐<br/>weak_points > review > new"]
    I --> G
    I -->|V2.0| J["复习提醒<br/>浏览器通知 + 邮件 + 免打扰"]
```

### 笔记状态机

```mermaid
stateDiagram-v2
    [*] --> uploading: 上传
    uploading --> converting
    converting --> converted: 转换成功
    converting --> failed: 转换失败
    converted --> cleaning: 触发清洗
    cleaning --> cleaned: 清洗成功
    cleaning --> cleaning_failed: 清洗失败
    cleaned --> learning: 触发理解
    learning --> archived: 理解完成（可学）
    learning --> learning_failed: 理解失败
    converted --> archived: 手动归档
    cleaned --> archived: 手动归档
    learning_failed --> archived: 手动归档
    archived --> cleaned: 取消归档
    cleaning_failed --> cleaning: 重新清洗
    learning_failed --> learning: 重新理解
    failed --> converting: 重试
```

注：archived 有两条达成路径——手动归档（converted/cleaned/learning_failed）与学习成功（learning）；取消归档统一回到 cleaned，知识卡片等学习产物保留。

---

## 4. 后端分层结构

```mermaid
flowchart LR
    subgraph "API 路由层 /api"
        A1["auth 认证"]
        A2["notes 笔记CRUD"]
        A3["upload 上传"]
        A4["cleaning 清洗"]
        A5["understanding 理解"]
        A6["review 复习"]
        A7["quick_review 快速复习"]
        A8["report 报告"]
        A9["graph 图谱"]
        A10["folders 文件夹"]
        A11["projects 项目"]
        A12["assessment 学习评估"]
        A13["knowledge 知识点"]
        A14["goals 目标"]
    end

    subgraph "业务服务层 services"
        S1["storage / vault_path / vault_meta 存储与Vault"]
        S2["project 项目管理 + 扫描导入"]
        S3["note 笔记业务"]
        S4["cleaning 清洗管道"]
        S5["understanding 理解管道"]
        S6["llm LLM调用（含流式）"]
        S7["embedding 嵌入 + Chroma"]
        S8["rag 混合检索问答"]
        S9["sm2 / review / mastery 复习"]
        S10["version 版本历史"]
        S11["graph 图谱"]
        S12["goal 目标 + 每日推荐"]
        S13["notification 提醒 + 邮件"]
        S14["report / assessment / folder / auth"]
        S15["knowledge_link 知识点关联"]
    end

    A1 --> S1 & S3 & S14
    A2 --> S3 & S10
    A3 --> S2 & S3
    A4 --> S4
    A5 --> S5 & S6 & S8
    A6 --> S9
    A7 --> S9
    A8 --> S14
    A9 --> S11
    A10 --> S14
    A11 --> S2
    A12 --> S14
    A13 --> S15
    A14 --> S12
```

---

## 5. 数据模型（14 张表）

```mermaid
erDiagram
    USERS ||--o{ NOTES : "拥有"
    USERS ||--o{ PROJECTS : "拥有"
    USERS ||--o{ FOLDERS : "拥有"
    USERS ||--o{ KNOWLEDGE_CARDS : "拥有"
    USERS ||--o{ QUIZ_ITEMS : "拥有"
    USERS ||--o{ REVIEW_LOGS : "记录"
    USERS ||--o{ CARD_RELATIONS : "拥有"
    USERS ||--o{ NOTE_VERSIONS : "拥有"
    USERS ||--o{ LEARNING_GOALS : "拥有"
    USERS ||--o{ DAILY_PLANS : "拥有"
    USERS ||--o{ ASSESSMENTS : "拥有"

    PROJECTS ||--o{ NOTES : "包含"

    NOTES ||--o{ KNOWLEDGE_CARDS : "提炼"
    NOTES ||--o{ QUIZ_ITEMS : "出题"
    NOTES ||--o{ REVIEW_LOGS : "复习"
    NOTES ||--o{ NOTE_VERSIONS : "版本"
    NOTES ||--o{ NOTE_ANNOTATIONS : "批注"
    NOTES ||--o{ NOTE_MATERIAL_LINKS : "材料关联"

    KNOWLEDGE_CARDS ||--o{ QUIZ_ITEMS : "生成"
    KNOWLEDGE_CARDS ||--o{ CARD_RELATIONS : "关联"
    KNOWLEDGE_CARDS ||--o{ REVIEW_LOGS : "被复习"

    LEARNING_GOALS ||--o{ DAILY_PLANS : "拆解"

    FOLDERS ||--o{ NOTES : "归档"

    USERS {
        string id PK
        string email UK
        string username UK
        string hashed_password
    }
    PROJECTS {
        string id PK
        string user_id FK
        string name
        string slug "Vault目录名,创建后不可变"
    }
    NOTES {
        string id PK
        string user_id FK
        string project_id FK
        string title
        string status "状态机"
        string original_file_path
        string original_md_path
        string clean_md_path
        string source_type
    }
    KNOWLEDGE_CARDS {
        string id PK
        string note_id FK
        string card_type "concept/formula/qa/definition"
        string title
        string content
    }
    QUIZ_ITEMS {
        string id PK
        string card_id FK
        string question_type "choice/fill_blank/short_answer"
        float easiness_factor
        int interval
        datetime next_review_at
    }
    REVIEW_LOGS {
        string id PK
        string quiz_id FK
        boolean is_correct
        int quality "SM-2 0-5"
    }
```

> 数据库迁移：`_migrate_sqlite()` 启动时自动补列 + Alembic 版本脚本（001~005）。

---

## 6. Vault 目录结构（项目隔离 + 状态旁载）

这是最近一次重构的核心，也是理解「项目 → 文件」关系的关键。

```mermaid
flowchart TB
    ROOT["Vault 根目录<br/>默认 data/storage<br/>（可用 VAULT_DIR 覆盖）"]
    ROOT --> U1["{user_id}/<br/>（每用户一个目录）"]
    U1 --> P1["{project_slug}/<br/>（每项目一个目录，创建后不可变）"]
    U1 --> P2["{project_slug}/ …"]

    P1 --> SRC["source/{base}{ext}<br/>只读区：原始上传文件"]
    P1 --> OUT["output/markdown/{base}.md<br/>生成区：原始转换（只读）"]
    P1 --> CLEAN["output/markdown/{base}.clean.md<br/>生成区：清洗副本（工作副本）"]
    P1 --> META["output/meta/{base}.json<br/>状态旁载（写穿镜像）"]
    P1 --> HIST["history/versions/v{N}.md<br/>版本区：编辑/清洗归档"]
    P1 --> ASSETS["output/assets/<br/>预留"]
    P1 --> CACHE["cache/<br/>预留（OCR/ASR 缓存）"]

    SRC -. 命名关联法则 .-> OUT
```

### 关键约定

- **命名关联法则**：`source/{base}{ext}` ↔ `output/markdown/{base}.md` 仅扩展名不同 → 脱离数据库也能溯源
- **DB 是权威源**：状态先写数据库，再写穿镜像到 `output/meta/{base}.json`
- **创建项目时预建目录树**：`source / output/markdown / output/assets / output/meta / history/versions / cache`
- **手动放盘 + 扫描导入**：文件拷进 `source/` → 点「扫描导入」(`POST /projects/{id}/scan`) → 自动建笔记并触发转换，已导入文件自动跳过
- **路径兼容**：`_resolve_path` 同时兼容新 Vault 路径与历史遗留路径（如 `{user_id}/{folder_id}/original.md` 会自动补 bucket 前缀），保证旧笔记可读

---

## 7. Celery 任务与定时调度

```mermaid
flowchart TB
    subgraph "Celery Worker（filesystem broker）"
        T1["convert_document_task<br/>文档转换"]
        T2["clean_document_task<br/>AI 清洗"]
        T3["understand_document_task<br/>AI 理解"]
        T4["generate_questions_task<br/>题目生成"]
        T5["encode_text<br/>文本向量化"]
        T6["search_vectors<br/>向量检索"]
    end

    subgraph "Celery Beat 定时"
        B1["00:30 refresh_goal_progress<br/>刷新学习目标进度"]
        B2["09:00 send_daily_review_email<br/>发送复习提醒邮件"]
    end

    API["API 上传/触发"] --> T1
    T1 --> T2
    T2 --> T3
    T3 --> T4
    T1 --> T5
    QA["RAG 问答"] --> T6
    B1 --> DB[("数据库")]
    B2 --> MAIL["SMTP 邮件"]
```

---

## 8. 前端页面路由

```mermaid
flowchart TD
    subgraph "未登录"
        R1["/login 登录"]
        R2["/register 注册"]
    end

    subgraph "已登录（Sidebar + 主内容）"
        H["/ Dashboard 仪表盘"]
        N["/notes 笔记列表"]
        ND["/notes/:noteId 笔记详情<br/>原始/清洗/Diff 三视图 + 版本历史"]
        U["/upload 上传"]
        C["/cards 知识卡片"]
        CD["/cards/:cardId 卡片详情"]
        G["/graph 知识图谱"]
        Q["/qa 智能问答（SSE 流式）"]
        R["/review 复习答题"]
        QR["/review/quick/:noteId 快速复习"]
        QS["/questions 问题集"]
        T["/today 今日学习"]
        D["/daily 今日资料"]
        P["/projects 项目（扫描导入）"]
        A["/assessment 学习评估"]
        L["/goals 学习目标"]
    end

    H --> N & U & C & G & Q & R & P & L & T & D
```

---

## 9. 文件与数据流（一次完整上传会经历什么）

```mermaid
sequenceDiagram
    participant U as 用户
    participant FE as 前端
    participant API as FastAPI
    participant C as Celery Worker
    participant V as Vault 存储
    participant DB as SQLite

    U->>FE: 选择文件（或拷入项目 source/）
    FE->>API: POST /api/upload（或 /projects/{id}/scan）
    API->>V: 保存原始文件到 source/
    API->>DB: 创建笔记（status=converting）
    API->>C: 投递 convert_document_task
    C->>V: 读取原始文件
    C->>API: MinerU/ASR → Markdown
    C->>V: 写 output/markdown/{base}.md + meta json
    C->>DB: 更新笔记（converted）
    C->>C: 自动触发 clean_document_task
    C->>DB: 更新笔记（cleaned）
    C->>C: 自动触发 understand_document_task
    C->>DB: 生成卡片/题目（archived）
    FE->>API: 查询状态/内容（GET /api/notes/{id}）
```

---

## 10. 常见"为什么"速查

| 问题 | 答案 |
|------|------|
| 笔记内容在哪？ | DB 存路径，内容在 `data/storage/{user}/{project}/output/markdown/` 下实时读取 |
| 为什么我上传后一直 converting？ | ① MinerU Token 过期（HTTP 401）② Worker 未运行 ③ Worker 代码过期未重启 |
| 为什么创建项目没看到文件夹？ | 新版创建项目时会**同步预建磁盘目录树**，刷新前端即可看到 |
| 怎么把已有文件加入项目？ | 手动拷到项目 `source/` 目录 → 前端点「扫描导入」 |
| 旧项目/旧笔记还读得到吗？ | 可以。`_resolve_path` 兼容历史路径（自动补 bucket 前缀），根目录回落 `data/storage` |
| 数据库在哪？ | `backend/data/db/engramnote.db`（删掉后重启自动重建） |
| 向量数据在哪？ | `backend/data/chroma/` |
| 定时任务跑在哪个进程？ | Celery Beat（与 Worker 一起由启动脚本拉起） |

---

## 11. 启动与进程关系

```mermaid
flowchart LR
    START["start.bat"] --> API["uvicorn :8000<br/>--reload（热重载代码）"]
    START --> WORKER["celery worker<br/>--pool=solo（无热重载，改代码需重启）"]
    START --> BEAT["celery beat<br/>定时调度"]
    START --> FE["vite :5173"]
```

> ⚠️ **重要**：API 的 `--reload` 只热重载 FastAPI 进程；**Celery Worker 不会自动重载**。修改 `services/` 或 `config.py` 后，若转换/清洗/理解任务仍表现异常，请重启 Worker。

---

*本图由项目实际代码结构整理生成。若与代码有出入，以代码为准。*

# 🧠 EngramNote 修复总结（ADHD 友好版）

> 所有代码问题已修完 ✅ 测试已跑 ✅ 你只需要做 3 件事（见最后）

---

## ⏱️ 30 秒速览（只看这一节就够了）

| 问题 | 状态 |
|------|------|
| 数据可能被悄悄删掉（启动去重、重新学习清空） | ✅ 已修 |
| 定时任务（目标刷新/邮件提醒）从不运行 | ✅ 已修 |
| 笔记内容能注入病毒脚本（XSS） | ✅ 已修 |
| 复习上限前后端对不上（10 vs 50） | ✅ 已修 |
| 删除重复块按钮点了没反应 | ✅ 已修 |
| 双击提交会重复扣次数 | ✅ 已修 |
| 三套答题页面代码重复 480 行 | ✅ 已合并成一个组件 |
| start.bat 启动不了 | ✅ 已修（换行符+中文括号） |
| 测试 | ✅ 41 通过，0 新增失败 |

---

## ✅ 已修好的（按"对你意味着什么"分组）

### 🔴 数据安全（最重要的先修了）

- **重启不再偷偷删数据** —— 旧代码每次启动都会把"方向相反/不同类型"的知识关系当重复删掉，现在只删真正重复的，并且加了数据库约束防止再犯
- **归档笔记"重新学习"不再一键清空** —— 现在会先问"将删除 X 张卡片、Y 道题"，你确认才动手
- **删除笔记不再留垃圾数据** —— 标签关联、向量数据现在都会一起清干净

### 🟡 之前"声称修了但实际没修"的（已全部落实）

- 学习目标 scope 越权访问（能看到别人的笔记）→ 已加权限校验
- 文件夹归属校验（能把笔记塞进别人的文件夹）→ 已修
- 每日限额统一（后端 10 题，前端却显示 50）→ 已统一，前端动态显示
- 版本号唯一约束（并发保存会覆盖版本历史）→ 已加约束+重试
- 普通复习提交不校验是否到期 → 已加校验；快速复习不受影响
- 评估测验缓存签名缺 personal_note → 已修

### 🟢 体验类

- **XSS 漏洞**：笔记内容不再能注入脚本，公式和代码高亮不受影响
- **上传轮询**：失败状态（清洗失败/学习失败）不再空转到 10 分钟超时
- **双击提交**：四页答题加了防重复锁，SM-2 不会叠加
- **上传同名文件**：`a.pdf` 和 `a.md` 不再互相覆盖转换结果
- **图谱有向关系**：前置/后续关系方向不再被当成重复
- **清洗误删**：代码块和公式里的数字行不再被当页码删掉
- **时区**：日界统一为北京时间（之前 UTC 零点导致早上 8 点前的答题算到前一天）
- **三套答题 UI**：合并成共享组件 `QuizAnswerCard`，以后只改一处
- **四份数据库连接代码**：合并成一个公共模块

### ⚙️ 部署类

- **start.bat 无法启动** → 已修：换行符恢复 CRLF + 提示行去掉中文括号（之前 cmd 解析会崩）
- **Celery Beat 缺失**（定时任务永不执行）→ 启动脚本和 Docker 都补上了
- **任务名不匹配**（00:30 刷新调度引用不存在的任务名）→ 已修正
- **LLM 调用**：每次新建连接 → 改为共享连接池；限流真正生效
- **RAG 问答**：不再卡死整个服务器（同步等待改线程池）

---

## 🔢 数字说话

```
后端测试：41 通过 ✅   0 新增失败（5 个失败是修复前就有的老问题）
新增回归测试：6/6 通过（专门验证这次修的问题）
SM-2 复习算法测试：12/12 通过
前端类型检查：通过 ✅
```

---

## 🔜 还没做的（都不是代码问题）

- [ ] 安装测试插件：`pip install pytest-asyncio`（网络恢复后，装完 4 个老测试就能过）
- [ ] 换 JWT 密钥：`backend/.env` 里还是旧默认值，建议换随机密钥
- [ ] Docker 环境实测一次（compose 里新增的 worker/beat 服务没在容器里跑过）

---

## 👉 你只需要做 3 件事

| # | 做什么 | 花多久 |
|---|--------|--------|
| 1 | 重新跑 `start.bat`（已修好，会开 4 个窗口） | 1 分钟 |
| 2 | 换个 JWT 密钥（`backend/.env` 第 23 行，用 `python -c "import secrets; print(secrets.token_hex(32))"` 生成） | 1 分钟 |
| 3 | 改动还没提交 git，确认后提交（建议按 `fix/P0-*` 分阶段） | 5 分钟 |

---

## 🗺️ 文件地图（哪个文件是干嘛的）

| 文件 | 是什么 |
|------|--------|
| `deep_audit_report.md` | 审查报告：发现了什么问题（想了解背景看这个） |
| `fix_improvement_plan.md` | 修复方案+执行状态：每条怎么修的（想确认细节看这个） |
| `backend/tests/test_fixes.py` | 本次修复的 6 个回归测试 |
| `backend/app/tasks/common.py` | 合并后的数据库公共模块 |
| `frontend/src/components/quiz/QuizAnswerCard.tsx` | 合并后的共享答题组件 |
| `backend/app/utils/timeutil.py` | 北京时间日界工具 |

---

## 🆘 出错了怎么办

- **某个功能不对了** → 先在 `fix_improvement_plan.md` 里搜功能名，看是不是故意改的
- **怀疑是回归** → 跑 `python -m pytest backend/tests/test_fixes.py`（6 个测试，10 秒）
- **数据库出问题** → 有备份：`backend/data/db/engramnote.db.bak-20260815-200246`
- **改崩了** → 改动还没提交，`git checkout -- <文件>` 就能回到修复前

---

> 一句话总结：**数据安全 ✅ 定时任务 ✅ 体验修复 ✅ 测试通过 ✅ —— 项目现在比修复前安全得多，可以放心用了。**

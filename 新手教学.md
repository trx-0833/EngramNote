# EngramNote 新手完全教学

> **面向对象**：只会基础 Python 语法和函数的初学者
> **目标**：让你完全彻底地理解本项目到第 11 周为止所做的所有工作

---

## 目录

- [第零章：先搞懂几个大概念](#第零章先搞懂几个大概念)
- [第一章：项目是干什么的？](#第一章项目是干什么的)
- [第二章：项目长什么样？](#第二章项目长什么样)
- [第三章：后端代码逐文件讲解](#第三章后端代码逐文件讲解)
  - [3.1 config.py — 配置管理](#31-configpy--配置管理)
  - [3.2 database.py — 数据库连接](#32-databasepy--数据库连接)
  - [3.3 models/ — 数据模型](#33-models--数据模型)
  - [3.4 schemas/ — 数据格式校验](#34-schemas--数据格式校验)
  - [3.5 services/ — 业务逻辑](#35-services--业务逻辑)
  - [3.6 api/ — HTTP 接口](#36-api--http-接口)
  - [3.7 tasks/ — 异步任务](#37-tasks--异步任务)
  - [3.8 main.py — 应用入口](#38-mainpy--应用入口)
  - [3.9 清洗管道 — 后端核心模块](#39-清洗管道--后端核心模块)
  - [3.10 理解管道 — 后端核心模块](#310-理解管道--后端核心模块)
  - [3.11 复习调度引擎 — 后端核心模块](#311-复习调度引擎--后端核心模块)
  - [3.12 学习报告服务 — 后端核心模块（第9周）](#312-学习报告服务--后端核心模块第9周)
  - [3.13 全局异常中间件 — 后端核心模块（第10周）](#313-全局异常中间件--后端核心模块第10周)
  - [3.14 前端统一状态管理 — AuthContext（第11周）](#314-前端统一状态管理--authcontext第11周)
  - [3.15 统一状态组件与标签映射（第11周）](#315-统一状态组件与标签映射第11周)
- [第四章：前端代码逐文件讲解](#第四章前端代码逐文件讲解)
  - [4.1 client.ts — API 请求封装](#41-clientts--api-请求封装)
  - [4.2 App.tsx — 路由与认证](#42-apptsx--路由与认证)
  - [4.3 页面组件](#43-页面组件)
  - [4.4 清洗相关组件](#44-清洗相关组件)
  - [4.6 理解管道相关组件](#46-理解管道相关组件)
  - [4.7 复习调度相关组件](#47-复习调度相关组件)
  - [4.5 global.css — 设计系统](#45-globalcss--设计系统)
- [第五章：配置文件与基础设施](#第五章配置文件与基础设施)
- [第六章：数据流全链路走一遍](#第六章数据流全链路走一遍)
- [第七章：踩过的坑与解决思路](#第七章踩过的坑与解决思路)
- [第八章：如何启动项目](#第八章如何启动项目)

---

## 第零章：先搞懂几个大概念

在阅读代码之前，你需要先理解以下几个核心概念。别急，我们用最通俗的语言来解释。

### 0.1 什么是"后端"和"前端"？

想象一家餐厅：
- **前端** = 餐厅的大堂，顾客（用户）在这里点菜、看菜单、等上菜。在我们的项目中，前端就是你在浏览器里看到的页面——登录框、笔记列表、上传按钮等。
- **后端** = 餐厅的厨房，厨师在这里做菜、管理食材。在我们的项目中，后端就是运行在服务器上的 Python 程序——处理用户注册、存储笔记、转换文件等。

前端和后端之间通过 **HTTP 请求** 通信，就像顾客通过服务员向厨房下单。

### 0.2 什么是 API？

**API（Application Programming Interface，应用程序编程接口）** 就是后端暴露给前端的"菜单"。

比如：
- `POST /api/auth/register` — 前端调用这个接口来注册新用户
- `GET /api/notes` — 前端调用这个接口来获取笔记列表
- `POST /api/upload` — 前端调用这个接口来上传文件

每个 API 就像一个"服务窗口"，前端发送请求，后端返回结果。

### 0.3 什么是数据库？

数据库就像一个超级 Excel 表格，用来持久化存储数据。我们用的是 **SQLite**，它是最简单的数据库——整个数据库就是一个文件（`data/db/engramnote.db`），不需要安装任何数据库软件。

### 0.4 什么是 ORM？

**ORM（Object-Relational Mapping，对象关系映射）** 让你用 Python 类来操作数据库，而不用写 SQL 语句。

比如，不用写 `SELECT * FROM users WHERE email = 'xxx'`，而是写：
```python
result = await db.execute(select(User).where(User.email == "xxx"))
user = result.scalars().first()
```

我们用的 ORM 叫 **SQLAlchemy**，版本 2.0，支持异步操作（`async/await`）。

### 0.5 什么是异步（async/await）？

简单理解：`async/await` 让程序在等待慢操作（如数据库查询、网络请求）时，可以去干别的事，而不是傻等。

```python
# 同步写法：傻等数据库返回，期间什么都干不了
user = db.query(User).first()

# 异步写法：等待期间可以去处理其他请求
user = await db.execute(select(User))
```

FastAPI 天生支持异步，所以我们大量使用 `async def` 和 `await`。

### 0.6 什么是 JWT？

**JWT（JSON Web Token）** 是一种用户认证方式。流程如下：

1. 用户用邮箱和密码登录
2. 后端验证密码正确后，生成一个"令牌"（Token），返回给前端
3. 前端把这个 Token 存在浏览器的 `localStorage` 中
4. 之后每次请求，前端都在请求头里带上这个 Token：`Authorization: Bearer <token>`
5. 后端验证 Token 是否有效，从而知道"这是哪个用户"

就像你进小区要刷门禁卡一样——登录就是办卡，每次请求就是刷卡。

### 0.7 什么是 Celery？

**Celery** 是一个异步任务队列。有些操作很耗时（比如把一个 100 页的 PDF 转成 Markdown 可能要几分钟），如果让用户等几分钟才能继续操作，体验太差了。

解决方案：
1. 用户上传文件后，后端立刻返回"上传成功"
2. 同时把"转换这个文件"的任务丢给 Celery
3. Celery 在后台慢慢转换，前端通过轮询接口查询进度

就像你在餐厅点了菜，服务员给你一个取餐号，你不用在厨房门口等，可以先回座位聊天。

### 0.8 什么是策略模式？

**策略模式** 是一种设计模式，让你可以根据配置选择不同的实现方式，而不用改调用方的代码。

在我们的项目中，文件存储有两种方式：
- **本地文件系统**：文件存在 `data/storage/` 目录下（开发用，零依赖）
- **MinIO**：文件存在 MinIO 对象存储中（生产用，需要安装 MinIO）

上层代码只调用 `upload_file()`，不关心底层是本地存储还是 MinIO。`storage_service.py` 内部根据配置自动选择。

---

## 第一章：项目是干什么的？

**EngramNote** 是一个 AI 驱动的学习笔记管理与知识库工具。核心理念是：

> 以原始学习资料为起点，经过自动化清洗和理解，构建个人结构化知识库，并通过间隔重复与智能问答，形成"输入→内化→长期记忆"的学习闭环。

目前（第 1-8 周）实现的功能：
1. **用户注册/登录**：JWT 认证
2. **文件上传**：支持 PDF、图片、Office 文档、音视频
3. **文档转换**：PDF/图片/Office → Markdown（通过 mineru_plus），音视频 → 文本（通过 asr_plus）
4. **笔记管理**：列表、详情、搜索、删除、归档
5. **Markdown 渲染**：在浏览器中展示转换后的内容
6. **AI 清洗管道**：规则去噪（去空行/页眉页脚/水印/统一标点）+ 文本分块 + 嵌入向量生成 + 余弦相似度去重 + 清洗副本生成
7. **清洗界面**：三视图模式（原始版/清洗版/对比视图）、行级 diff 对比、重复块恢复/删除操作
8. **AI 理解管道**：章节切分 → LLM 摘要生成 → 知识点提取 → 题目生成
9. **智能问答**：RAG 问答（知识卡片关键词检索 + LLM 生成回答）
10. **知识卡片管理**：查看知识点列表、卡片详情、编辑、删除
11. **题目管理**：查看生成的选择题/填空题/简答题
12. **复习调度引擎**：SM-2 间隔重复算法、按需查询到期题目、答题即时反馈、复习统计与历史
13. **问题集浏览**：按笔记分组展示所有题目、题型/难度筛选、答案折叠显示

---

## 第二章：项目长什么样？

```
EngramNote/
├── backend/              ← 后端（Python + FastAPI）
│   ├── .env              ← 环境变量（API Key 等）
│   ├── requirements.txt  ← Python 依赖列表
│   ├── data/             ← 运行时数据（自动创建）
│   │   ├── db/           ← SQLite 数据库文件
│   │   ├── storage/      ← 上传的文件和 Markdown
│   │   ├── chroma/       ← Chroma 向量数据库（嵌入数据）
│   │   ├── models/       ← 嵌入模型缓存（BGE-M3）
│   │   └── celery/       ← Celery 队列数据
│   └── app/              ← 核心代码
│       ├── main.py       ← 应用入口
│       ├── config.py     ← 配置管理
│       ├── database.py   ← 数据库连接
│       ├── models/       ← 数据模型（对应数据库表）
│       │   ├── knowledge_card.py  ← 知识卡片模型
│       │   ├── quiz_item.py       ← 题库模型（含SM-2调度字段）
│       │   ├── review_log.py      ← 复习记录模型
│       ├── schemas/      ← 数据格式校验（API 请求/响应）
│       │   ├── review.py          ← 复习调度 Schema
│       │   ├── knowledge.py       ← 知识卡片和题目 Schema（含note_title字段）
│       ├── api/          ← HTTP 接口
│       │   ├── understanding.py  ← 理解管道 API
│       │   ├── review.py          ← 复习调度 API
│       ├── services/     ← 业务逻辑
│       │   ├── llm_service.py    ← LLM API 调用服务
│       │   ├── understanding_service.py  ← 理解管道服务
│       │   ├── rag_service.py    ← RAG 问答服务
│       │   ├── sm2_service.py    ← SM-2 间隔重复算法
│       │   ├── review_service.py ← 复习调度服务
│       └── tasks/        ← 异步任务
│           ├── understand_tasks.py  ← 理解管道异步任务
├── frontend/             ← 前端（React + TypeScript）
│   ├── package.json      ← 前端依赖
│   └── src/
│       ├── App.tsx       ← 根组件（路由）
│       ├── api/          ← API 请求封装
│       ├── components/   ← 公共组件（含 CleaningPanel、DiffView）
│       ├── pages/        ← 页面组件
│       │   ├── KnowledgeCards.tsx  ← 知识卡片列表页
│       │   ├── CardDetail.tsx      ← 知识卡片详情页
│       │   ├── QA.tsx              ← 智能问答页
│       │   ├── Review.tsx          ← 复习答题页
│       │   ├── QuestionSets.tsx    ← 问题集列表页（按笔记分组/筛选/答案折叠）
│       └── styles/       ← 样式
└── docker-compose.yml    ← Docker 配置（可选）
```

后端代码的组织遵循**分层架构**：

```
请求 → API层（接收请求、参数校验） → Service层（业务逻辑） → Model层（数据库操作）
                                      ↓
                              Storage层（文件存储）
                              Task层（异步任务）
```

每一层只做自己的事，不越界。这样代码清晰，也容易修改。

---

## 第三章：后端代码逐文件讲解

### 3.1 config.py — 配置管理

**文件位置**：`backend/app/config.py`

**作用**：集中管理所有配置项，从 `.env` 文件和环境变量中读取。

#### 核心概念：pydantic-settings

`pydantic-settings` 是一个库，让你用 Python 类来定义配置，自动从 `.env` 文件和环境变量中读取值。

```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    database_url: str = ""        # 默认值：空字符串
    storage_backend: str = "local"  # 默认值：local
    jwt_secret_key: str = "engramnote-dev-secret-change-in-production"
    mineru_backend: str = "pipeline"  # Mineru 解析后端："pipeline"（本地）或 "vlm-http-client"（云端）

    model_config = {
        "env_file": ".env",       # 从 .env 文件读取
        "extra": "ignore",        # 忽略 .env 中多余的变量
    }
```

当你创建 `Settings()` 实例时，它会自动读取 `.env` 文件。如果 `.env` 里有 `DATABASE_URL=xxx`，就用那个值；如果没有，就用默认值。

**`mineru_backend` 配置说明**：
- `"pipeline"`：使用本地 MinerU 模型解析文档（需要本地安装 MinerU 和模型）
- `"vlm-http-client"`：使用云端 API 解析文档（需要配置 `MINERU_API_TOKEN` 和 `MINERU_SERVER_URL`）
- `"hybrid-http-client"`：混合模式（本地+云端）
- 上传文件时前端也可以选择解析方式，覆盖此默认值

#### 核心概念：lru_cache 单例

```python
from functools import lru_cache

@lru_cache
def get_settings() -> Settings:
    return Settings()
```

`@lru_cache` 装饰器让 `get_settings()` 只在第一次调用时创建 `Settings()` 实例，之后每次调用都返回同一个实例。这叫**单例模式**，避免重复解析 `.env` 文件。

#### 目录结构定义

```python
PROJECT_ROOT = Path(__file__).resolve().parent.parent  # backend/ 目录
DATA_DIR = PROJECT_ROOT / "data"                        # backend/data/
STORAGE_DIR = DATA_DIR / "storage"                      # backend/data/storage/
DB_DIR = DATA_DIR / "db"                                # backend/data/db/
```

`Path(__file__)` 是当前文件的路径，`.resolve()` 得到绝对路径，`.parent` 往上一级。这样无论项目部署在哪里，路径都是正确的。

#### 智能默认值方法

`Settings` 类有几个方法，根据配置智能返回默认值：

```python
def get_database_url(self) -> str:
    if self.database_url:              # 如果用户配置了 PostgreSQL
        return self.database_url
    # 否则默认用 SQLite
    db_path = DB_DIR / "engramnote.db"
    db_path.parent.mkdir(parents=True, exist_ok=True)  # 自动创建目录
    return f"sqlite+aiosqlite:///{db_path}"
```

这种设计让开发环境零配置启动，生产环境只需改 `.env` 文件。

---

### 3.2 database.py — 数据库连接

**文件位置**：`backend/app/database.py`

**作用**：创建数据库引擎和会话工厂，提供数据库操作的基础设施。

#### 创建引擎

```python
from sqlalchemy.ext.asyncio import create_async_engine

engine = create_async_engine(database_url, **engine_kwargs)
```

**引擎**是数据库连接的管理者。它负责：
- 建立与数据库的连接
- 管理连接池（PostgreSQL 模式下）
- 执行 SQL 语句

#### SQLite 的特殊处理

SQLite 是文件数据库，和 PostgreSQL 有些不同：

```python
_is_sqlite = database_url.startswith("sqlite")

# SQLite 不支持多线程并发访问，需要关闭这个限制
connect_args = {}
if _is_sqlite:
    connect_args = {"check_same_thread": False}

# SQLite 不支持连接池参数
if not _is_sqlite:
    engine_kwargs["pool_size"] = 5
    engine_kwargs["max_overflow"] = 10
```

#### 会话工厂

```python
from sqlalchemy.ext.asyncio import async_sessionmaker, AsyncSession

async_session = async_sessionmaker(
    engine,
    class_=AsyncSession,
    expire_on_commit=False,  # 提交后不过期对象属性
)
```

**会话（Session）** 是一次数据库操作的上下文。每个 HTTP 请求创建一个会话，请求结束后关闭。`async_sessionmaker` 是会话工厂，用来批量创建会话。

`expire_on_commit=False` 的意思是：当你执行 `await db.commit()` 后，之前查询到的对象属性不会失效。这在异步代码中很重要，否则访问属性时会触发懒加载，而异步环境下懒加载会报错。

#### 依赖注入：get_db()

```python
async def get_db():
    async with async_session() as session:
        try:
            yield session    # 把会话"交给"路由函数使用
        finally:
            await session.close()  # 请求结束后关闭会话
```

这是 FastAPI 的**依赖注入**机制。路由函数通过 `Depends(get_db)` 获取数据库会话，不用自己创建和关闭：

```python
@router.get("/notes")
async def list_notes(db: AsyncSession = Depends(get_db)):  # 自动获取会话
    ...
```

#### 初始化数据库：init_db()

```python
async def init_db():
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
```

`Base.metadata.create_all` 会根据所有模型类自动创建对应的数据表。`run_sync` 是因为 `create_all` 是同步函数，需要包装成异步调用。

---

### 3.3 models/ — 数据模型

数据模型定义了数据库表的结构。每个 Python 类对应一张表，每个类属性对应一列。

#### base.py — 基础模型

```python
class BaseModel(Base):
    __abstract__ = True  # 不会创建对应的表，只作为父类

    id: Mapped[str] = mapped_column(
        primary_key=True,
        default=lambda: str(uuid.uuid4()),  # 自动生成 UUID
    )
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        server_default=func.now(),  # 数据库自动填充当前时间
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        server_default=func.now(),
        onupdate=func.now(),  # 记录更新时自动刷新
    )
```

**关键概念**：
- `__abstract__ = True`：这个类本身不会创建表，但继承它的子类会。这叫**模板方法模式**——公共字段只写一次。
- `id` 使用 UUID 而非自增整数：UUID 是全局唯一的随机字符串，不会暴露数据量。
- `Mapped[str]`：SQLAlchemy 2.0 的类型注解语法，表示这个字段是字符串类型。
- `server_default=func.now()`：由数据库服务器生成时间，而不是 Python 代码，确保一致性。
- `onupdate=func.now()`：每次更新记录时自动刷新时间。

#### user.py — 用户模型

```python
class User(BaseModel):  # 继承 BaseModel，自动获得 id、created_at、updated_at
    __tablename__ = "users"  # 数据库中的表名

    email: Mapped[str] = mapped_column(String(255), unique=True, index=True, nullable=False)
    username: Mapped[str] = mapped_column(String(100), unique=True, index=True, nullable=False)
    hashed_password: Mapped[str] = mapped_column(String(255), nullable=False)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
```

**字段解释**：
- `unique=True`：值必须唯一，不能有两个用户用同一个邮箱
- `index=True`：创建索引，加速按邮箱/用户名查询
- `nullable=False`：不允许为空
- `hashed_password`：存的是密码的**哈希值**，不是明文密码！即使数据库泄露，攻击者也无法知道用户的密码

**为什么不存明文密码？** 因为如果数据库被黑客窃取，明文密码就暴露了。哈希是单向的——你可以从密码算出哈希，但无法从哈希反推密码。验证时，把用户输入的密码哈希一下，和数据库中的哈希比对即可。

#### note.py — 笔记模型

这是最复杂的模型，包含两个枚举类和一个模型类。

**枚举类 SourceType**：文件来源类型

```python
class SourceType(str, enum.Enum):
    pdf = "pdf"
    image = "image"
    docx = "docx"
    pptx = "pptx"
    xlsx = "xlsx"
    audio = "audio"
    video = "video"
```

枚举（Enum）是一种特殊的类，限定变量只能取预定义的值。比如 `source_type` 只能是 `pdf`、`image` 等，不能是随便什么字符串。这可以防止无效数据进入数据库。

**枚举类 NoteStatus**：笔记状态

```python
class NoteStatus(str, enum.Enum):
    uploading = "uploading"       # 上传中
    converting = "converting"     # 转换中
    converted = "converted"       # 已转换
    cleaning = "cleaning"         # 清洗中
    cleaned = "cleaned"           # 已清洗
    cleaning_failed = "cleaning_failed"  # 清洗失败（与转换失败 failed 区分）
    learning = "learning"         # 学习中
    archived = "archived"         # 已归档
    failed = "failed"             # 转换失败
```

这叫**状态机**。笔记从上传到归档，经历一系列状态变化：

```
uploading → converting → converted → cleaning → cleaned → learning → archived
                ↓                        ↓            ↓
              failed                   failed    cleaning_failed
```

任何阶段都可能失败（`failed`），记录错误信息。清洗阶段有专属的 `cleaning_failed` 状态，用户可以从该状态重新触发清洗。

**Note 模型**：

```python
class Note(BaseModel):
    __tablename__ = "notes"

    user_id: Mapped[str] = mapped_column(String, ForeignKey("users.id"), index=True)
    title: Mapped[str] = mapped_column(String(500), default="未命名笔记")
    source_type: Mapped[SourceType] = mapped_column(Enum(SourceType))
    original_file_path: Mapped[str] = mapped_column(String(1000), default="")
    original_md_path: Mapped[str] = mapped_column(String(1000), default="")
    clean_md_path: Mapped[Optional[str]] = mapped_column(String(1000), nullable=True)
    status: Mapped[NoteStatus] = mapped_column(Enum(NoteStatus), default=NoteStatus.uploading)
    file_size: Mapped[int] = mapped_column(Integer, default=0)
    page_count: Mapped[Optional[int]] = mapped_column(Integer, nullable=True)
    error_message: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    metadata_: Mapped[Optional[Dict[str, Any]]] = mapped_column("metadata", JSON, nullable=True)
```

**关键字段解释**：
- `user_id` + `ForeignKey("users.id")`：外键，关联到 users 表的 id 字段。表示"这个笔记属于哪个用户"。
- `original_file_path`：原始文件在存储中的路径，格式为 `{user_id}/{note_id}/{filename}`
- `original_md_path`：转换后的原始 Markdown 文件路径
- `clean_md_path`：清洗后的 Markdown 文件路径（可为空，因为清洗功能还没实现）
- `metadata_`：注意 Python 属性名带下划线，但数据库列名是 `metadata`。因为 SQLAlchemy 的 `Base` 类有个属性叫 `metadata`，直接用会冲突。

---

### 3.4 schemas/ — 数据格式校验

**文件位置**：`backend/app/schemas/`

**作用**：定义 API 请求和响应的数据格式，自动校验和序列化。

#### 为什么需要 schemas？

模型（models）是给数据库用的，schemas 是给 API 用的。它们虽然字段类似，但职责不同：

- **模型**：关心数据怎么存储（表名、列类型、索引、外键）
- **Schema**：关心数据怎么传输（哪些字段必填、值范围、是否返回给前端）

比如，用户模型有 `hashed_password`，但 API 响应中绝对不能返回密码（哪怕是哈希值），所以 `UserResponse` 没有 `hashed_password` 字段。

#### user.py — 用户 Schema

```python
# 请求模型（前端发过来的数据）
class UserRegisterRequest(BaseModel):
    email: EmailStr                    # 自动校验邮箱格式
    username: str = Field(
        min_length=2,
        max_length=50,
        pattern=r"^[a-zA-Z0-9]+$",    # 仅允许英文字母和数字
        description="用户名只能包含英文字母和数字",
    )
    password: str = Field(min_length=6, max_length=100)  # 至少6位

class UserLoginRequest(BaseModel):
    email: EmailStr
    password: str

# 响应模型（返回给前端的数据）
class UserResponse(BaseModel):
    id: str
    email: str
    username: str
    is_active: bool
    created_at: datetime
    # 注意：没有 hashed_password！

    model_config = {"from_attributes": True}  # 允许从 ORM 对象自动转换
```

`from_attributes = True` 让 Pydantic 可以直接从 SQLAlchemy 模型对象提取数据，不用手动一个个赋值：

```python
# 有了 from_attributes，可以这样：
UserResponse.model_validate(user)  # 自动从 user 对象提取字段

# 没有的话，得这样：
UserResponse(id=user.id, email=user.email, username=user.username, ...)
```

#### note.py — 笔记 Schema

```python
class NoteResponse(BaseModel):
    id: str
    user_id: str
    title: str
    source_type: SourceType
    status: NoteStatus
    file_size: int
    page_count: Optional[int] = None
    error_message: Optional[str] = None
    created_at: datetime
    updated_at: datetime

class NoteDetailResponse(NoteResponse):
    """笔记详情，比列表多了 Markdown 内容"""
    original_md_content: Optional[str] = None
    clean_md_content: Optional[str] = None
    metadata_: Optional[Dict] = None

class NoteListResponse(BaseModel):
    items: List[NoteResponse]  # 笔记列表
    total: int                  # 总数
    page: int                   # 当前页码
    page_size: int              # 每页数量
```

`NoteDetailResponse` 继承 `NoteResponse`，额外增加了 Markdown 内容字段。这是**继承复用**——列表和详情共享大部分字段，详情只是多了内容。

---

### 3.5 services/ — 业务逻辑

**文件位置**：`backend/app/services/`

**作用**：封装核心业务逻辑，被 API 层调用。这一层是整个项目最"有料"的部分。

#### auth_service.py — 认证服务

**密码哈希**：

```python
import bcrypt

def hash_password(password: str) -> str:
    salt = bcrypt.gensalt()                              # 生成随机盐
    hashed = bcrypt.hashpw(password.encode("utf-8"), salt)  # 哈希
    return hashed.decode("utf-8")                        # 转回字符串

def verify_password(plain_password: str, hashed_password: str) -> bool:
    return bcrypt.checkpw(
        plain_password.encode("utf-8"),
        hashed_password.encode("utf-8")
    )
```

**盐（salt）** 是一段随机字符串，和密码拼在一起再哈希。这样即使两个用户的密码相同，哈希值也不同，防止彩虹表攻击。

**JWT Token**：

```python
from jose import jwt

def create_access_token(user_id: str) -> str:
    expire = datetime.now(timezone.utc) + timedelta(minutes=settings.jwt_expire_minutes)
    payload = {"sub": user_id, "exp": expire}  # sub=主体(用户ID), exp=过期时间
    return jwt.encode(payload, settings.jwt_secret_key, algorithm=settings.jwt_algorithm)

def decode_access_token(token: str) -> Optional[str]:
    try:
        payload = jwt.decode(token, settings.jwt_secret_key, algorithms=[settings.jwt_algorithm])
        return payload.get("sub")  # 返回用户 ID
    except JWTError:
        return None  # Token 无效或过期
```

JWT 的工作原理：
1. **创建**：把用户 ID 和过期时间编码成一个字符串，用密钥签名
2. **验证**：用同样的密钥解码，验证签名是否正确、是否过期
3. **无状态**：服务器不需要存储 Token，只需要密钥就能验证

**注册流程**：

```python
async def register_user(db: AsyncSession, req: UserRegisterRequest) -> User:
    # 1. 检查邮箱是否已存在
    result = await db.execute(select(User).where(User.email == req.email))
    if result.scalars().first():
        raise ValueError("该邮箱已被注册")

    # 2. 检查用户名是否已存在
    result = await db.execute(select(User).where(User.username == req.username))
    if result.scalars().first():
        raise ValueError("该用户名已被使用")

    # 3. 创建用户（密码哈希后存储）
    user = User(
        email=req.email,
        username=req.username,
        hashed_password=hash_password(req.password),
    )
    db.add(user)          # 添加到会话
    await db.commit()     # 提交到数据库
    await db.refresh(user)  # 刷新对象，获取数据库生成的字段（如 id、created_at）
    return user
```

#### note_service.py — 笔记服务

**分页查询**：

```python
async def get_notes_list(db, user_id, page=1, page_size=20, keyword=None):
    # 基础查询：只查当前用户的笔记
    query = select(Note).where(Note.user_id == user_id)

    # 可选：关键词搜索
    if keyword:
        query = query.where(Note.title.ilike(f"%{keyword}%"))

    # 计算总数
    count_query = select(func.count()).select_from(query.subquery())
    total = (await db.execute(count_query)).scalar() or 0

    # 分页：排序 + 偏移 + 限制
    query = query.order_by(Note.created_at.desc()).offset((page - 1) * page_size).limit(page_size)
    result = await db.execute(query)
    notes = list(result.scalars().all())

    return notes, total
```

分页逻辑：
- `offset((page - 1) * page_size)`：跳过前面的记录
- `limit(page_size)`：只取 page_size 条
- 比如 page=2, page_size=20：跳过前 20 条，取第 21-40 条

**Markdown 内容读取**：

```python
async def get_note_markdown_content(note: Note) -> Optional[str]:
    if not note.original_md_path:
        return None
    try:
        data = get_object_bytes(settings.minio_bucket_markdown, note.original_md_path)
        return data.decode("utf-8")  # 字节 → 字符串
    except Exception:
        return None  # 读取失败不影响其他数据
```

Markdown 内容存在对象存储中，不在数据库里。数据库只存路径引用。这是**大对象外部存储**模式——数据库存元数据，文件存内容。

#### storage_service.py — 存储服务

这是**策略模式**的典型实现。

**统一接口**：

```python
def upload_file(bucket, object_name, file_path, content_type="application/octet-stream"):
    if settings.storage_backend == "minio":
        _upload_file_minio(bucket, object_name, file_path, content_type)
    else:
        _upload_file_local(bucket, object_name, file_path)
```

上层代码只调用 `upload_file()`，不用关心底层是本地存储还是 MinIO。

**本地存储实现**：

```python
def _upload_file_local(bucket, object_name, file_path):
    path = _resolve_path(bucket, object_name)  # 映射为本地路径
    path.parent.mkdir(parents=True, exist_ok=True)  # 创建目录
    shutil.copy2(file_path, path)  # 复制文件
```

**路径映射**：把 `bucket/object_name` 映射为本地文件路径。比如：
- bucket = "original-files"
- object_name = "user-id/note-id/document.pdf"
- 本地路径 = `data/storage/original-files/user-id/note-id/document.pdf`

**安全检查**：

```python
def _resolve_path(bucket, object_name):
    root = _get_storage_root() / bucket
    path = root / object_name
    path = path.resolve()  # 解析 ".." 和符号链接
    root = root.resolve()
    if not str(path).startswith(str(root)):
        raise ValueError(f"非法路径: {object_name}")  # 防止路径遍历攻击
    return path
```

如果有人传入 `object_name = "../../etc/passwd"`，`resolve()` 会解析为 `/etc/passwd`，不在 `root` 下，就会被拒绝。这叫**路径遍历攻击防护**。

---

### 3.6 api/ — HTTP 接口

**文件位置**：`backend/app/api/`

**作用**：定义 HTTP 接口，接收请求、调用服务层、返回响应。

#### router.py — 路由注册

```python
api_router = APIRouter()
api_router.include_router(auth_router, prefix="/auth", tags=["认证"])
api_router.include_router(notes_router, prefix="/notes", tags=["笔记"])
api_router.include_router(upload_router, prefix="/upload", tags=["上传"])
```

把三个模块的路由统一注册到一个总路由器上，最终在 `main.py` 中挂载到 `/api` 前缀下。所以完整路径是：
- `/api/auth/register`
- `/api/notes`
- `/api/upload`

#### auth.py — 认证接口

**认证依赖**：

```python
async def get_current_user_dependency(request: Request, db: AsyncSession = Depends(get_db)) -> User:
    # 1. 从请求头提取 Token
    auth_header = request.headers.get("Authorization")
    if not auth_header or not auth_header.startswith("Bearer "):
        raise HTTPException(status_code=401, detail="未提供认证令牌")

    # 2. 解码 Token，获取用户 ID
    token = auth_header[7:]  # 去掉 "Bearer " 前缀
    user_id = decode_access_token(token)
    if not user_id:
        raise HTTPException(status_code=401, detail="无效或过期的令牌")

    # 3. 从数据库查询用户
    result = await db.execute(select(User).where(User.id == user_id))
    user = result.scalars().first()
    if not user or not user.is_active:
        raise HTTPException(status_code=401, detail="用户不存在或已禁用")
    return user
```

这个函数是 FastAPI 的**依赖**，其他需要认证的接口通过 `Depends(get_current_user_dependency)` 注入当前用户：

```python
@router.get("/me")
async def get_me(current_user: User = Depends(get_current_user_dependency)):
    return UserResponse.model_validate(current_user)
```

**注册接口**：

```python
@router.post("/register", response_model=TokenResponse, status_code=201)
async def register(req: UserRegisterRequest, db: AsyncSession = Depends(get_db)):
    try:
        user = await register_user(db, req)
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))

    token = create_access_token(user.id)  # 注册成功自动签发 Token
    return TokenResponse(access_token=token, user=UserResponse.model_validate(user))
```

注意：注册成功后直接返回 Token，用户不需要再登录。这叫**注册即登录**模式。

#### upload.py — 上传接口

这是最复杂的接口，完整流程如下：

```python
@router.post("", response_model=NoteResponse, status_code=201)
async def upload_document(
    file: UploadFile,
    backend: Optional[str] = Form(None),  # 解析后端选择（可选）
    current_user: User,
    db: AsyncSession,
):
    # 1. 校验文件扩展名
    ext = os.path.splitext(file.filename)[1].lower()
    if ext not in ALLOWED_EXTS:
        raise HTTPException(status_code=400, detail=f"不支持的文件格式: {ext}")

    # 2. 读取文件内容并校验大小
    content = await file.read()
    if len(content) > settings.max_upload_size_mb * 1024 * 1024:
        raise HTTPException(status_code=400, detail="文件大小超过限制")

    # 3. 创建笔记记录
    note = Note(id=note_id, user_id=current_user.id, title=..., status=NoteStatus.uploading, ...)
    db.add(note)
    await db.commit()

    # 4. 保存原始文件到存储
    upload_file(settings.minio_bucket_original, object_name, tmp_path, ...)

    # 5. 触发 Celery 异步转换任务，传递用户选择的解析后端
    note.status = NoteStatus.converting
    await db.commit()
    convert_document_task.delay(note_id, object_name, source_type.value, backend=backend)

    return NoteResponse.model_validate(note)
```

**`backend` 参数说明**：
- `None`（默认）：使用 `config.py` 中 `mineru_backend` 的默认值
- `"pipeline"`：强制使用本地 MinerU 模型解析
- `"vlm-http-client"`：强制使用云端 API 解析

**临时文件中转**：

```python
with tempfile.NamedTemporaryFile(delete=False, suffix=ext) as tmp:
    tmp.write(content)
    tmp_path = tmp.name
upload_file(..., tmp_path, ...)
os.unlink(tmp_path)  # 上传后删除临时文件
```

为什么用临时文件？因为 `upload_file` 需要一个文件路径参数。先把内容写入临时文件，上传后删除。

---

### 3.7 tasks/ — 异步任务

**文件位置**：`backend/app/tasks/`

#### celery_app.py — Celery 配置

```python
celery_app = Celery(
    "engramnote",
    broker=settings.get_celery_broker_url(),       # 消息队列
    backend=settings.get_celery_result_backend(),   # 结果存储
)

celery_app.conf.update(
    task_serializer="json",          # 用 JSON 序列化任务参数
    task_acks_late=True,             # 任务完成后才确认（防止崩溃丢失）
    worker_prefetch_multiplier=1,    # 每次只预取一个任务
)
```

**broker** 是消息队列，用来分发任务。我们默认用文件系统作为 broker（零依赖），消息存在 `data/celery/broker/` 目录下。

**task_acks_late=True**：默认情况下，Celery 收到任务就确认。但如果 worker 在执行过程中崩溃，任务就丢了。设为 `True` 后，只有任务执行完才确认，崩溃后任务会被重新分配。

#### convert_tasks.py — 文档转换任务

这是 Celery 任务的核心实现。

**为什么需要独立的数据库连接？**

Celery worker 运行在独立进程中，不在 FastAPI 的上下文里，所以不能使用 FastAPI 的数据库连接池。需要创建自己的：

```python
_sync_engine = None
_sync_session_factory = None

def _get_sync_session():
    global _sync_engine, _sync_session_factory
    if _sync_session_factory is None:
        _sync_engine = create_async_engine(settings.database_url, echo=False)
        _sync_session_factory = async_sessionmaker(_sync_engine, expire_on_commit=False)
    return _sync_session_factory
```

使用全局变量缓存，避免每次任务都创建新的引擎。

**转换逻辑**：

```python
async def _convert_document(note_id, file_path, source_type, backend=None):
    # 1. 从存储下载原始文件
    download_file(settings.minio_bucket_original, file_path, local_file)

    # 2. 根据类型选择转换引擎
    if source_type in (pdf, image, docx, pptx, xlsx):
        from mineru_plus.converter import convert
        # backend 参数：前端上传时选择，或使用 config.py 中的 mineru_backend 默认值
        result = convert(local_file, backend=backend or settings.mineru_backend)
        markdown_content = result.markdown_content
    elif source_type in (audio, video):
        from asr_plus.asr_engine import ASREngine
        engine = ASREngine.get_instance()
        transcribed_text, detected_lang = engine.transcribe_file(local_file)
        # 用 DeepSeek 润色 + 生成标题
        polished_text = restore_punctuation(transcribed_text, ...)
        title = generate_title(transcribed_text, ...)
        markdown_content = f"# {title}\n\n{polished_text}"

    # 3. 上传 Markdown 到存储
    upload_bytes(settings.minio_bucket_markdown, md_object_name, markdown_content.encode("utf-8"))

    # 4. 更新笔记状态
    await _update_note_status(note_id, NoteStatus.converted, original_md_path=md_object_name)
```

**`backend` 参数说明**：
- 前端上传时可以通过 `backend` 参数指定解析方式
- `backend=None` 时使用 `settings.mineru_backend` 的默认值（在 config.py 中配置）
- `"pipeline"` 使用本地 MinerU 模型，`"vlm-http-client"` 使用云端 API

**Celery 任务定义**：

```python
@celery_app.task(bind=True, max_retries=2, default_retry_delay=60)
def convert_document_task(self, note_id, file_path, source_type, backend=None):
    import asyncio
    try:
        asyncio.run(_convert_document(note_id, file_path, source_type, backend))
    except Exception as exc:
        try:
            self.retry(exc=exc)  # 重试
        except self.MaxRetriesExceededError:
            asyncio.run(_update_note_status(note_id, NoteStatus.failed, error_message=...))
```

- `bind=True`：可以访问 `self`（任务实例），用于重试
- `max_retries=2`：最多重试 2 次
- `default_retry_delay=60`：重试间隔 60 秒
- `asyncio.run()`：在同步的 Celery 任务中运行异步代码
- `backend`：可选参数，前端上传时指定的解析后端，传递给 `_convert_document`

---

### 3.8 main.py — 应用入口

**文件位置**：`backend/app/main.py`

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    await init_db()  # 启动时：创建数据库表
    yield            # 应用运行中...
    # yield 之后是关闭时的清理逻辑（当前无需清理）

app = FastAPI(title=settings.app_name, lifespan=lifespan)

# CORS：允许前端开发服务器跨域访问
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],  # Vite 默认端口
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 注册路由
app.include_router(api_router, prefix="/api")

# 健康检查
@app.get("/health")
async def health_check():
    return {"status": "ok", "app": settings.app_name}
```

**CORS 是什么？** 浏览器有同源策略：前端（localhost:5173）不能直接请求后端（localhost:8000），因为端口不同。CORS 中间件就是告诉浏览器"我允许这个来源的请求"。

**lifespan 是什么？** FastAPI 的生命周期管理。`yield` 之前的代码在启动时执行，`yield` 之后的代码在关闭时执行。替代了旧的 `@app.on_event("startup")` 写法。

---

### 3.9 清洗管道 — 后端核心模块

**涉及文件**：
- `backend/app/services/cleaning_service.py` — 规则清洗与副本生成
- `backend/app/services/embedding_service.py` — 嵌入向量生成与向量存储
- `backend/app/tasks/clean_tasks.py` — Celery 异步清洗任务
- `backend/app/api/cleaning.py` — 清洗 API 端点

#### 什么是清洗管道？

清洗管道是 AI 三大角色（清洗→理解→持久化）中的第一个。它的目标是：**从原始 Markdown 中去除噪声和重复内容，生成一份干净的副本，绝对不改变原意、不总结、不润色。**

清洗管道的完整流程：

```
原始 Markdown → 规则清洗（去噪）→ 文本分块 → 嵌入向量生成 → 余弦相似度去重 → 生成清洗副本
```

#### cleaning_service.py — 规则清洗与副本生成

**规则清洗（clean_rules）**：

```python
def clean_rules(text: str) -> str:
    """规则清洗：去空行、去页眉页脚、去水印、统一标点"""
    lines = text.split('\n')
    cleaned = []
    for line in lines:
        stripped = line.strip()
        # 去空行
        if not stripped:
            continue
        # 去页眉页脚（正则匹配常见模式）
        if re.match(r'^第\s*\d+\s*页', stripped):
            continue
        if re.match(r'^\s*-\s*\d+\s*-\s*$', stripped):
            continue
        # 去水印
        if '水印' in stripped or 'watermark' in stripped.lower():
            continue
        # 统一中文标点
        line = line.replace(',', '，').replace(';', '；').replace(':', '：')
        cleaned.append(line)
    return '\n'.join(cleaned)
```

**核心思想**：用正则表达式匹配常见的噪声模式，逐行过滤。这是"规则清洗"——不需要 AI，纯靠模式匹配。

**文本分块（split_into_chunks）**：

```python
def split_into_chunks(text: str, chunk_size: int = 500, overlap: int = 50) -> list:
    """将文本按固定长度分块，块之间有重叠"""
    chunks = []
    start = 0
    while start < len(text):
        end = start + chunk_size
        chunk = text[start:end]
        chunks.append(chunk)
        start += chunk_size - overlap  # 重叠部分确保不丢失上下文
    return chunks
```

**为什么需要重叠？** 如果一个句子恰好在分块边界被切断，重叠可以确保它在上一个块的尾部和下一个块的头部都有出现，避免语义丢失。

**生成清洗副本（generate_clean_copy）**：

```python
def generate_clean_copy(original_text: str, duplicate_groups: list) -> str:
    """生成清洗副本：重复块用 HTML 注释包裹"""
    lines = original_text.split('\n')
    for group in duplicate_groups:
        # 保留第一个块，其余块注释掉
        for block in group[1:]:
            start, end = block['start_line'], block['end_line']
            for i in range(start, end):
                if i < len(lines):
                    lines[i] = f'<!-- 重复块（相似度 {block["similarity"]:.4f}）-->\n{lines[i]}\n<!-- /重复块 -->'
    return '\n'.join(lines)
```

**为什么用 HTML 注释？** 这样在 Markdown 渲染时，重复块不会显示，但原文仍然保留在文件中。用户可以通过"恢复块"操作取消注释，也可以通过"删除块"操作彻底删除。

**恢复块（restore_block）和删除块（delete_block）**：

```python
def restore_block(clean_text: str, block_index: int) -> str:
    """恢复被注释的重复块：去掉 HTML 注释标记"""
    # 找到对应块的注释标记并移除
    ...

def delete_block(clean_text: str, block_index: int) -> str:
    """彻底删除重复块：连同注释标记一起删除"""
    # 找到对应块并完全移除
    ...
```

#### embedding_service.py — 嵌入向量生成与向量存储

这是清洗管道中最"AI"的部分，涉及两个核心概念：**嵌入（Embedding）** 和 **向量数据库**。

**什么是嵌入？**

嵌入就是把文本转换成一组数字（向量），使得语义相似的文本对应的向量在数学上也相近。比如"猫是一种动物"和"猫是哺乳动物"的嵌入向量会很接近，而"猫是一种动物"和"今天天气不错"的嵌入向量会相差很远。

我们使用的嵌入模型是 **BAAI/bge-m3**，它支持多语言（包括中文），通过 ModelScope 下载（Xorbits/bge-m3）。

**EmbeddingService — 嵌入服务**：

```python
class EmbeddingService:
    """嵌入向量生成服务（延迟加载模型）"""

    _model = None  # 类变量，全局共享一个模型实例

    @classmethod
    def get_model(cls):
        if cls._model is None:
            cls._model = cls._load_model()
        return cls._model

    @staticmethod
    def _load_model():
        """加载嵌入模型，优先从 ModelScope 下载"""
        model_name = settings.embedding_model  # "BAAI/bge-m3"
        # 优先尝试 ModelScope 下载
        model_dir = EmbeddingService._download_from_modelscope(model_name)
        if model_dir:
            return SentenceTransformer(model_dir)
        # 回退到 HuggingFace
        return SentenceTransformer(model_name)

    @staticmethod
    def _download_from_modelscope(model_name: str):
        """通过 ModelScope 下载模型（国内可访问）"""
        modelscope_mapping = {
            "BAAI/bge-m3": "Xorbits/bge-m3",
        }
        ms_model_name = modelscope_mapping.get(model_name)
        if not ms_model_name:
            return None
        from modelscope import snapshot_download
        cache_dir = str(DATA_DIR / "models")
        model_dir = snapshot_download(ms_model_name, cache_dir=cache_dir)
        return model_dir
```

**延迟加载**：模型文件很大（约 2GB），不启动时就不加载。第一次调用 `get_model()` 时才加载，之后复用同一个实例。

**ModelScope 优先**：HuggingFace 在国内无法直接访问，所以优先通过 ModelScope 下载。ModelScope 是国内的 AI 模型平台，镜像了 HuggingFace 上的主流模型。

**VectorStore — 向量存储**：

```python
class VectorStore:
    """Chroma 向量数据库封装"""

    def __init__(self):
        self._client = chromadb.PersistentClient(path=str(DATA_DIR / "chroma"))
        self._collection = self._client.get_or_create_collection("note_chunks")

    def add_chunks(self, note_id: str, chunks: list, embeddings: list):
        """添加文本块及其嵌入向量"""
        ids = [f"{note_id}_chunk_{i}" for i in range(len(chunks))]
        metadatas = [{"note_id": note_id, "chunk_index": i} for i in range(len(chunks))]
        self._collection.add(ids=ids, documents=chunks, embeddings=embeddings, metadatas=metadatas)

    def find_duplicates(self, note_id: str, embeddings: list, threshold: float = 0.85) -> list:
        """找出相似度超过阈值的重复块"""
        duplicate_groups = []
        n = len(embeddings)
        for i in range(n):
            for j in range(i + 1, n):
                similarity = cosine_similarity(embeddings[i], embeddings[j])
                if similarity > threshold:
                    duplicate_groups.append({
                        "block_a": i, "block_b": j,
                        "similarity": similarity
                    })
        return duplicate_groups
```

**Chroma 是什么？** Chroma 是一个嵌入式向量数据库，类似于 SQLite 之于关系型数据库——不需要单独部署服务，数据直接存在本地文件（`data/chroma/`）中。

**余弦相似度**：衡量两个向量方向的相似程度，值域 [-1, 1]。值越接近 1，表示两个向量越相似。我们设定阈值为 0.85，超过这个值就认为是重复内容。

#### clean_tasks.py — Celery 异步清洗任务

```python
@celery_app.task(bind=True, max_retries=2, default_retry_delay=60)
def clean_document_task(self, note_id: str):
    """异步清洗任务：读取原文→规则清洗→分块→嵌入→去重→生成副本"""
    import asyncio
    import logging
    logger = logging.getLogger(__name__)

    try:
        # 检查笔记是否已被用户停止清洗
        current_status = asyncio.run(_get_note_status(note_id))
        if current_status == NoteStatus.cleaning_failed:
            logger.info(f"笔记已被用户停止清洗，跳过任务 (note_id={note_id})")
            return

        # 将状态更新为 cleaning（自动触发时必要，手动触发时冗余但无害）
        try:
            asyncio.run(_update_note_status(note_id, NoteStatus.cleaning))
        except Exception:
            pass

        asyncio.run(_clean_document(note_id))
    except Exception as exc:
        logger.error(f"清洗任务异常 (note_id={note_id}): {exc}", exc_info=True)

        # 重试前检查：如果用户已停止清洗，不再重试
        try:
            current_status = asyncio.run(_get_note_status(note_id))
            if current_status == NoteStatus.cleaning_failed:
                logger.info(f"笔记已被用户停止清洗，放弃重试 (note_id={note_id})")
                return
        except Exception:
            pass  # 状态查询失败时仍尝试重试

        try:
            self.retry(exc=exc)
        except self.MaxRetriesExceededError:
            try:
                asyncio.run(_update_note_status(
                    note_id, NoteStatus.cleaning_failed,
                    error_message=f"清洗任务重试失败: {str(exc)}",
                ))
            except Exception as update_err:
                logger.error(f"更新笔记状态失败 (note_id={note_id}): {update_err}")

async def _clean_document(note_id: str):
    # 1. 读取原始 Markdown
    note = await _get_note(note_id)
    original_text = get_object_bytes(settings.minio_bucket_markdown, note.original_md_path).decode()

    # 2. 规则清洗
    cleaned_text = clean_rules(original_text)

    # 3. 文本分块
    chunks = split_into_chunks(cleaned_text, settings.chunk_size, settings.chunk_overlap)

    # 4. 生成嵌入向量
    model = EmbeddingService.get_model()
    embeddings = model.encode(chunks)

    # 5. 存入向量数据库
    vector_store = VectorStore()
    vector_store.add_chunks(note_id, chunks, embeddings)

    # 6. 查找重复块
    duplicates = vector_store.find_duplicates(note_id, embeddings, settings.similarity_threshold)

    # 7. 生成清洗副本
    clean_copy = generate_clean_copy(cleaned_text, duplicates)

    # 8. 上传清洗副本到存储
    clean_md_path = note.original_md_path.replace("original", "clean")
    upload_bytes(settings.minio_bucket_markdown, clean_md_path, clean_copy.encode())

    # 9. 更新笔记状态
    await _update_note_status(note_id, NoteStatus.cleaned, clean_md_path=clean_md_path)
```

**自动触发**：在 `convert_tasks.py` 中，转换成功后自动触发清洗：

```python
# convert_tasks.py 中转换成功后
try:
    from .clean_tasks import clean_document_task
    clean_document_task.delay(note_id)
except Exception:
    pass  # 清洗触发失败不影响转换结果
```

这样用户上传 PDF 后，转换完成会自动进入清洗流程，无需手动操作。

#### cleaning.py — 清洗 API 端点

提供 6 个 API 端点供前端调用：

```python
# 1. 手动触发清洗（converted/cleaned/cleaning_failed 状态可触发）
@router.post("/{note_id}/start", response_model=CleaningStartResponse)
async def start_cleaning(note_id: str, ...)

# 2. 停止清洗（仅 cleaning 状态可停止，状态变为 cleaning_failed）
@router.post("/{note_id}/stop", response_model=CleaningStopResponse)
async def stop_cleaning(note_id: str, ...)

# 3. 查询清洗状态
@router.get("/{note_id}/status", response_model=CleaningStatusResponse)
async def get_cleaning_status(note_id: str, ...)

# 4. 获取清洗 diff（原始版 vs 清洗版的逐行对比）
@router.get("/{note_id}/diff", response_model=CleaningDiffResponse)
async def get_cleaning_diff(note_id: str, ...)

# 5. 恢复重复块
@router.post("/{note_id}/restore/{block_index}", response_model=BlockOperationResponse)
async def restore_duplicate_block(note_id: str, block_index: int, ...)

# 6. 删除重复块
@router.post("/{note_id}/delete/{block_index}", response_model=BlockOperationResponse)
async def delete_duplicate_block(note_id: str, block_index: int, ...)
```

**关键设计**：`start_cleaning` 在触发 Celery 任务前，先将数据库状态更新为 `cleaning`，确保前端刷新时能立即看到状态变化。`cleaning_failed` 状态也可以触发清洗，实现"重新清洗"功能。

**Pydantic Schema**：

```python
class DiffLine(BaseModel):
    type: str          # "added"（新增）、"removed"（删除）、"unchanged"（未变）
    content: str       # 行内容
    line_number: int   # 行号

class DiffBlock(BaseModel):
    block_index: int       # 块索引
    similarity: float      # 相似度
    start_line: int        # 起始行号
    end_line: int          # 结束行号
    preview: str           # 预览文本

class CleaningDiffResponse(BaseModel):
    original_lines: int    # 原始行数
    cleaned_lines: int     # 清洗后行数
    diff_lines: List[DiffLine]     # 逐行对比
    duplicate_blocks: List[DiffBlock]  # 重复块列表
```

---

### 3.10 理解管道 — 后端核心模块

**涉及文件**：
- `backend/app/services/llm_service.py` — LLM API 调用服务
- `backend/app/services/understanding_service.py` — 章节切分与知识点提取
- `backend/app/services/rag_service.py` — RAG 问答服务
- `backend/app/tasks/understand_tasks.py` — Celery 异步理解任务
- `backend/app/api/understanding.py` — 理解管道 API 端点
- `backend/app/models/knowledge_card.py` — 知识卡片数据模型
- `backend/app/models/quiz_item.py` — 题库数据模型

#### 什么是理解管道？

理解管道是 AI 三大角色（清洗→理解→持久化）中的第二个。它的目标是：**从清洗后的笔记中提取结构化知识，生成摘要和题目，并支持智能问答。**

理解管道的完整流程：

```
清洗后 Markdown → 章节切分 → LLM 摘要生成 → 知识点提取 → 题目生成 → 存入知识卡片表和题库表
```

#### llm_service.py — LLM API 调用服务

这是理解管道的核心，封装了所有与 LLM 的交互。

**为什么不用 LangChain？**

LangChain 是一个流行的 LLM 编排框架，但它引入了大量依赖和抽象层。我们遵循 Karpathy 风格——from-scratch, minimal dependencies——直接用 `httpx` 调用 OpenAI 兼容 API。DeepSeek 和 GLM 都兼容 OpenAI 格式，所以一套代码通吃。

**debug 模式自动选择 LLM**：

```python
class LLMService:
    def __init__(self):
        llm_config = settings.get_llm_config()
        self._api_key = llm_config["api_key"]
        self._model = llm_config["model"]
        self._base_url = llm_config["base_url"]
        self._provider = llm_config["provider"]
```

`settings.get_llm_config()` 根据 `debug` 标志自动选择：
- `debug=True` → GLM-4.7-flash（免费，适合开发调试）
- `debug=False` → DeepSeek v4-flash（生产环境，效果更稳定）

**通用聊天接口**：

```python
async def chat(self, messages: list, **kwargs) -> str:
    """通用聊天接口（OpenAI 兼容格式）"""
    async with httpx.AsyncClient(timeout=120.0) as client:
        response = await client.post(
            f"{self._base_url}/chat/completions",
            headers={"Authorization": f"Bearer {self._api_key}"},
            json={
                "model": self._model,
                "messages": messages,
                **kwargs,
            },
        )
    data = response.json()
    return data["choices"][0]["message"]["content"]
```

这就是一个标准的 HTTP POST 请求，发送到 OpenAI 兼容的 API 端点。请求体包含模型名、消息列表和可选参数。

**429 限流处理与速率限制**：

```python
# 速率限制器：令牌桶算法，请求前获取令牌
# 429 时：CAPPED EXPONENTIAL + JITTER（避免惊群效应）
if "429" in str(e):
    delay = min(30 * (attempt + 1), 120)
    delay = delay * (0.5 + random.random() * 0.5)  # 50% 抖动
# 其他错误：CAPPED EXPONENTIAL + JITTER
else:
    delay = min(self._retry_delay * (2 ** attempt), 60)
    delay = delay * (0.5 + random.random() * 0.5)  # 50% 抖动
await asyncio.sleep(delay)
```

GLM 免费版有速率限制，频繁调用会触发 429（Too Many Requests）。项目设计了三级限流：
1. **RateLimiter 令牌桶**：每次 API 调用前先获取令牌，控制每分钟最大请求数（默认 10 RPM）
2. **asyncio.Semaphore(3)**：同一 LLMService 实例最多 3 个并发请求
3. **重试抖动 + 上限**：429 延迟上限 120s，其他错误上限 60s，加入 50% 随机抖动避免惊群效应

#### ConversationSession — 多轮对话会话

**为什么需要多轮对话？**

之前的实现中，每次 API 调用都是独立的——每次构建全新的 `messages` 列表，LLM 看不到之前的结果。这导致：
1. 不同章节可能重复提取相同概念
2. 不同批次的题目可能雷同

**ConversationSession 的设计思路**：

像聊天窗口一样，把所有问题放在同一个对话中依次追问：

```
Session 1（知识卡片提取窗口）:
  [system] 你是知识提取助手...
  [user]   章节1内容
  [assistant] 章节1的摘要和知识点
  [user]   章节2内容          ← LLM 能看到章节1的结果
  [assistant] 章节2的摘要和知识点
  ...

Session 2（题目生成窗口）:
  [system] 你是出题助手...
  [user]   批次1的10个知识点
  [assistant] 批次1的10道题
  [user]   批次2的10个知识点   ← LLM 能看到批次1的题目
  [assistant] 批次2的10道题
  ...
```

**核心代码**：

```python
class ConversationSession:
    """多轮对话会话，在同一窗口内累积消息"""

    def __init__(self, llm_service, system_prompt, temperature=0.3,
                 max_tokens=4096, response_format=None, max_context_pairs=30):
        self._llm = llm_service
        self._messages = [{"role": "system", "content": system_prompt}]
        self._temperature = temperature
        self._max_tokens = max_tokens
        self._response_format = response_format
        self._max_context_pairs = max_context_pairs

    async def ask(self, user_content: str) -> str:
        """在当前对话窗口中追问，返回助手回复"""
        self._messages.append({"role": "user", "content": user_content})
        response = await self._llm.chat(
            self._messages,
            temperature=self._temperature,
            max_tokens=self._max_tokens,
            response_format=self._response_format,
        )
        self._messages.append({"role": "assistant", "content": response})
        self._trim_if_needed()  # 防止上下文无限增长
        return response

    def _trim_if_needed(self):
        """超过限制时裁剪中间对话，保留 system + 最近的对话"""
        max_msg_count = self._max_context_pairs * 2 + 1
        if len(self._messages) > max_msg_count:
            system_msg = self._messages[0]
            recent = self._messages[-(self._max_context_pairs * 2):]
            self._messages = [system_msg] + recent
```

**使用方式**：

```python
# 创建知识提取会话
session = llm_service.create_understanding_session()

# 依次追问每个章节（在同一对话窗口内）
for chapter in chapters:
    response = await session.ask(f"章节标题：{chapter['title']}\n\n章节内容：\n{chapter['content']}")
    parsed = _parse_understanding_response(response)
    # parsed = {"summary": "...", "points": [...]}
```

**关键设计决策**：
- **两个独立窗口**：知识卡片提取用一个窗口，题目生成用另一个窗口。不同任务不应共享上下文
- **上下文裁剪**：当消息超过 `max_context_pairs` 轮时，裁剪中间对话保留最近轮次，防止 token 爆炸
- **摘要+知识点合并**：每个章节只需1次 API 调用（同时返回摘要和知识点），从34次降为17次；后续优化为每3章合并为1次请求，17章节从17次降为6次
- **system prompt 含防重复指令**：如"不要与之前已提取的知识点重复"，利用多轮对话的优势

**批量题目生成**：

```python
async def generate_questions_batch(self, cards: list) -> list:
    """批量题目生成：每批30个卡片合并1次API调用"""
    messages = [
        {
            "role": "system",
            "content": (
                f"你是一个专业的出题助手。请根据给定的 {len(cards)} 个知识点，"
                f"为每个知识点生成1道题。\n\n"
                '请严格按以下 JSON 格式返回：\n'
                '{"questions": [\n'
                '  {"card_index": 1, "question_type": "choice", ...}\n'
                ']}\n'
            ),
        },
    ]
```

批量模式的好处：143个知识卡片只需5次API调用（每批30个），而不是143次。大幅减少 429 限流风险。`batch_size` 可在 `understand_tasks.py` 中调整，当前默认值为 30。

**JSON 解析容错**：

```python
try:
    result = json.loads(response)
except json.JSONDecodeError:
    import re
    json_match = re.search(r'\{[\s\S]*\}', response)
    if json_match:
        result = json.loads(json_match.group())
```

LLM 有时返回的 JSON 前后有多余文字（如"好的，以下是结果：..."），正则提取可以容错处理。

#### understanding_service.py — 章节切分与知识点提取

**章节切分**：

```python
def split_into_chapters(markdown_text: str) -> list:
    """根据 Markdown 标题层级切分章节"""
    chapters = []
    current_chapter = None

    for line in markdown_text.split('\n'):
        if line.startswith('#'):
            # 遇到标题，开始新章节
            if current_chapter:
                chapters.append(current_chapter)
            current_chapter = {
                "title": line.lstrip('#').strip(),
                "level": len(line) - len(line.lstrip('#')),
                "content": "",
            }
        elif current_chapter:
            current_chapter["content"] += line + '\n'

    if current_chapter:
        chapters.append(current_chapter)
    return chapters
```

**核心思想**：逐行扫描 Markdown，遇到 `#` 开头的行就认为是一个新章节的开始。`#` 的数量表示标题层级（`#` = 一级标题，`##` = 二级标题）。

#### rag_service.py — RAG 问答服务

**什么是 RAG？**

RAG（Retrieval-Augmented Generation，检索增强生成）是一种让 LLM 基于特定知识回答问题的技术。流程：
1. 用户提问
2. 从知识库中检索相关内容
3. 把问题和检索到的内容一起送给 LLM
4. LLM 基于这些内容生成回答

这样 LLM 的回答就有据可依，不会"胡编乱造"。

**当前实现的检索方式**：

由于嵌入模型在 FastAPI 进程中加载会导致段错误（0xC0000005），向量检索暂时禁用。当前使用知识卡片中文 n-gram 匹配：

```python
# 中文关键词提取：4字、3字、2字
question_lower = question.lower()
question_keywords = set()
for length in range(4, 1, -1):
    for i in range(len(question_lower) - length + 1):
        question_keywords.add(question_lower[i:i+length])
question_keywords = {kw for kw in question_keywords if len(kw) >= 2}

# 匹配知识卡片
for card in all_cards:
    card_text = f"{card.title} {card.content}".lower()
    match_count = sum(1 for kw in question_keywords if kw in card_text)
    if match_count > 0:
        scored_cards.append((card, match_count))
```

**n-gram 匹配原理**：从用户问题中提取所有 2-4 字的子串作为关键词，然后统计每个知识卡片包含多少个关键词，按匹配数排序取 Top-K。

#### understand_tasks.py — Celery 异步理解任务

```python
@celery_app.task(bind=True, max_retries=2, default_retry_delay=60, rate_limit="3/m")
def understand_document_task(self, note_id: str):
    """异步理解任务"""
    asyncio.run(_understand_document(note_id))

async def _understand_document(note_id: str):
    # 1. 读取清洗后的 Markdown
    # 2. 切分章节
    chapters = split_into_chapters(markdown_text)

    # 3. 每3章合并为1次API请求，批量生成摘要和提取知识点
    batch_size = 3
    for batch_start in range(0, len(chapters), batch_size):
        batch = chapters[batch_start:batch_start + batch_size]
        response = await session.ask(batch_content)
        # 解析多章节JSON响应...
        # 4. 存入知识卡片表...

    # 5. 更新笔记状态为 archived
    # 6. 自动触发题目生成（batch_size=30）
```

**批量题目生成逻辑**：

```python
# 提前提取 ORM 属性值到字典，避免 commit 后延迟加载问题
cards_data = [
    {"id": card.id, "user_id": card.user_id, "title": card.title, "content": card.content, "card_type": card.card_type.value}
    for card in cards
]

# 分批生成（每批30卡片）
batch_size = 30
for batch_start in range(0, len(cards_data), batch_size):
    batch = cards_data[batch_start:batch_start + batch_size]
    questions = await llm_service.generate_questions_batch(cards_input)
    # 存入数据库（使用新会话）
    async with session_factory() as session:
        for q in questions:
            quiz_item = QuizItem(
                options=json.dumps(q.get("options"), ensure_ascii=False) if q.get("options") else None,
                ...
            )
            session.add(quiz_item)
        await session.commit()
```

**关键设计**：
- **提前提取属性值**：SQLAlchemy commit 后 ORM 对象属性过期，访问会触发 MissingGreenlet 错误。所以在查询后立即把需要的值提取到字典中
- **每批独立会话**：每批题目使用新的数据库会话写入，避免长事务问题
- **options JSON 序列化**：SQLite 的 Text 字段不能直接存 Python 列表，必须用 `json.dumps()` 转为字符串

#### knowledge_card.py — 知识卡片数据模型

```python
class CardType(str, enum.Enum):
    concept = "concept"        # 概念
    formula = "formula"        # 公式
    qa = "qa"                 # 问答对
    definition = "definition"  # 定义

class KnowledgeCard(BaseModel):
    __tablename__ = "knowledge_cards"
    user_id: Mapped[str] = mapped_column(String, ForeignKey("users.id"), index=True)
    note_id: Mapped[str] = mapped_column(String, ForeignKey("notes.id"), index=True)
    card_type: Mapped[CardType] = mapped_column(Enum(CardType))
    title: Mapped[str] = mapped_column(String(500))
    content: Mapped[str] = mapped_column(Text)
    summary: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    chapter_title: Mapped[Optional[str]] = mapped_column(String(500), nullable=True)
    source_text: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
```

知识卡片是理解管道的核心产出。每张卡片关联一个用户和一篇笔记，记录知识点的类型、标题、内容、所属章节和原始出处。

#### quiz_item.py — 题库数据模型

```python
class QuestionType(str, enum.Enum):
    choice = "choice"              # 选择题
    fill_blank = "fill_blank"      # 填空题
    short_answer = "short_answer"  # 简答题

class DifficultyLevel(str, enum.Enum):
    easy = "easy"
    medium = "medium"
    hard = "hard"

class QuizItem(BaseModel):
    __tablename__ = "quiz_items"
    user_id: Mapped[str] = mapped_column(String, ForeignKey("users.id"), index=True)
    card_id: Mapped[str] = mapped_column(String, ForeignKey("knowledge_cards.id"), index=True)
    note_id: Mapped[str] = mapped_column(String, ForeignKey("notes.id"), index=True)
    question_type: Mapped[QuestionType] = mapped_column(Enum(QuestionType))
    difficulty: Mapped[DifficultyLevel] = mapped_column(Enum(DifficultyLevel), default=DifficultyLevel.medium)
    question: Mapped[str] = mapped_column(Text)
    answer: Mapped[str] = mapped_column(Text)
    options: Mapped[Optional[str]] = mapped_column(Text, nullable=True)  # JSON字符串
    explanation: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
```

**为什么 options 是 Text 而不是 JSON？** SQLite 不支持 JSON 数组类型，所以选择题的选项用 JSON 字符串存储（如 `'["A.选项1", "B.选项2"]'`），读取时用 `json.loads()` 解析。

---

### 3.11 复习调度引擎 — 后端核心模块

**涉及文件**：
- `backend/app/services/sm2_service.py` — SM-2 间隔重复算法
- `backend/app/services/review_service.py` — 复习调度业务逻辑
- `backend/app/models/review_log.py` — 复习记录数据模型
- `backend/app/schemas/review.py` — 复习调度请求/响应模型
- `backend/app/api/review.py` — 复习调度 API 端点

#### 什么是复习调度引擎？

复习调度引擎是 AI 三大角色（清洗→理解→持久化）中"持久化"的核心实现。它的目标是：**基于遗忘曲线（SM-2 算法），自动安排每道题目的复习时间，确保用户在最佳时间点复习，形成长期记忆。**

复习调度的完整流程：

```
题目生成（next_review_at=NULL）→ 用户访问复习页面 → 按需查询到期题目 → 答题 → SM-2 计算质量评分 → 更新间隔/EF/下次复习时间 → 记录 ReviewLog
```

#### SM-2 算法是什么？

SM-2（SuperMemo 2）是最经典的间隔重复算法，由 Piotr Woźniak 于 1987 年提出。核心思想：

1. **遗忘曲线**：人的记忆会随时间衰退，但在即将遗忘时复习，记忆效果最好
2. **间隔递增**：每次正确回答后，下次复习间隔变长（1天→6天→EF×6天→...）
3. **舒适度因子（EF）**：反映记忆难度，EF 越高说明越容易记住（默认 2.5，最低 1.3）
4. **质量评分（0-5）**：用户对答案的掌握程度，≥3 为"通过"，<3 为"失败"

**SM-2 算法核心逻辑**：

```python
def calculate_sm2(quality, interval=1, repetition=0, easiness_factor=2.5) -> SM2Result:
    """
    quality: 0-5 的质量评分
    interval: 当前复习间隔（天）
    repetition: 连续正确次数
    easiness_factor: 舒适度因子
    """

    # 1. 更新舒适度因子
    # EF' = EF + (0.1 - (5-q) * (0.08 + (5-q) * 0.02))
    ef = easiness_factor + (0.1 - (5 - quality) * (0.08 + (5 - quality) * 0.02))
    ef = max(ef, 1.3)  # EF 最低 1.3

    # 2. 根据质量评分更新间隔和重复次数
    if quality >= 3:  # 通过
        repetition += 1
        if repetition == 1:
            interval = 1      # 第一次正确：1天后复习
        elif repetition == 2:
            interval = 6      # 第二次正确：6天后复习
        else:
            interval = round(interval * ef)  # 之后：间隔 = 上次间隔 × EF
    else:  # 失败
        repetition = 0        # 重置连续正确次数
        interval = 1          # 间隔重置为1天

    # 3. 计算下次复习时间
    next_review_at = datetime.now(timezone.utc) + timedelta(days=interval)

    return SM2Result(interval, repetition, ef, next_review_at)
```

**关键数学公式**：
- `EF' = EF + (0.1 - (5-q) × (0.08 + (5-q) × 0.02))`
  - quality=5（完美）→ EF 增加 0.1（变更容易）
  - quality=3（勉强通过）→ EF 不变
  - quality<3（失败）→ EF 减小（变更难），最低 1.3

#### quality_from_answer — 答题质量评分映射

SM-2 算法需要 0-5 的质量评分，但不同题型的"正确性"判断方式不同：

```python
def quality_from_answer(question_type, user_answer, correct_answer) -> int:
    """
    根据题型和答案，映射到 SM-2 质量评分

    选择题(choice)：多格式匹配 → 5（正确）或 1（错误）
      - 精确匹配：user == correct
      - 字母前缀匹配：answer="A", user="A. 选项文本" → 比较首字母
      - 去前缀纯文本比较：去掉 "A. " 后比较剩余文本
    填空题(fill_blank)：精确匹配 → 5，部分匹配 → 3，不匹配 → 1
    简答题(short_answer)：n-gram 关键词覆盖度 → 1-5 线性映射
    """
```

**简答题评分 `_score_short_answer`**：

简答题没有标准答案，所以使用 n-gram 关键词匹配：

```python
def _score_short_answer(user_answer, correct_answer) -> int:
    # 1. 从正确答案中提取关键词（2-4字中文片段 + 英文单词）
    correct_keywords = set()
    for length in range(4, 1, -1):  # 4字、3字、2字
        for i in range(len(correct_lower) - length + 1):
            correct_keywords.add(correct_lower[i:i+length])

    # 2. 计算用户答案覆盖了多少关键词
    matched = sum(1 for kw in correct_keywords if kw in user_lower)
    coverage = matched / len(correct_keywords) if correct_keywords else 0

    # 3. 线性映射到 1-5
    return max(1, min(5, round(coverage * 5)))
```

**为什么用 n-gram 而不是语义相似度？** 因为语义相似度需要嵌入模型，而嵌入模型在 FastAPI 进程中会导致段错误（见坑 19）。n-gram 匹配是轻量级的替代方案，不需要任何模型。

#### review_service.py — 复习调度业务逻辑

**按需查询到期题目**：

```python
async def get_due_quizzes(user_id, db, limit=50):
    """查询到期待复习的题目"""
    now = datetime.now(timezone.utc)
    query = (
        select(QuizItem)
        .where(QuizItem.user_id == user_id)
        .where(
            or_(
                QuizItem.next_review_at <= now,  # 已到期
                QuizItem.next_review_at.is_(None),  # 尚未复习过
            )
        )
        .order_by(
            nullsfirst(QuizItem.next_review_at.asc())  # 空值优先，已到期按时间排序
        )
        .limit(limit)
    )
```

**为什么用按需查询而不是 Celery Beat？**

原计划用 Celery Beat 定时生成每日复习任务，但按需查询更简单：
- 不需要维护定时任务
- 不需要任务去重逻辑
- 用户随时访问都能获取最新到期题目
- 查询条件 `next_review_at <= now OR next_review_at IS NULL` 直接覆盖所有需要复习的题目

**提交答题完整流程**：

```python
async def submit_answer(quiz_id, user_id, user_answer, time_spent_ms, db):
    """提交答题：获取题目→计算质量→运行SM-2→更新QuizItem→创建ReviewLog"""
    # 1. 获取题目
    quiz = await db.get(QuizItem, quiz_id)

    # 2. 计算质量评分
    quality = quality_from_answer(quiz.question_type, user_answer, quiz.answer)

    # 3. 运行 SM-2 算法
    result = calculate_sm2(
        quality=quality,
        interval=quiz.interval,
        repetition=quiz.repetition,
        easiness_factor=quiz.easiness_factor,
    )

    # 4. 更新 QuizItem 的 SM-2 调度字段
    quiz.interval = result.interval
    quiz.repetition = result.repetition
    quiz.easiness_factor = result.easiness_factor
    quiz.next_review_at = result.next_review_at
    quiz.last_reviewed_at = datetime.now(timezone.utc)
    quiz.review_count += 1

    # 5. 创建 ReviewLog 记录
    review_log = ReviewLog(
        user_id=user_id,
        quiz_id=quiz_id,
        note_id=quiz.note_id,
        user_answer=user_answer,
        is_correct=(quality >= 3),
        quality=quality,
        time_spent_ms=time_spent_ms,
    )
    db.add(review_log)
    await db.commit()
```

**关键设计**：QuizItem 的 SM-2 字段和 ReviewLog 是分离的。QuizItem 存储调度状态（下次什么时候复习），ReviewLog 存储答题历史（每次答了什么、花了多久）。这样查询调度和查询历史互不干扰。

#### review_log.py — 复习记录数据模型

```python
class ReviewLog(BaseModel):
    __tablename__ = "review_logs"

    user_id: Mapped[str] = mapped_column(String, ForeignKey("users.id"), index=True)
    quiz_id: Mapped[str] = mapped_column(String, ForeignKey("quiz_items.id"), index=True)
    note_id: Mapped[str] = mapped_column(String, ForeignKey("notes.id"), index=True)
    user_answer: Mapped[str] = mapped_column(Text)
    is_correct: Mapped[bool] = mapped_column(Boolean)
    quality: Mapped[int] = mapped_column(Integer)          # SM-2 质量评分 0-5
    time_spent_ms: Mapped[int] = mapped_column(Integer)     # 答题耗时（毫秒）
    review_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
```

**为什么 ReviewLog 要关联 note_id？** 虽然 note_id 可以通过 quiz_id → QuizItem.note_id 间接获取，但直接存储可以避免 JOIN 查询，加速"某篇笔记的复习历史"这类查询。

#### schemas/review.py — 复习调度请求/响应模型

```python
class DueQuizResponse(BaseModel):
    """到期题目响应"""
    id: str
    question_type: str      # choice / fill_blank / short_answer
    question: str
    options: Optional[str]   # 选择题选项（JSON字符串）
    answer: str              # 正确答案（答题后返回）
    explanation: Optional[str]
    difficulty: str
    interval: int            # 当前复习间隔
    repetition: int          # 连续正确次数
    easiness_factor: float   # 舒适度因子

class SubmitAnswerRequest(BaseModel):
    """提交答题请求"""
    quiz_id: str
    user_answer: str
    time_spent_ms: int = 0

class SubmitAnswerResponse(BaseModel):
    """提交答题响应"""
    is_correct: bool
    quality: int             # SM-2 质量评分
    correct_answer: str
    explanation: Optional[str]
    sm2_info: SM2Info        # SM-2 调度信息

class SM2Info(BaseModel):
    """SM-2 调度信息"""
    interval: int
    repetition: int
    easiness_factor: float
    next_review_at: Optional[datetime]

class ReviewStatsResponse(BaseModel):
    """复习统计响应"""
    due_count: int           # 待复习数
    today_done: int          # 今日完成数
    today_correct: int       # 今日正确数
    today_accuracy: float    # 今日正确率
    total_reviews: int       # 总复习次数
    total_correct: int       # 总正确次数
    total_accuracy: float    # 总正确率
    total_quizzes: int       # 题目总数
```

#### api/review.py — 复习调度 API 端点

4 个 API 端点，全部需要认证：

```python
# 1. 获取到期题目
@router.get("/due", response_model=DueQuizListResponse)
async def get_due_quizzes_api(current_user, db)

# 2. 提交答题
@router.post("/submit", response_model=SubmitAnswerResponse)
async def submit_answer_api(req: SubmitAnswerRequest, current_user, db)

# 3. 获取复习统计
@router.get("/stats", response_model=ReviewStatsResponse)
async def get_review_stats_api(current_user, db)

# 4. 获取复习历史
@router.get("/history", response_model=ReviewHistoryResponse)
async def get_review_history_api(page: int = 1, page_size: int = 20, current_user, db)
```

路由注册在 `api/router.py` 中：

```python
from .review import router as review_router
api_router.include_router(review_router, prefix="/review", tags=["复习"])
```

### 3.12 学习报告服务 — 后端核心模块（第9周）

**涉及文件**：
- `backend/app/services/report_service.py` — 学习报告业务逻辑
- `backend/app/schemas/report.py` — 学习报告请求/响应模型
- `backend/app/api/report.py` — 学习报告 API 端点

#### 什么是学习报告服务？

学习报告服务是复习调度引擎的"数据可视化层"。它从 `review_logs` 表中聚合统计数据，让用户直观了解自己的学习情况：今天掌握了多少新知识、哪些是薄弱点、最近一周的学习趋势如何。

#### report_service.py — 三个核心函数

**1. get_daily_report — 今日学习报告**

```python
async def get_daily_report(user_id, db):
    """聚合今日学习数据"""
    # 今日新掌握知识点数：今日首次答对（quality >= 3）且 review_count == 1 的 QuizItem 数量
    # 为什么用 review_count == 1？因为 submit_answer 会先 +1 再查询，
    # 所以首次答对时 review_count 刚好是 1
    new_mastered = select(func.count(distinct(ReviewLog.quiz_id)))
        .join(QuizItem)
        .where(ReviewLog.is_correct == True, QuizItem.review_count == 1)

    # 今日复习总时长：sum(time_spent_ms)
    # 今日正确率：正确数 / 总数
    # 各题型正确率：按 question_type 分组统计
```

**2. get_weekly_trend — 7天趋势**

```python
async def get_weekly_trend(user_id, db):
    """最近7天每日复习数和正确率"""
    # 逐天查询 review_logs，统计每天的复习数、正确数、正确率
    # 为什么用逐天查询而不是 GROUP BY 日期？
    # 因为 SQLite 的日期函数兼容性不好，逐天查询更可靠
    for i in range(6, -1, -1):
        day = now - timedelta(days=i)
        day_start = day.replace(hour=0, minute=0, second=0)
        day_end = day_start + timedelta(days=1)
        # 查询 day_start <= review_at < day_end 的记录
```

**3. get_weak_points — 薄弱点分析**

```python
async def get_weak_points(user_id, db, limit=5):
    """错误次数最多的知识卡片"""
    # 子查询：统计每张卡片的错误次数
    error_subq = (
        select(QuizItem.card_id, func.sum(case((ReviewLog.is_correct == False, 1), else_=0)))
        .join(ReviewLog)
        .group_by(QuizItem.card_id)
        .subquery()
    )
    # 关联 KnowledgeCard 获取标题，按错误次数降序排列
    result = select(KnowledgeCard, error_subq.c.error_count)
        .join(error_subq)
        .order_by(error_subq.c.error_count.desc())
        .limit(limit)
```

**为什么薄弱点用子查询而不是直接 JOIN？** 因为需要先按 card_id 分组统计错误次数，再关联 KnowledgeCard 获取标题。如果直接 JOIN 会导致分组粒度不对。

#### schemas/report.py — 响应模型

```python
class DailyReportResponse(BaseModel):
    date: str                          # 报告日期
    new_mastered: int = 0             # 今日新掌握知识点数
    total_review_time_ms: int = 0     # 今日复习总时长
    total_reviews: int = 0            # 今日复习总数
    today_accuracy: float = 0.0       # 今日正确率
    weak_point_count: int = 0         # 薄弱点数量
    question_type_accuracy: List[QuestionTypeAccuracy] = []  # 各题型正确率

class WeeklyTrendItem(BaseModel):
    date: str              # 日期
    review_count: int = 0  # 复习次数
    correct_count: int = 0 # 正确次数
    accuracy: float = 0.0  # 正确率

class WeakPointItem(BaseModel):
    card_id: str           # 卡片ID
    card_title: str        # 卡片标题
    card_type: str         # 卡片类型
    error_count: int = 0   # 错误次数
    accuracy: float = 0.0  # 正确率
```

#### api/report.py — 3个API端点

```python
@router.get("/daily")           # 今日学习报告
@router.get("/weekly-trend")    # 7天趋势
@router.get("/weak-points")     # 薄弱点列表（支持 limit 参数，默认5）
```

路由注册：`api_router.include_router(report_router, prefix="/report", tags=["学习报告"])`

#### 薄弱点优先排序（review_service.py 修改）

第9周还修改了 `review_service.py` 的 `get_due_quizzes`，增加了薄弱点优先排序：

```python
async def get_due_quizzes(user_id, db, limit=50):
    # 子查询：统计每张卡片的错误次数
    error_subq = (
        select(QuizItem.card_id, func.sum(case((ReviewLog.is_correct == False, 1), else_=0)))
        .join(ReviewLog)
        .group_by(QuizItem.card_id)
        .subquery()
    )

    # 到期题目关联薄弱点排序
    result = select(QuizItem)
        .outerjoin(error_subq, error_subq.c.card_id == QuizItem.card_id)
        .order_by(
            QuizItem.next_review_at.asc().nullsfirst(),  # 最过期的优先
            func.coalesce(error_subq.c.error_count, 0).desc(),  # 错误越多越优先
        )
```

**排序策略**：先按到期时间排序（最过期的优先），同一到期时间的题目，错误次数多的优先推送。这样用户会先复习最需要巩固的知识。

### 3.13 全局异常中间件 — 后端核心模块（第10周）

**涉及文件**：
- `backend/app/middleware/__init__.py` — 中间件包
- `backend/app/middleware/error_handler.py` — 全局异常处理中间件
- `backend/app/main.py` — 中间件注册

#### 什么是全局异常中间件？

在 Web 应用中，如果后端代码抛出异常（比如数据库连接失败、参数错误），默认行为是返回一个丑陋的 500 错误页面。全局异常中间件的作用是：**拦截所有未处理的异常，返回统一格式的 JSON 响应**，让前端可以统一处理错误。

#### ErrorHandlerMiddleware 实现

```python
class ErrorHandlerMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request, call_next):
        try:
            response = await call_next(request)  # 正常处理请求
            return response
        except Exception as exc:
            # 区分三种异常类型：
            if isinstance(exc, HTTPException):
                # 已知 HTTP 异常：保留原始状态码和详情
                return JSONResponse(status_code=exc.status_code, content={
                    "detail": exc.detail,
                    "error_code": f"HTTP_{exc.status_code}",
                })
            elif isinstance(exc, ValidationError):
                # Pydantic 验证错误：422
                return JSONResponse(status_code=422, content={
                    "detail": str(exc),
                    "error_code": "VALIDATION_ERROR",
                })
            else:
                # 未知异常：500，记录日志但不暴露堆栈
                logger.error(f"未处理异常: {request.method} {request.url.path} — {exc}")
                return JSONResponse(status_code=500, content={
                    "detail": "服务器内部错误，请稍后重试",
                    "error_code": "INTERNAL_ERROR",
                })
```

**关键设计**：
- 未知异常不暴露堆栈信息（安全考虑），只返回"服务器内部错误"
- 所有错误返回统一格式 `{detail, error_code}`，前端可以统一解析
- 中间件放在 CORS 之后、路由之前，确保所有请求都经过

#### 第10周其他优化

**流式上传**（upload.py）：
```python
# 旧：content = await file.read()  # 一次性读入内存
# 新：分块读写，避免大文件占满内存
with tempfile.NamedTemporaryFile(delete=False) as tmp:
    while True:
        chunk = await file.read(1024 * 1024)  # 1MB chunks
        if not chunk: break
        total_size += len(chunk)
        if total_size > max_size: raise HTTPException(400, "文件太大")
        tmp.write(chunk)
```

**os.replace 替代 shutil.copy2**（storage_service.py）：
```python
# 旧：shutil.copy2(file_path, path)  # 复制，双份磁盘占用
# 新：os.replace(file_path, path)    # 原子移动，无额外占用
```

**Celery 结果自动清理**（celery_app.py）：
```python
result_expires=3600  # 1小时后自动清理结果文件，避免磁盘堆积
```

**数据库索引优化**：
- `quiz_items.next_review_at` 添加索引 → 到期查询加速
- `review_logs.review_at` 添加索引 → 报告聚合查询加速

### 3.14 前端统一状态管理 — AuthContext（第11周）

**文件位置**：`frontend/src/contexts/AuthContext.tsx`

#### 什么是 React Context？

之前，App.tsx 用 `useState` 管理认证状态，通过 `onLogin`/`onLogout` props 一层层传递给子组件。这种方式在组件层级深的时候很麻烦（叫"props drilling"）。

React Context 提供了一种"全局变量"机制：在顶层 Provider 中设置值，任何子组件都可以通过 `useContext` 直接读取，不需要 props 传递。

#### AuthContext 的实现

```typescript
// 1. 创建 Context 对象
const AuthContext = createContext<AuthContextType | null>(null)

// 2. 创建 Provider 组件
export function AuthProvider({ children }) {
    const [isAuthenticated, setIsAuthenticated] = useState(!!getToken())

    // 监听 Token 过期事件
    useEffect(() => {
        const handleTokenExpired = () => setIsAuthenticated(false)
        window.addEventListener(TOKEN_EXPIRED_EVENT, handleTokenExpired)
        return () => window.removeEventListener(TOKEN_EXPIRED_EVENT, handleTokenExpired)
    }, [])

    // login 方法：调API + 存Token + 更新状态
    const login = useCallback(async (email, password) => {
        const res = await apiLogin(email, password)
        setToken(res.access_token)
        setIsAuthenticated(true)  // 更新 Context 中的状态
    }, [])

    // logout 方法：清除Token + 更新状态
    const logout = useCallback(() => {
        removeToken()
        setIsAuthenticated(false)
    }, [])

    return (
        <AuthContext.Provider value={{ isAuthenticated, login, register, logout }}>
            {children}
        </AuthContext.Provider>
    )
}

// 3. 创建 useAuth hook，方便子组件使用
export function useAuth() {
    const ctx = useContext(AuthContext)
    if (!ctx) throw new Error('useAuth must be used within AuthProvider')
    return ctx
}
```

**使用方式**：
- App.tsx：`<AuthProvider><AppRoutes /></AuthProvider>` 包裹整个应用
- Login.tsx：`const { login } = useAuth()` 获取 login 方法
- Navbar.tsx：`const { logout } = useAuth()` 获取 logout 方法
- 任何需要认证状态的组件：`const { isAuthenticated } = useAuth()`

### 3.15 统一状态组件与标签映射（第11周）

**文件位置**：
- `frontend/src/components/LoadingSpinner.tsx`
- `frontend/src/components/EmptyState.tsx`
- `frontend/src/components/ErrorDisplay.tsx`
- `frontend/src/utils/labels.ts`

#### 为什么要统一？

之前每个页面都自己写 loading/empty/error 的展示逻辑，代码重复且风格不一致。第11周创建了三个统一组件：

- **LoadingSpinner**：旋转动画 + 可选文字，替代各页面的 `<p>加载中...</p>`
- **EmptyState**：图标 + 提示文字 + 可选操作按钮，替代各页面的空数据 div
- **ErrorDisplay**：错误信息 + 重试按钮，替代各页面的错误提示

#### 统一标签映射

之前 `statusLabels`、`cardTypeLabels`、`questionTypeLabels` 等映射在每个页面组件内部各定义一份。第11周将它们集中到 `utils/labels.ts`，各页面从此处导入：

```typescript
// utils/labels.ts
export const statusLabels: Record<string, string> = {
    converting: '转换中', converted: '已完成', cleaning: '清洗中', ...
}
export const cardTypeLabels: Record<string, string> = {
    concept: '概念', formula: '公式', qa: '问答', definition: '定义',
}
// ... 其他映射

// 各页面使用
import { statusLabels, cardTypeLabels } from '../utils/labels'
```

---

## 第四章：前端代码逐文件讲解

前端使用 **React 18 + TypeScript + Vite** 构建。如果你不熟悉 React，这里快速科普：

- **React** 是一个 UI 库，用组件的方式构建页面
- **TypeScript** 是 JavaScript 的超集，增加了类型检查
- **Vite** 是构建工具，提供开发服务器和打包功能
- **JSX/TSX** 是 React 的语法扩展，让你在 JavaScript 中写 HTML

### 4.1 client.ts — API 请求封装

**文件位置**：`frontend/src/api/client.ts`

**Token 管理**：

```typescript
const TOKEN_KEY = 'engramnote_token';

export const TOKEN_EXPIRED_EVENT = 'token-expired';  // Token 过期事件名称

export function getToken(): string | null {
    return localStorage.getItem(TOKEN_KEY);  // 从浏览器存储读取
}

export function setToken(token: string): void {
    localStorage.setItem(TOKEN_KEY, token);  // 保存到浏览器存储
}

export function removeToken(): void {
    localStorage.removeItem(TOKEN_KEY);  // 删除
}

export function notifyTokenExpired(): void {
    removeToken();  // 清除过期 Token
    window.dispatchEvent(new CustomEvent(TOKEN_EXPIRED_EVENT));  // 通知 App 组件
}
```

`localStorage` 是浏览器的本地存储，关闭浏览器后数据还在。Token 存在这里，刷新页面后用户仍然是登录状态。

**Token 过期处理**：当后端返回 401 状态码时，`notifyTokenExpired()` 会清除本地 Token 并派发全局 `token-expired` 事件。`App.tsx` 监听此事件后自动将认证状态设为 `false`，用户被重定向到登录页。

**通用请求函数**：

```typescript
async function request<T>(path: string, options: RequestInit = {}): Promise<T> {
    const token = getToken();
    const headers: Record<string, string> = {
        'Content-Type': 'application/json',
        ...(options.headers as Record<string, string>),
    };
    if (token) {
        headers['Authorization'] = `Bearer ${token}`;  // 自动附带 Token
    }

    const response = await fetch(`/api${path}`, { ...options, headers });

    if (!response.ok) {
        // Token 过期或无效时，通知应用跳转到登录页
        if (response.status === 401) {
            notifyTokenExpired();
            throw new Error('登录已过期，请重新登录');
        }
        const error = await response.json().catch(() => ({ detail: response.statusText }));
        throw new Error(error.detail || `请求失败: ${response.status}`);
    }
    return response.json();
}
```

这个函数封装了所有 API 请求的公共逻辑：
1. 自动添加 `Content-Type: application/json`
2. 自动附带 JWT Token
3. 统一处理错误响应

**类型参数 `<T>`**：这是 TypeScript 的泛型，让函数返回值的类型由调用方指定。比如 `request<User>('/auth/me')` 返回 `User` 类型。

**API 函数**：

```typescript
export async function login(email: string, password: string): Promise<TokenResponse> {
    return request<TokenResponse>('/auth/login', {
        method: 'POST',
        body: JSON.stringify({ email, password }),
    });
}

export async function getNotes(page = 1, pageSize = 20, keyword?: string): Promise<NoteListResponse> {
    const params = new URLSearchParams({ page: String(page), page_size: String(pageSize) });
    if (keyword) params.set('keyword', keyword);
    return request<NoteListResponse>(`/notes?${params}`);
}
```

每个 API 函数就是对 `request` 的简单包装，指定了路径、方法和参数。

**文件上传的特殊处理**：

```typescript
export async function uploadFile(file: File, backend?: string): Promise<Note> {
    const token = getToken();
    const formData = new FormData();
    formData.append('file', file);
    if (backend) formData.append('backend', backend);  // 可选：指定解析后端

    const response = await fetch('/api/upload', {
        method: 'POST',
        headers: token ? { Authorization: `Bearer ${token}` } : {},
        body: formData,  // 不设 Content-Type，让浏览器自动设置
    });
    // 401 时同样触发 Token 过期处理
    if (response.status === 401) {
        notifyTokenExpired();
        throw new Error('登录已过期，请重新登录');
    }
    ...
}
```

文件上传不能用 `request` 函数，因为：
1. 需要用 `FormData` 而不是 JSON
2. 不能手动设置 `Content-Type`，浏览器会自动设置 `multipart/form-data` 并加上边界字符串
3. `backend` 参数可选，不传则使用后端默认配置

### 4.2 App.tsx — 路由与认证

**文件位置**：`frontend/src/App.tsx`

第11周重构后，App.tsx 拆分为 App（Provider 包裹）和 AppRoutes（路由逻辑）两部分：

```typescript
// AuthProvider 在最外层提供全局认证状态
function App() {
    return (
        <AuthProvider>
            <AppRoutes />
        </AuthProvider>
    )
}

// AppRoutes 根据认证状态渲染不同路由
function AppRoutes() {
    const { isAuthenticated } = useAuth()  // 从 Context 获取认证状态

    if (!isAuthenticated) {
        // 未登录：只显示登录/注册页面
        return (
            <Routes>
                <Route path="/register" element={<Register />} />
                <Route path="*" element={<Login />} />
            </Routes>
        )
    }

    // 已登录：显示导航栏 + 主内容
    return (
        <>
            <Navbar />
            <main className="container" style={{ paddingTop: '80px' }}>
                <Routes>
                    <Route path="/" element={<Dashboard />} />
                    <Route path="/notes" element={<NotesList />} />
                    <Route path="/today" element={<TodayLearn />} />
                    {/* ... 其他路由 */}
                </Routes>
            </main>
        </>
    )
}
```

**核心逻辑**：
1. `AuthProvider` 包裹整个应用，提供全局认证状态（替代之前的 `useState`）
2. `useAuth()` hook 在任何组件中获取 `isAuthenticated`、`login`、`logout`、`register` 方法
3. 未登录时，所有路径都重定向到登录页
4. 登录成功后，`login()` 方法自动更新 Context 中的 `isAuthenticated = true`，触发所有使用 `useAuth()` 的组件重新渲染
5. 退出登录时，`logout()` 方法清除 Token 并设置 `isAuthenticated = false`
6. **Token 过期自动跳转**：AuthContext 内部监听 `TOKEN_EXPIRED_EVENT` 事件，自动将 `isAuthenticated` 设为 `false`

**为什么用 Context 替代 useState？**：之前 `isAuthenticated` 只在 App.tsx 中通过 `useState` 管理，需要通过 `onLogin`/`onLogout` props 一层层传递给子组件。Context 让任何组件都能直接通过 `useAuth()` 获取认证状态，不需要 props 传递链。

### 4.3 页面组件

#### Login.tsx — 登录页

```typescript
export default function Login() {
    const { login } = useAuth()  // 从 AuthContext 获取 login 方法
    const [email, setEmail] = useState('')      // 邮箱输入值
    const [password, setPassword] = useState('')  // 密码输入值
    const [error, setError] = useState('')       // 错误提示
    const [loading, setLoading] = useState(false) // 加载状态

    async function handleSubmit(e: React.FormEvent) {
        e.preventDefault()  // 阻止表单默认提交行为（页面刷新）
        setError('')
        setLoading(true)

        try {
            await login(email, password)  // AuthContext 的 login 方法：调API + 存Token + 更新状态
        } catch (err) {
            setError(err instanceof Error ? err.message : '登录失败')
        } finally {
            setLoading(false)
        }
    }
    ...
}
```

**受控组件**：`value={email}` 和 `onChange={(e) => setEmail(e.target.value)}` 让 React 完全控制输入框的值。每次输入都会更新状态，状态变化又更新输入框的显示。

**e.preventDefault()**：HTML 表单默认会刷新页面提交。在 React 中，我们用 JavaScript 处理提交，所以要阻止默认行为。

#### Upload.tsx — 上传页

这是前端最复杂的页面，包含拖拽上传和轮询逻辑。

**拖拽上传**：

```typescript
<div
    onDrop={handleDrop}         // 文件放下时
    onDragOver={handleDragOver} // 文件悬停时
    onDragLeave={handleDragLeave}  // 文件离开时
    onClick={() => fileInputRef.current?.click()}  // 点击时触发文件选择
>
    <input ref={fileInputRef} type="file" style={{ display: 'none' }} />
</div>
```

拖拽 API：
- `onDragOver`：文件拖到区域上方时触发，必须调用 `e.preventDefault()` 才能让浏览器允许放下
- `onDrop`：文件放下时触发，通过 `e.dataTransfer.files` 获取文件
- 隐藏的 `<input type="file">`：点击区域时通过 `ref` 触发文件选择对话框

**解析方式选择**：

上传页面提供三个解析方式按钮，用户可以在上传前选择：

```typescript
const BACKEND_OPTIONS = [
  { value: '', label: '自动', description: '使用后端默认配置' },
  { value: 'pipeline', label: '本地解析', description: '使用本地模型解析' },
  { value: 'vlm-http-client', label: '云端解析', description: '使用云端API解析' },
]

const [parseBackend, setParseBackend] = useState('')  // 默认"自动"
```

选择后传递给 `uploadFile(file, parseBackend || undefined)`，后端根据参数决定使用本地还是云端解析。

**轮询转换状态**：

```typescript
async function pollStatus(id: string) {
    const interval = setInterval(async () => {
        const res = await getUploadStatus(id)
        if (res.status === 'converted' || res.status === 'cleaned' || res.status === 'archived') {
            clearInterval(interval)  // 转换完成，停止轮询
            setTimeout(() => navigate(`/notes/${id}`), 1000)  // 1秒后跳转
        } else if (res.status === 'failed') {
            clearInterval(interval)  // 转换失败，停止轮询
            setError(res.error_message || '转换失败')
        }
    }, 5000)  // 每5秒查询一次
}
```

`setInterval` 每隔 5 秒调用一次 API 查询状态。转换完成或失败时，用 `clearInterval` 停止轮询。

#### NoteDetail.tsx — 笔记详情

**Markdown 渲染**：

```typescript
import { marked } from 'marked'
import { markedHighlight } from 'marked-highlight'
import hljs from 'highlight.js'

// marked v14+ 使用 marked-highlight 扩展替代已移除的 highlight 选项
marked.use(markedHighlight({
    langPrefix: 'hljs language-',
    highlight(code: string, lang: string) {
        if (lang && hljs.getLanguage(lang)) {
            return hljs.highlight(code, { language: lang }).value
        }
        return hljs.highlightAuto(code).value
    },
}))

// 使用
const htmlContent = marked.parse(mdContent)
...
<article dangerouslySetInnerHTML={{ __html: htmlContent }} />
```

- `marked` 库把 Markdown 文本转换为 HTML
- `marked-highlight` 扩展为代码块提供语法高亮（marked v14 移除了内置的 `highlight` 选项）
- `highlight.js` 对代码块进行语法高亮
- `dangerouslySetInnerHTML` 是 React 中插入原始 HTML 的方式（名字吓人，但在这里是安全的，因为内容是我们自己生成的）

### 4.4 清洗相关组件

#### CleaningPanel.tsx — 清洗操作面板

**文件位置**：`frontend/src/components/CleaningPanel.tsx`

**作用**：提供清洗操作界面，包括触发清洗、查看进度、管理重复块。

```typescript
export default function CleaningPanel({ noteId, status, onStatusChange }: CleaningPanelProps) {
    const [cleaning, setCleaning] = useState(false)
    const [duplicateBlocks, setDuplicateBlocks] = useState<DiffBlock[]>([])

    // 触发清洗
    async function startCleaning() {
        setCleaning(true)
        await startCleaning(noteId)
        pollCleaningStatus()
    }

    // 轮询清洗状态
    async function pollCleaningStatus() {
        const interval = setInterval(async () => {
            const res = await getCleaningStatus(noteId)
            if (res.status === 'cleaned') {
                clearInterval(interval)
                onStatusChange('cleaned')
            }
        }, 5000)
    }

    // 恢复重复块
    async function handleRestore(blockIndex: number) {
        await restoreBlock(noteId, blockIndex)
        // 刷新 diff 数据
    }

    // 删除重复块
    async function handleDelete(blockIndex: number) {
        await deleteBlock(noteId, blockIndex)
        // 刷新 diff 数据
    }
}
```

**核心功能**：
1. **触发清洗**：调用 `POST /api/cleaning/{noteId}/start`（converted/cleaned/cleaning_failed 状态可触发）
2. **停止清洗**：调用 `POST /api/cleaning/{noteId}/stop`（仅 cleaning 状态可停止，状态变为 cleaning_failed）
3. **轮询状态**：NoteDetail 页面每 5 秒查询清洗进度，直到状态变为 cleaned 或 cleaning_failed
4. **重复块管理**：展示重复块列表，支持恢复/删除操作
5. **清洗失败处理**：cleaning_failed 状态显示错误信息和"重新清洗"按钮

#### DiffView.tsx — 行级 diff 对比视图

**文件位置**：`frontend/src/components/DiffView.tsx`

**作用**：以颜色编码的方式展示原始版与清洗版的逐行对比。

```typescript
export default function DiffView({ diffLines }: DiffViewProps) {
    return (
        <div className="diff-view">
            {diffLines.map((line, index) => (
                <div key={index} className={`diff-line diff-${line.type}`}>
                    <span className="line-number">{line.lineNumber}</span>
                    <span className="line-prefix">
                        {line.type === 'added' ? '+' : line.type === 'removed' ? '-' : ' '}
                    </span>
                    <span className="line-content">{line.content}</span>
                </div>
            ))}
        </div>
    )
}
```

**颜色编码**：
- `diff-removed`（红色背景）：被删除的行（如空行、页眉页脚、重复块）
- `diff-added`（绿色背景）：新增的行（如 HTML 注释标记）
- `diff-unchanged`（白色背景）：未改变的行

#### NoteDetail.tsx — 笔记详情页（重写）

**文件位置**：`frontend/src/pages/NoteDetail.tsx`

**重写内容**：从单视图改为三视图模式。

```typescript
type ViewMode = 'original' | 'clean' | 'diff'

export default function NoteDetail() {
    const [viewMode, setViewMode] = useState<ViewMode>('original')
    const [diffData, setDiffData] = useState<CleaningDiffResponse | null>(null)

    // 清洗完成后自动切换到清洗版
    useEffect(() => {
        if (note.status === 'cleaned' && viewMode === 'original') {
            setViewMode('clean')
        }
    }, [note.status])

    // 切换到对比视图时懒加载 diff 数据
    useEffect(() => {
        if (viewMode === 'diff' && !diffData) {
            getCleaningDiff(noteId).then(setDiffData)
        }
    }, [viewMode])

    return (
        <div>
            {/* 视图切换按钮 */}
            <div className="view-mode-tabs">
                <button onClick={() => setViewMode('original')}>原始版</button>
                <button onClick={() => setViewMode('clean')}>清洗版</button>
                <button onClick={() => setViewMode('diff')}>对比视图</button>
            </div>

            {/* 根据视图模式渲染不同内容 */}
            {viewMode === 'original' && <MarkdownContent content={originalMd} />}
            {viewMode === 'clean' && <MarkdownContent content={cleanMd} />}
            {viewMode === 'diff' && diffData && <DiffView diffLines={diffData.diff_lines} />}

            {/* 清洗操作面板 */}
            <CleaningPanel noteId={noteId} status={note.status} onStatusChange={refreshNote} />
        </div>
    )
}
```

**三视图模式**：
1. **原始版**：显示未经任何处理的 Markdown 原文
2. **清洗版**：显示经过去噪和去重后的清洗副本
3. **对比视图**：以行级 diff 的方式展示两者的差异

**懒加载**：diff 数据只在用户切换到对比视图时才请求，避免不必要的 API 调用和计算。

---

### 4.7 复习调度相关组件

#### Review.tsx — 复习答题页

**文件位置**：`frontend/src/pages/Review.tsx`

**作用**：用户在此页面进行每日复习答题，查看即时反馈和 SM-2 调度信息。

**核心状态**：

```typescript
const [dueQuizzes, setDueQuizzes] = useState<DueQuiz[]>([])     // 到期题目列表
const [currentIndex, setCurrentIndex] = useState(0)              // 当前题目索引
const [userAnswer, setUserAnswer] = useState('')                 // 用户答案
const [showResult, setShowResult] = useState(false)              // 是否显示结果
const [lastResult, setLastResult] = useState<SubmitAnswerResponse | null>(null)  // 上次答题结果
const [stats, setStats] = useState<ReviewStats | null>(null)     // 复习统计
const [completed, setCompleted] = useState(false)                // 是否全部完成
```

**答题流程**：

```
加载到期题目 → 显示当前题目 → 用户输入答案 → 提交 → 即时反馈（正确/错误+解析+SM-2信息） → 下一题 → 全部完成 → 显示统计
```

**按题型渲染不同输入**：

```typescript
{quiz.question_type === 'choice' && (
    // 选择题：显示选项按钮
    <div className="options">
        {JSON.parse(quiz.options || '[]').map((opt, i) => (
            <button key={i} onClick={() => setUserAnswer(opt)}>{opt}</button>
        ))}
    </div>
)}
{quiz.question_type === 'fill_blank' && (
    // 填空题：文本输入框
    <input value={userAnswer} onChange={(e) => setUserAnswer(e.target.value)} />
)}
{quiz.question_type === 'short_answer' && (
    // 简答题：文本域
    <textarea value={userAnswer} onChange={(e) => setUserAnswer(e.target.value)} />
)}
```

**即时反馈**：

答题提交后，显示正确答案、解析和 SM-2 调度信息：

```typescript
{showResult && lastResult && (
    <div className={`feedback ${lastResult.is_correct ? 'correct' : 'incorrect'}`}>
        <p>{lastResult.is_correct ? '✓ 正确！' : '✗ 错误'}</p>
        <p>正确答案：{lastResult.correct_answer}</p>
        {lastResult.explanation && <p>解析：{lastResult.explanation}</p>}
        <p>下次复习：{lastResult.sm2_info.next_review_at}</p>
        <p>间隔：{lastResult.sm2_info.interval}天 | EF：{lastResult.sm2_info.easiness_factor}</p>
    </div>
)}
```

#### client.ts — 复习调度 API 封装

**新增的 TypeScript 接口**：

```typescript
interface DueQuiz {
    id: string
    question_type: string
    question: string
    options: string | null
    answer: string
    explanation: string | null
    difficulty: string
    interval: number
    repetition: number
    easiness_factor: number
}

interface SM2Info {
    interval: number
    repetition: number
    easiness_factor: number
    next_review_at: string | null
}

interface SubmitAnswerResponse {
    is_correct: boolean
    quality: number
    correct_answer: string
    explanation: string | null
    sm2_info: SM2Info
}

interface ReviewStats {
    due_count: number
    today_done: number
    today_correct: number
    today_accuracy: number
    total_reviews: number
    total_correct: number
    total_accuracy: number
    total_quizzes: number
}
```

**新增的 API 函数**：

```typescript
export async function getDueQuizzes(): Promise<DueQuizListResponse> {
    return request<DueQuizListResponse>('/review/due')
}

export async function submitAnswer(quizId: string, userAnswer: string, timeSpentMs: number): Promise<SubmitAnswerResponse> {
    return request<SubmitAnswerResponse>('/review/submit', {
        method: 'POST',
        body: JSON.stringify({ quiz_id: quizId, user_answer: userAnswer, time_spent_ms: timeSpentMs }),
    })
}

export async function getReviewStats(): Promise<ReviewStats> {
    return request<ReviewStats>('/review/stats')
}

export async function getReviewHistory(page = 1, pageSize = 20): Promise<ReviewHistoryResponse> {
    return request<ReviewHistoryResponse>(`/review/history?page=${page}&page_size=${pageSize}`)
}
```

#### Dashboard.tsx — 仪表盘（第8周+第9周增强）

**文件位置**：`frontend/src/pages/Dashboard.tsx`

**第8周**：新增"今日待复习"卡片，当有待复习题目时显示：

```typescript
{reviewStats && reviewStats.due_count > 0 && (
    <div className="card review-card" onClick={() => navigate('/review')} style={{ cursor: 'pointer' }}>
        <h3>今日待复习</h3>
        <p className="review-count">{reviewStats.due_count}</p>
        <p>点击开始复习</p>
    </div>
)}
```

**第9周增强**：新增三大区域——今日学习报告、薄弱点列表、7天趋势柱状图。

**今日学习报告**：4个统计卡片（新掌握数、复习次数、正确率、复习时长）+ 各题型正确率标签：

```typescript
{dailyReport && (
    <section>
        <h2>今日学习报告 ({dailyReport.date})</h2>
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(140px, 1fr))' }}>
            <div className="card">{dailyReport.new_mastered} 新掌握</div>
            <div className="card">{dailyReport.total_reviews} 复习次数</div>
            <div className="card">{dailyReport.today_accuracy}% 正确率</div>
            <div className="card">{formatTime(dailyReport.total_review_time_ms)} 复习时长</div>
        </div>
    </section>
)}
```

**薄弱点列表**：top 3 薄弱点，红色标记，可点击跳转卡片详情：

```typescript
{weakPoints.length > 0 && (
    <div className="card" style={{ borderLeft: '4px solid #f44336' }}>
        <h3>薄弱点</h3>
        {weakPoints.map(wp => (
            <div onClick={() => navigate(`/cards/${wp.card_id}`)}>
                {wp.card_title} — 错{wp.error_count}次 | {wp.accuracy}%
            </div>
        ))}
    </div>
)}
```

**7天趋势柱状图**：纯CSS实现，不引入echarts。正确率>=60%蓝色，<60%橙色：

```typescript
{weeklyTrend.length > 0 && (
    <section>
        <h2>本周复习趋势</h2>
        <div style={{ display: 'flex', alignItems: 'flex-end', height: 120 }}>
            {weeklyTrend.map(day => (
                <div style={{ flex: 1 }}>
                    <div style={{
                        height: day.review_count / maxReviewCount * 80,
                        background: day.accuracy >= 60 ? 'var(--color-primary)' : '#ff9800',
                    }} />
                    <span>{day.date.slice(5)}</span>
                </div>
            ))}
        </div>
    </section>
)}
```

**为什么不用 echarts？** 保持前端最小依赖，纯CSS柱状图足以展示7天趋势。

#### Navbar.tsx — 导航栏新增"复习"按钮

```typescript
<button onClick={() => navigate('/review')}>复习</button>
```

#### App.tsx — 路由新增 /review

```typescript
import Review from './pages/Review'
// ...
<Route path="/review" element={<Review />} />
```

---

### 4.5 global.css — 设计系统

**文件位置**：`frontend/src/styles/global.css`

**CSS 变量**：

```css
:root {
    --color-primary: #2563eb;        /* 主色调：蓝色 */
    --color-bg: #f8fafc;             /* 背景色：浅灰 */
    --color-surface: #ffffff;         /* 卡片色：白色 */
    --color-text: #1e293b;           /* 文字色：深灰 */
    --color-border: #e2e8f0;         /* 边框色 */
    --color-error: #dc2626;          /* 错误色：红色 */
    --space-md: 16px;                /* 中等间距 */
    --radius-md: 8px;                /* 中等圆角 */
    --shadow-sm: 0 1px 2px rgba(0,0,0,0.05);  /* 小阴影 */
}
```

CSS 变量是设计系统的核心。所有颜色、间距、圆角、阴影都定义为变量，组件中使用变量而非硬编码值。这样修改主题时只需改变量定义，不用改每个组件。

**组件样式**：

```css
.btn {
    display: inline-flex;
    align-items: center;
    padding: var(--space-sm) var(--space-md);
    border-radius: var(--radius-sm);
    font-weight: 500;
    transition: all 0.15s ease;  /* 平滑过渡 */
}

.btn-primary { background: var(--color-primary); color: white; }
.btn-secondary { background: var(--color-surface); border: 1px solid var(--color-border); }
.btn-danger { background: var(--color-error); color: white; }
```

按钮有三种变体：主要（蓝色）、次要（白底灰边）、危险（红色）。通过 CSS 类名组合使用。

---

### 4.6 理解管道相关组件

#### KnowledgeCards.tsx — 知识卡片列表页

**文件位置**：`frontend/src/pages/KnowledgeCards.tsx`

**作用**：展示当前用户所有知识卡片，按所属笔记分组，每组可折叠/展开。

```typescript
interface NoteGroup {
  note_id: string
  note_title: string       // 后端 JOIN notes 表获取
  cards: KnowledgeCard[]
}

export default function KnowledgeCards() {
    const [groups, setGroups] = useState<NoteGroup[]>([])
    const [expandedNotes, setExpandedNotes] = useState<Set<string>>(new Set())

    useEffect(() => {
        getKnowledgeCards(1, 999).then(data => {
            const grouped = groupByNote(data.items)
            setGroups(grouped)
            setExpandedNotes(new Set(grouped.map(g => g.note_id)))
        })
    }, [])

    function groupByNote(cards: KnowledgeCard[]): NoteGroup[] {
        const map = new Map<string, KnowledgeCard[]>()
        for (const card of cards) {
            const list = map.get(card.note_id) || []
            list.push(card)
            map.set(card.note_id, list)
        }
        return Array.from(map.entries())
            .map(([noteId, cards]) => ({ note_id: noteId, note_title: cards[0].note_title, cards }))
            .sort((a, b) => b.cards[0].created_at.localeCompare(a.cards[0].created_at))
    }

    return (
        <div>
            {/* 每个文件一个分组标题栏，点击折叠/展开 */}
            {/* 展开后显示该文件的卡片网格 */}
            {/* 卡片样式与原来相同 */}
        </div>
    )
}
```

**关键变更**：
1. **全量加载**：`pageSize=999`，不再依赖服务端分页
2. **按 note_id 分组**：使用 `Map` 将同一笔记的卡片归为一组
3. **可折叠**：`expandedNotes` Set 控制每个分组的展开/折叠状态，点击标题栏切换
4. **`note_title`**：来自后端 JOIN `notes` 表，笔记被删除后显示"已删除的笔记"

#### CardDetail.tsx — 知识卡片详情页

**文件位置**：`frontend/src/pages/CardDetail.tsx`

**作用**：展示知识卡片的完整内容、原始出处和关联题目。

```typescript
export default function CardDetail() {
    const { cardId } = useParams()
    const [card, setCard] = useState<KnowledgeCardDetail | null>(null)

    useEffect(() => {
        if (cardId) getKnowledgeCard(cardId).then(setCard)
    }, [cardId])

    return (
        <div>
            {/* 卡片标题、类型、内容 */}
            {/* 章节摘要、原始出处 */}
            {/* 关联题目列表 */}
        </div>
    )
}
```

#### QA.tsx — 智能问答页

**文件位置**：`frontend/src/pages/QA.tsx`

**作用**：用户输入问题，AI 基于知识库回答。

```typescript
export default function QA() {
    const [question, setQuestion] = useState('')
    const [answer, setAnswer] = useState<AnswerResponse | null>(null)
    const [loading, setLoading] = useState(false)

    async function handleAsk() {
        setLoading(true)
        const res = await askQuestion(question)
        setAnswer(res)
        setLoading(false)
    }

    return (
        <div>
            <input value={question} onChange={(e) => setQuestion(e.target.value)} />
            <button onClick={handleAsk}>提问</button>
            {/* AI 回答 + 引用来源 */}
        </div>
    )
}
```

#### NoteDetail.tsx — 笔记详情页增强

**文件位置**：`frontend/src/pages/NoteDetail.tsx`（修改）

**新增功能**：
- **"开始学习"按钮**：cleaned 状态下显示，触发理解管道
- **学习状态轮询**：learning 状态下每5秒查询一次，直到变为 archived 或 learning_failed
- **learning_failed 状态**：显示错误信息和"重新学习"按钮

```typescript
// 开始学习按钮
{note.status === 'cleaned' && (
    <button onClick={handleStartLearning}>开始学习</button>
)}

// 学习状态轮询
useEffect(() => {
    if (note?.status !== 'learning' || !noteId) return
    const interval = setInterval(async () => {
        const data = await getNote(noteId)
        setNote(data)
        if (data.status !== 'learning') clearInterval(interval)
    }, 5000)
    return () => clearInterval(interval)
}, [note?.status, noteId])
```

---

### 4.7 第7周新增功能（第7周：知识库归档与整理）

#### 笔记归档与取消归档

**后端**：[api/notes.py](file:///d:/test/engramnote/backend/app/api/notes.py)

新增两个端点，实现笔记的手动归档/取消归档：

```python
@router.post("/{note_id}/archive", response_model=NoteResponse)
async def archive_note_api(note_id, current_user, db):
    """切换笔记归档状态：archived ↔ cleaned"""
    note = await get_note_detail(db, note_id, current_user.id)
    if note.status == NoteStatus.archived:
        note.status = NoteStatus.cleaned
    else:
        note.status = NoteStatus.archived
    await db.commit()
    return NoteResponse.model_validate(note)
```

工作原理：
- 状态切换是双向的：`archived → cleaned`（取消归档）或 `cleaned/converted → archived`（归档）
- 理解管道完成后**自动**将笔记设为 `archived`，用户也可以**手动**触发切换
- 已归档的笔记可以通过 `GET /notes/archive` 单独筛选

**前端**：[NoteDetail.tsx](file:///d:/test/engramnote/frontend/src/pages/NoteDetail.tsx)

在笔记详情页头部按钮区添加"归档/取消归档"按钮：
```tsx
{(note.status === 'cleaned' || note.status === 'learning_failed' || note.status === 'converted' || note.status === 'archived') && (
  <button className="btn btn-secondary" onClick={handleArchive}>
    {note.status === 'archived' ? '取消归档' : '归档'}
  </button>
)}
```

**前端**：[NotesList.tsx](file:///d:/test/engramnote/frontend/src/pages/NotesList.tsx)

笔记列表添加"全部 / 已归档"筛选标签，通过 `showArchived` 状态切换调用 `getNotes()` 或 `getArchivedNotes()`。

#### 知识卡片编辑与删除

**后端**：[api/understanding.py](file:///d:/test/engramnote/backend/app/api/understanding.py)

```python
# 编辑卡片
@router.put("/cards/{card_id}", response_model=KnowledgeCardResponse)
async def update_card(card_id, req: CardUpdateRequest, current_user, db):
    """更新知识卡片的标题和内容（可选字段，只传需要改的）"""

# 删除卡片（级联删除关联题目）
@router.delete("/cards/{card_id}", status_code=204)
async def delete_card(card_id, current_user, db):
    """先删除 QuizItem.where(card_id == card_id)，再删除卡片本身"""
```

**Schema**：[schemas/knowledge.py](file:///d:/test/engramnote/backend/app/schemas/knowledge.py)
```python
class CardUpdateRequest(BaseModel):
    """卡片更新请求，title 和 content 均为可选"""
    title: Optional[str] = None
    content: Optional[str] = None
```

**前端**：[CardDetail.tsx](file:///d:/test/engramnote/frontend/src/pages/CardDetail.tsx)

| 模式 | 触发方式 | UI 变化 |
|---|---|---|
| 编辑模式 | 点击"编辑"按钮 | 标题变为 `<input>`，内容变为 `<textarea>`，顶部按钮变为"保存/取消" |
| 删除模式 | 点击"删除"按钮 | `confirm()` 确认对话框 → 调 API → 成功后跳转回 `/cards` |

核心状态：
```tsx
const [editing, setEditing] = useState(false)
const [editTitle, setEditTitle] = useState('')
const [editContent, setEditContent] = useState('')
```

#### 知识卡片去重检测

**后端**：[understanding_service.py](file:///d:/test/engramnote/backend/app/services/understanding_service.py)

```python
async def detect_card_duplicates(db, user_id, card, threshold=5):
    """检测单张卡片与用户已有卡片之间的重复
    
    不使用嵌入模型（避免段错误），降级为 n-gram 关键词匹配。
    2-4字的 n-gram 提取后与已有卡片做关键词匹配，score > threshold 则视为重复。
    """
```

API 端点：`GET /understanding/{note_id}/duplicates`

返回该笔记每张卡片与其他已有卡片的重复候选列表（最多返回 top 5），前端在 NoteDetail 中展示。

#### 双向链接

**NoteDetail → 卡片**：在 [NoteDetail.tsx](file:///d:/test/engramnote/frontend/src/pages/NoteDetail.tsx) 页面底部展示"关联知识卡片"区域：
- 调用 `getKnowledgeCards(1, 50, noteId)`
- 展示最多 6 张卡片预览（使用与 KnowledgeCards 相同的卡片网格样式）
- 点击卡片跳转到 `/cards/{card.id}`
- 超过 6 张显示"查看全部 N 张卡片"按钮

**CardDetail → 笔记**：在[CardDetail.tsx](file:///d:/test/engramnote/frontend/src/pages/CardDetail.tsx)顶部添加"来源笔记"链接：
```tsx
{card.note_id && (
  <p>
    来源笔记：
    <span style={{ cursor: 'pointer', color: 'var(--color-primary)' }}
          onClick={() => navigate(`/notes/${card.note_id}`)}>
      {card.note_title || '查看笔记'}
    </span>
  </p>
)}
```

---

## 第五章：配置文件与基础设施

### .env — 环境变量

```
DATABASE_URL=               # 留空=SQLite，填值=PostgreSQL
STORAGE_BACKEND=local       # local=本地文件系统，minio=MinIO
CELERY_BACKEND=local        # local=文件系统broker，redis=Redis
JWT_SECRET_KEY=engramnote-dev-secret-change-in-production
DEEPSEEK_API_KEY=sk-xxx     # DeepSeek AI API 密钥
GLM_API_KEY=xxx.xxx         # 智谱 GLM API 密钥（JWT格式：id.secret）
GLM_MODEL=glm-4.7-flash     # GLM 模型名（必须全小写）
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4  # GLM API 地址
DEBUG=true                   # debug模式：true=用GLM(免费)，false=用DeepSeek
MINERU_API_TOKEN=eyJxxx     # Mineru 文档解析 API 令牌
```

**关键设计**：所有配置都有默认值，`.env` 文件可以完全为空也能运行。这是**零配置启动**的理念。

### requirements.txt — Python 依赖

```
fastapi>=0.115.0            # Web 框架
uvicorn[standard]>=0.30.0   # ASGI 服务器
sqlalchemy[asyncio]>=2.0.0  # ORM
aiosqlite>=0.20.0           # SQLite 异步驱动
pydantic>=2.0.0             # 数据校验
pydantic-settings>=2.0.0    # 配置管理
python-jose[cryptography]   # JWT
bcrypt>=4.0.0               # 密码哈希
celery>=5.4.0               # 异步任务队列
python-multipart>=0.0.9     # 文件上传支持
```

注意可选依赖被注释掉了：
```
# asyncpg>=0.30.0    # PostgreSQL 驱动
# minio>=7.2.0       # MinIO 客户端
# redis>=5.0.0       # Redis 客户端
```

开发阶段不需要这些，生产环境取消注释即可。

### vite.config.ts — 前端开发服务器配置

```typescript
export default defineConfig({
    plugins: [react()],
    server: {
        port: 5173,
        proxy: {
            '/api': {
                target: 'http://localhost:8000',  // 代理到后端
                changeOrigin: true,
            },
        },
    },
})
```

**代理**：前端运行在 `localhost:5173`，后端运行在 `localhost:8000`。当前端请求 `/api/xxx` 时，Vite 开发服务器会自动把请求转发到后端。这样前端代码中可以直接写 `/api/xxx`，不用写完整的 `http://localhost:8000/api/xxx`。

### docker-compose.yml — Docker 配置（可选）

```yaml
services:
    postgres:    # PostgreSQL 数据库
    redis:       # Redis 消息队列
    minio:       # MinIO 对象存储
```

这是生产环境的配置，开发阶段不需要。当前系统没有安装 Docker，所以项目改用了零依赖方案。

---

## 第六章：数据流全链路走一遍

让我们跟踪一个完整的操作——"用户上传一个 PDF 文件，经过转换和清洗，查看清洗结果"。

### 步骤 1：用户在前端点击上传

1. 用户在 Upload 页面拖拽一个 PDF 文件
2. 前端校验文件扩展名（`.pdf` 在允许列表中）
3. 调用 `uploadFile(file)` 发送 POST 请求到 `/api/upload`

### 步骤 2：后端接收上传请求

1. `upload.py` 的 `upload_document()` 函数处理请求
2. 校验文件扩展名和大小
3. 创建 Note 记录（状态：`uploading`）
4. 将文件保存到 `data/storage/original-files/{user_id}/{note_id}/xxx.pdf`
5. 更新状态为 `converting`
6. 触发 Celery 任务：`convert_document_task.delay(note_id, ...)`
7. 返回笔记信息给前端

### 步骤 3：前端轮询状态

1. 前端收到上传成功的响应，获取 `noteId`
2. 每 5 秒调用 `getUploadStatus(noteId)` 查询状态
3. 状态为 `converting` 时继续等待

### 步骤 4：Celery 在后台转换

1. Celery worker 从队列中取出任务
2. 从存储下载原始 PDF 到临时目录
3. 调用 `mineru_plus.converter.convert(local_file)` 转换为 Markdown
4. 将 Markdown 上传到 `data/storage/markdown/{user_id}/{note_id}/original.md`
5. 更新笔记状态为 `converted`，记录 `original_md_path`

### 步骤 5：自动触发清洗

1. 转换成功后，`convert_tasks.py` 自动调用 `clean_document_task.delay(note_id)`
2. 笔记状态更新为 `cleaning`
3. 前端轮询发现状态变为 `cleaning`，继续等待

### 步骤 6：Celery 在后台清洗

1. Celery worker 从队列中取出清洗任务
2. 从存储读取原始 Markdown 文本
3. **规则清洗**：去空行、去页眉页脚、去水印、统一标点
4. **文本分块**：按 chunk_size=500 分块，overlap=50
5. **嵌入向量生成**：使用 BGE-M3 模型为每个块生成嵌入向量
6. **存入向量数据库**：将块和向量存入 Chroma
7. **余弦相似度去重**：两两计算块间相似度，超过 0.85 阈值标记为重复
8. **生成清洗副本**：重复块用 HTML 注释包裹，保留原文可恢复
9. 将清洗副本上传到 `data/storage/markdown/{user_id}/{note_id}/clean.md`
10. 更新笔记状态为 `cleaned`，记录 `clean_md_path`

### 步骤 7：前端检测到清洗完成

1. 轮询发现状态变为 `cleaned`
2. 自动跳转到笔记详情页，默认显示清洗版

### 步骤 8：用户查看笔记详情

1. 前端调用 `getNote(noteId)` 请求 `/api/notes/{noteId}`
2. 后端从数据库查询笔记记录
3. 从存储中读取 Markdown 文件内容（原始版 + 清洗版）
4. 返回笔记信息 + Markdown 内容
5. 前端用 `marked` 将 Markdown 渲染为 HTML 并展示

### 步骤 9：用户审阅清洗结果（可选）

1. 用户切换到"对比视图"查看 diff
2. 前端调用 `getCleaningDiff(noteId)` 获取逐行对比数据
3. DiffView 组件以颜色编码展示差异
4. 用户可以对重复块执行"恢复"或"删除"操作

---

## 第七章：踩过的坑与解决思路

### 坑 1：passlib 与 bcrypt 5.x 不兼容

**问题**：`passlib` 是一个密码哈希库，但它不支持 `bcrypt` 5.x 版本，会报错。

**解决**：不使用 `passlib`，直接使用 `bcrypt` 库：
```python
import bcrypt
hashed = bcrypt.hashpw(password.encode("utf-8"), bcrypt.gensalt())
```

**教训**：当第三方库之间有版本冲突时，看看能不能直接用底层库，绕开中间层。

### 坑 2：Python 3.9 不支持 `str | None` 语法

**问题**：`str | None` 是 Python 3.10+ 的语法，3.9 会报语法错误。

**解决**：使用 `Optional[str]` 替代：
```python
from typing import Optional
def foo(x: Optional[str]) -> None: ...
```

**教训**：如果需要兼容旧版本 Python，使用 `typing` 模块的类型注解。

### 坑 3：SQLite 不支持 JSONB

**问题**：PostgreSQL 支持 `JSONB` 类型（二进制 JSON，查询更快），但 SQLite 不支持。

**解决**：使用 `JSON` 类型，兼容两种数据库：
```python
metadata_: Mapped[Optional[Dict]] = mapped_column(JSON, nullable=True)
```

**教训**：开发环境用 SQLite 时，要避免使用只有 PostgreSQL 才有的特性。

### 坑 4：SQLite 不支持 pool_size

**问题**：`create_async_engine` 的 `pool_size` 参数是连接池配置，SQLite 不支持。

**解决**：条件判断：
```python
if not _is_sqlite:
    engine_kwargs["pool_size"] = 5
    engine_kwargs["max_overflow"] = 10
```

### 坑 5：系统没有安装 Docker

**问题**：原始方案需要 Docker 运行 PostgreSQL、Redis、MinIO，但开发机器没有 Docker。

**解决**：全面改用零依赖方案：
- PostgreSQL → SQLite
- Redis → 文件系统 broker
- MinIO → 本地文件系统

通过策略模式和配置切换，保留了后续切换到生产方案的能力。

**教训**：好的架构设计应该让开发环境尽可能简单，同时不丧失生产环境的扩展能力。

### 坑 6：Celery unregistered task

**问题**：新增 `clean_tasks.py` 后，Celery worker 报错 `Received unregistered task of type 'app.tasks.clean_tasks.clean_document_task'`。

**解决**：在 `celery_app.py` 的 `include` 列表中添加 `clean_tasks`：
```python
celery_app.conf.update(
    include=["app.tasks.convert_tasks", "app.tasks.clean_tasks"],
)
```

**教训**：Celery 不会自动发现新任务模块，必须手动注册到 `include` 列表中。新增任务文件后一定要检查这个配置。

### 坑 7：HuggingFace 国内连接超时

**问题**：下载 BAAI/bge-m3 嵌入模型时，HuggingFace 连接超时，无法下载。

**解决**：优先通过 ModelScope 下载模型，ModelScope 是国内的 AI 模型平台，镜像了 HuggingFace 上的主流模型：
```python
from modelscope import snapshot_download
model_dir = snapshot_download("Xorbits/bge-m3", cache_dir="data/models")
```

**教训**：在国内开发 AI 应用时，优先使用国内模型平台（ModelScope）作为下载源，HuggingFace 作为回退。

### 坑 8：httpx 连接重置（WinError 10053）

**问题**：前端轮询笔记状态时，偶发 `httpx.ConnectResetError: [WinError 10053]` 连接重置错误。

**解决**：在轮询函数中添加 try/except 重试逻辑，并增加 timeout 参数：
```typescript
async function pollStatus(id: string) {
    const interval = setInterval(async () => {
        try {
            const res = await getUploadStatus(id)
            // 处理状态...
        } catch (err) {
            // 连接重置时忽略，下次轮询会重试
            console.warn('轮询状态失败，将在下次重试', err)
        }
    }, 5000)
}
```

**教训**：网络请求在 Windows 上可能偶发连接重置，轮询类操作必须有容错机制。

### 坑 9：笔记卡在 cleaning 状态

**问题**：笔记状态变为 `cleaning` 后不再变化，清洗任务似乎没有执行。

**原因**：旧版 Celery worker 没有注册 `clean_tasks`，任务被接收但无法执行，而状态已经更新为 `cleaning`。

**解决**：创建 `reset_cleaning.py` 脚本，将卡在 `cleaning` 状态的笔记重置为 `converted`，然后重启 Celery worker 并重新触发清洗。

**教训**：状态机中的状态更新和任务分发必须保证一致性。如果任务可能失败，状态更新应该放在任务开始执行后而不是分发时。

### 坑 10：手动清洗触发后状态不更新

**问题**：用户点击"重新清洗"按钮后，前端刷新仍显示旧状态（`cleaning_failed`），看起来像是没有触发。

**原因**：`start_cleaning` API 只触发了 Celery 任务，但没有在数据库中更新状态。状态更新是在 Celery worker 执行任务时才做的，前端刷新时数据库中还是旧状态。

**解决**：在 `start_cleaning` API 中，触发 Celery 任务前先将数据库状态更新为 `cleaning`：
```python
# 先更新数据库状态
db_note.status = NoteStatus.cleaning
db_note.error_message = None
await db.commit()
# 再触发 Celery 任务
clean_document_task.delay(note_id)
```

**教训**：API 返回的状态应该与数据库中的状态一致，不能只依赖异步任务来更新状态。

### 坑 11：NoteDetail 缺少清洗状态轮询

**问题**：用户触发清洗后，页面不会自动刷新，需要手动刷新浏览器才能看到清洗进度。

**解决**：在 NoteDetail 中添加 useEffect 轮询逻辑：
```typescript
useEffect(() => {
    if (note?.status !== 'cleaning' || !noteId) return
    const interval = setInterval(async () => {
        const data = await getNote(noteId)
        setNote(data)
        if (data.status !== 'cleaning') {
            clearInterval(interval)
            if (data.status === 'cleaned' && data.clean_md_content) {
                setViewMode('clean')
            }
        }
    }, 5000)
    return () => clearInterval(interval)
}, [note?.status, noteId])
```

**教训**：涉及异步状态变化的页面必须有轮询或 WebSocket 机制，否则用户无法感知进度。

### 坑 12：marked v14 不支持 highlight 选项

**问题**：`marked.setOptions({ highlight })` 在 marked v14 中报 TypeScript 错误：`'highlight' does not exist in type 'MarkedOptions'`。

**原因**：marked v14 移除了内置的 `highlight` 选项，改为通过扩展实现。

**解决**：安装 `marked-highlight` 扩展，改用 `marked.use()` 注册：
```typescript
import { markedHighlight } from 'marked-highlight'
marked.use(markedHighlight({
    langPrefix: 'hljs language-',
    highlight(code: string, lang: string) {
        if (lang && hljs.getLanguage(lang)) {
            return hljs.highlight(code, { language: lang }).value
        }
        return hljs.highlightAuto(code).value
    },
}))
```

**教训**：依赖库升级时注意查看 Breaking Changes，API 可能发生变化。

### 坑 13：Celery retry 与停止清洗冲突

**问题**：用户点击"停止清洗"后，状态变为 `cleaning_failed`，但 Celery 任务的 `self.retry()` 会重新执行任务，`_update_note_status(cleaning)` 会把 `cleaning_failed` 覆盖回 `cleaning`，导致清洗无视用户停止操作重新启动。

**原因**：Celery retry 机制不感知业务状态。重试时直接从头执行任务逻辑，包括更新状态为 `cleaning`，覆盖了用户通过 `stop_cleaning` API 设置的 `cleaning_failed` 状态。

**解决**：新增 `_get_note_status()` 函数，在两个关键位置检查状态：
1. **任务开始前**：如果状态已是 `cleaning_failed`（用户已停止），直接跳过
2. **重试前**：如果状态已是 `cleaning_failed`，放弃重试，直接 return

```python
# 任务开始前检查
current_status = asyncio.run(_get_note_status(note_id))
if current_status == NoteStatus.cleaning_failed:
    return  # 用户已停止，不执行

# 重试前检查
current_status = asyncio.run(_get_note_status(note_id))
if current_status == NoteStatus.cleaning_failed:
    return  # 用户已停止，不重试
```

**教训**：异步任务的重试机制必须与业务状态协调。当用户可以主动取消操作时，重试逻辑必须在执行前检查当前状态，避免覆盖用户的取消操作。

### 坑 14：GLM 模型名大小写问题

**问题**：`.env` 中 `GLM_MODEL=GLM-4.7-flash`（大写），调用智谱 API 时报错。

**原因**：智谱 API 要求模型名必须全小写 `glm-4.7-flash`。

**解决**：修正 `.env` 中的模型名为全小写，并在 `config.py` 中设置默认值也为小写。

**教训**：不同 LLM 提供商对模型名的格式要求不同，调用前务必查阅官方文档。

### 坑 15：pydantic-settings 静默丢弃未声明字段

**问题**：`.env` 中有 `GLM_API_KEY`、`GLM_MODEL`、`GLM_BASE_URL`，但 `settings.glm_api_key` 访问时报 `AttributeError`。

**原因**：`Settings` 类设置了 `extra="ignore"`，未在类中声明的字段会被静默丢弃，不会报错也不会存储。

**解决**：在 `Settings` 类中显式声明这三个字段：
```python
glm_api_key: str = ""
glm_model: str = "glm-4.7-flash"
glm_base_url: str = "https://open.bigmodel.cn/api/paas/v4"
```

**教训**：使用 pydantic-settings 时，`.env` 中的变量必须在 Settings 类中有对应字段声明，否则会被静默忽略。`extra="ignore"` 不报错但也不存储，容易造成困惑。

### 坑 16：GLM 429 限流与批量题目生成

**问题**：GLM 免费版有速率限制，143个知识卡片逐个生成题目时频繁触发 429 Too Many Requests。

**解决**：
1. 增加429专用延迟（30s/60s/90s/120s/150s）
2. 增加重试次数 3→5
3. 题目生成改为批量模式（每批30卡片合并1次API调用），143卡片只需5次调用

**教训**：免费 LLM API 有严格的速率限制，批量调用是减少请求次数的有效策略。

### 坑 17：SQLAlchemy 异步会话延迟加载 MissingGreenlet

**问题**：Celery 任务中 `asyncio.run()` 访问 ORM 对象属性时报 `MissingGreenlet` 错误。

**原因**：`await db.commit()` 后 ORM 对象属性过期，再次访问触发同步延迟加载，在异步上下文中报错。

**解决**：查询后立即提取所有属性值到字典列表：
```python
cards_data = [
    {"id": card.id, "user_id": card.user_id, "title": card.title, ...}
    for card in cards
]
# 后续使用 cards_data 字典，而非 card ORM 对象
```

**教训**：在 SQLAlchemy 异步会话中，commit 后不要直接访问 ORM 对象属性。要么提前提取值，要么使用 `expire_on_commit=False`。

### 坑 18：SQLite options 字段类型不匹配

**问题**：`QuizItem.options` 字段类型是 `Text`（字符串），但代码直接传入 Python 列表，SQLite 报错 `Error binding parameter 7 - probably unsupported type`。

**解决**：手动 JSON 序列化：
```python
options=json.dumps(q.get("options"), ensure_ascii=False) if q.get("options") else None
```

**教训**：SQLite 不支持原生 JSON 数组类型，存储前必须将 Python 列表/字典转为 JSON 字符串。

### 坑 19：嵌入模型在 FastAPI 进程中加载导致段错误

**问题**：RAG 问答服务加载 BGE-M3 嵌入模型时，FastAPI 进程崩溃，报错 0xC0000005（段错误）。

**原因**：嵌入模型（基于 PyTorch/SentenceTransformer）与 FastAPI 的异步事件循环存在冲突，在 Windows 上尤为明显。

**临时解决**：RAG 服务禁用向量检索，改用知识卡片中文 n-gram 匹配替代。

**永久方案**：将嵌入模型加载移到独立服务或子进程中（TODO）。

**教训**：重量级 ML 模型不应在 Web 服务进程中直接加载，应通过独立进程或微服务隔离。

### 坑 20：GLM 免费版 429 限流严重

**问题**：GLM 免费版 429 限流严重，5次重试（30s/60s/90s/120s/150s 延迟）全部失败，理解管道无法完成。

**原因**：GLM 免费版的速率限制非常严格，短时间内多次调用必然触发 429。即使使用了多轮对话模式减少了调用次数（34次→17次），仍然可能触发限流。

**临时解决**：测试时切换到 DeepSeek（设置 `DEBUG=false`），DeepSeek 无此限制。

**已实施的方案**：
1. 生产环境使用 DeepSeek 或付费版 GLM
2. **RateLimiter 令牌桶**：控制每分钟最大请求数（`llm_max_rpm`，默认 10 RPM），请求前获取令牌，无令牌时等待
3. **asyncio.Semaphore(3)**：同一 LLMService 实例最多 3 个并发请求
4. **章节合并**：每3章合并为1次 API 请求，理解管道请求数从17次降为6次
5. **batch_size 增大**：题目生成从每批10卡片改为30卡片

**教训**：免费 LLM API 的速率限制可能严重到无法完成批量任务。开发时需要准备备用 LLM 提供商。多级限流（令牌桶 + Semaphore + 批次合并 + 抖动重试）是有效的综合方案。

### 坑 21：SQLite 缺少 SM-2 新增列

**问题**：QuizItem 模型新增了 6 个 SM-2 字段（interval, repetition, easiness_factor, next_review_at, last_reviewed_at, review_count），但已有的 SQLite 数据库没有这些列，查询时报错。

**原因**：SQLAlchemy 的 `create_all` 只创建不存在的表，不会修改已有表的结构（不会 ALTER TABLE ADD COLUMN）。

**解决**：手动执行 ALTER TABLE 添加新列：
```sql
ALTER TABLE quiz_items ADD COLUMN interval INTEGER DEFAULT 1;
ALTER TABLE quiz_items ADD COLUMN repetition INTEGER DEFAULT 0;
ALTER TABLE quiz_items ADD COLUMN easiness_factor FLOAT DEFAULT 2.5;
ALTER TABLE quiz_items ADD COLUMN next_review_at DATETIME;
ALTER TABLE quiz_items ADD COLUMN last_reviewed_at DATETIME;
ALTER TABLE quiz_items ADD COLUMN review_count INTEGER DEFAULT 0;
```

**教训**：在开发阶段修改模型后，需要手动同步数据库结构。生产环境应使用 Alembic 等迁移工具。

### 坑 22：DueQuizResponse 缺少 repetition 字段

**问题**：E2E 测试提交答案后，前端报错"缺少 repetition 字段"。

**原因**：`schemas/review.py` 的 DueQuizResponse 最初没有 `repetition` 字段，但前端需要此字段来展示复习进度。

**解决**：在 DueQuizResponse 中添加 `repetition: int = 0`。

**教训**：设计 API 响应模型时，要考虑前端实际需要哪些字段，不能只从后端数据模型出发。前后端联调时尽早发现缺失字段。

### 坑 23：简答题部分匹配评分偏低

**问题**：用户回答"劳动合同是关于劳动关系的协议"，正确答案是完整的法律条文，n-gram 关键词覆盖率很低，quality=1。

**原因**：n-gram 匹配从正确答案中提取 2-4 字片段作为关键词，如果正确答案很长而用户回答很短，覆盖率自然很低。这是 n-gram 方法的固有局限。

**解决**：调整测试断言为 `1 <= q2 <= 4`，接受 n-gram 评分的波动性。后续可考虑：1) 只从用户答案中提取关键词去匹配正确答案（反向覆盖率）；2) 使用 TF-IDF 加权关键词；3) 接入语义相似度模型。

**教训**：n-gram 匹配对"简答"类题目的评分不够精确，适合作为 MVP 方案，后续需要优化。

### 坑 24：选择题任何选项都判错

**问题**：用户在复习页面选择正确选项，系统始终判定为错误。

**原因**：`quality_from_answer` 的选择题分支用 `user_lower == correct_lower` 精确匹配。但前端 Review.tsx 点击选项时，`userAnswer` 设为完整选项文本（如 `"A. 劳动合同是劳动者与用人单位确立劳动关系的协议"`），而数据库 `answer` 字段可能只是 `"A"`。两者永远不相等，所以 quality 始终为 1。

**解决**：将选择题评分改为多格式匹配策略，按优先级依次尝试：
1. 精确匹配（最简单的情况）
2. 提取字母前缀（A/B/C/D）匹配
3. answer 是字母、user 是完整文本 → 检查用户答案是否以该字母开头
4. user 是字母、answer 是完整文本 → 检查正确答案是否以该字母开头
5. 去掉字母前缀后比较纯文本内容
6. 以上都不匹配 → quality=1（错误）

```python
# 核心修复代码
user_letter = re.match(r'^([a-d])', user_lower)
correct_letter = re.match(r'^([a-d])', correct_lower)
if user_letter and correct_letter and user_letter.group(1) == correct_letter.group(1):
    return 5
```

**教训**：前后端数据格式不一致是常见 bug 源头。选择题的"答案"在不同上下文中有不同表示（字母 vs 完整文本），评分逻辑必须兼容所有格式。

### 坑 25：问题集页面 page_size 超限导致 422

**问题**：问题集页面加载时显示"共 0 道题"，实际有题目存在。

**原因**：`getQuestions(1, 999, noteId)` 的 `page_size=999` 超过后端 `Query(le=100)` 限制，触发 FastAPI 422 验证错误。前端 `new Error(array)` 将错误数组渲染为 `[object Object]`，用户看到的是不可读的错误信息。

**解决**：
1. QuestionSets.tsx 改为 `page_size=100`，用 `do...while` 循环分页加载全部题目
2. client.ts 的 `request` 函数增加 422 错误 detail 数组处理：检测数组类型，提取每项的 `msg` 字段拼接为可读字符串

```typescript
// client.ts 修复
const detail = Array.isArray(error.detail)
  ? error.detail.map((e) => e.msg || e.message || String(e)).join('; ')
  : (error.detail || `请求失败: ${response.status}`);
throw new Error(detail);
```

**教训**：FastAPI 的参数验证错误（422）返回的 `detail` 是数组格式，与普通错误的字符串格式不同，前端必须特殊处理。

### 坑 26：变量未定义导致 500 内部错误

**问题**：访问 `/review` 页面提示"服务器内部错误"。

**原因**：`get_review_stats` 函数中，`due_count` 的计算引用了 `today_done` 变量（`remaining_quota = max(0, DAILY_REVIEW_LIMIT - today_done)`），但 `today_done` 的数据库查询在它之后才执行。Python 运行时 `today_done` 未定义，抛出 `NameError`，FastAPI 返回 500。

**解决**：将 `today_done` 的查询移到 `due_count` 计算之前，确保变量先赋值再使用。

**教训**：Python 不会在函数定义时检查变量是否存在，只在运行时解析。重构代码移动语句块时，务必检查变量依赖顺序——被引用的变量必须先于引用它的代码执行。

---

## 第八章：如何启动项目

### 后端启动

```bash
# 1. 激活 conda 环境
conda activate mineru_env

# 2. 进入后端目录
cd D:\test\EngramNote\backend

# 3. 安装依赖
pip install -r requirements.txt

# 4. 启动后端服务
uvicorn app.main:app --reload --port 8000
```

`--reload` 表示代码修改后自动重启，方便开发。

### Celery Worker 启动


```bash
# 在另一个终端
conda activate mineru_env
cd D:\test\EngramNote\backend
celery -A app.tasks.celery_app:celery_app worker --loglevel=info --pool=solo
```

`--pool=solo` 在 Windows 上必须使用，因为 Windows 不支持 Celery 的默认进程池。

### 前端启动

```bash
# 1. 确保安装了 Node.js 18+
node --version  # 应该 >= 18.0.0

# 2. 进入前端目录
cd D:\test\EngramNote\frontend

# 3. 安装依赖
npm install

# 4. 启动开发服务器
npm run dev
```

### 验证

1. 打开浏览器访问 `http://localhost:5173`
2. 注册一个账号
3. 上传一个 PDF 文件
4. 等待转换完成，查看笔记详情

或者用后端测试脚本：
```bash
conda activate mineru_env
cd D:\test\EngramNote\backend
python test_api.py
```

---

## 附录：术语速查表

| 术语 | 全称 | 通俗解释 |
|------|------|----------|
| API | Application Programming Interface | 后端给前端提供的"服务窗口" |
| ORM | Object-Relational Mapping | 用 Python 类操作数据库，不用写 SQL |
| JWT | JSON Web Token | 用户认证的"门禁卡" |
| CORS | Cross-Origin Resource Sharing | 允许前端跨域访问后端 |
| CRUD | Create, Read, Update, Delete | 增删改查四种基本操作 |
| Celery | — | 异步任务队列，后台处理耗时操作 |
| broker | — | Celery 的消息队列，用来分发任务 |
| SQLAlchemy | — | Python 最流行的 ORM 框架 |
| Pydantic | — | 数据校验库，FastAPI 的核心依赖 |
| FastAPI | — | Python 的现代 Web 框架，自动生成 API 文档 |
| Vite | — | 前端构建工具，开发服务器极快 |
| React | — | Facebook 的 UI 库，用组件构建页面 |
| TypeScript | — | JavaScript + 类型检查 |
| SQLite | — | 最简单的数据库，整个库就是一个文件 |
| bcrypt | — | 密码哈希算法，安全存储密码 |
| MinIO | — | 开源的对象存储服务，类似 AWS S3 |
| Docker | — | 容器化工具，一键部署多个服务 |
| lifespan | — | FastAPI 的生命周期管理，启动/关闭时执行代码 |
| 依赖注入 | Dependency Injection | FastAPI 自动把数据库会话等"注入"到路由函数中 |
| 策略模式 | Strategy Pattern | 根据配置选择不同实现，调用方不关心细节 |
| 状态机 | State Machine | 定义对象的状态和状态之间的转换规则 |
| 轮询 | Polling | 定时反复查询，直到得到想要的结果 |
| 嵌入 | Embedding | 把文本转换成数字向量，语义相似的文本向量也相近 |
| 向量数据库 | Vector Database | 专门存储和检索向量数据的数据库，用于相似度搜索 |
| Chroma | — | 嵌入式向量数据库，零部署，数据存在本地文件 |
| BGE-M3 | — | 多语言嵌入模型，支持中文，用于生成文本向量 |
| 余弦相似度 | Cosine Similarity | 衡量两个向量方向的相似程度，值域 [-1, 1]，越接近 1 越相似 |
| ModelScope | — | 国内的 AI 模型平台，镜像了 HuggingFace 上的主流模型 |
| diff | — | 两个文本之间的差异对比，显示新增/删除/未变的行 |
| 分块 | Chunking | 将长文本按固定长度或语义切分成小块，便于嵌入和检索 |
| LLM | Large Language Model | 大语言模型，如 DeepSeek、GLM，能理解和生成文本 |
| RAG | Retrieval-Augmented Generation | 检索增强生成，先检索相关知识再让 LLM 回答，减少"胡编乱造" |
| n-gram | — | 文本中连续的 N 个字符/词，用于关键词匹配和文本分析 |
| 429 | HTTP 429 Too Many Requests | API 速率限制错误，表示请求太频繁 |
| 批量生成 | Batch Generation | 将多个任务合并到一次 API 调用中，减少请求次数 |
| SM-2 | SuperMemo 2 | 最经典的间隔重复算法，根据答题质量（0-5）计算复习间隔和舒适度因子 |
| 遗忘曲线 | Forgetting Curve | 艾宾浩斯遗忘曲线，描述记忆随时间衰退的规律，是间隔重复的理论基础 |
| 舒适度因子 | Easiness Factor (EF) | SM-2 算法中的参数，反映记忆难度，默认2.5，最低1.3，越高复习间隔越长 |
| 间隔重复 | Spaced Repetition | 在最佳时间点复习以强化记忆的学习方法，间隔随掌握程度递增 |
| 按需查询 | On-demand Query | 用户访问时查询到期数据，替代定时任务生成，简化架构 |

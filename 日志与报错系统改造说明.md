# 日志系统与报错系统改造说明

> 目标：让每一次报错都能**快速定位** —— 一条错误信息 → 一个 request_id → 一键检索同请求的全部日志（含完整堆栈、用户、任务、耗时）。

改造日期：2026-08-16
涉及代码：

| 文件 | 动作 | 说明 |
|------|------|------|
| `backend/app/core/context.py` | 新增 | 请求/任务上下文（contextvars），贯穿全链路 |
| `backend/app/core/logging_config.py` | 重写 | 上下文感知格式化 + 三路文件输出 + JSON 结构化日志 |
| `backend/app/core/tempfile_compat.py` | 新增 | 运行环境兼容层（临时目录权限问题修复） |
| `backend/app/middleware/request_context.py` | 新增 | 请求ID 生成/透传 + 结构化访问日志（含耗时/状态码/用户） |
| `backend/app/middleware/error_handler.py` | 重写 | 错误响应统一携带 request_id，堆栈完整落盘 |
| `backend/app/main.py` | 修改 | 注册新中间件 + 全局异常处理器（HTTPException/422 也带 request_id） |
| `backend/app/tasks/celery_app.py` | 修改 | Worker 信号：任务上下文注入 + 任务生命周期日志 |
| `backend/app/tasks/convert_tasks.py` | 修复 | 重试耗尽后笔记卡死 converting 的 bug（F-30） |
| `backend/app/tasks/clean_tasks.py` | 修复 | 同上（卡死 cleaning） |
| `backend/app/tasks/understand_tasks.py` | 修复 | 同上（卡死 learning）+ 题目任务重试日志 |

---

## 一、问题背景

改造前的问题：

1. **报错无法定位**：错误日志只有 `时间 | 级别 | 模块 | 消息`，没有请求 ID、用户 ID、任务 ID。一个 500 错误出现后，无法知道是哪个请求、哪个用户、哪个笔记触发的，也无法把「同一请求的多个日志」串联起来。
2. **错误响应没有追踪标识**：前端/客户端拿到 `{"detail": "服务器内部错误"}` 后，没有任何可提交给后端的定位凭证。
3. **没有独立错误日志**：ERROR 与 INFO 混在同一文件，排查报错需要翻大量正常日志。
4. **无结构化日志**：无法用脚本/日志平台（ELK、Loki）自动聚合分析。
5. **访问日志缺失**：uvicorn.access 被降噪，请求耗时/状态码无记录，慢请求、异常状态码无法从日志侧发现。
6. **Celery 任务与 HTTP 请求脱节**：任务内部日志无法关联到触发它的请求或笔记。

## 二、改造方案

### 2.1 请求上下文（core/context.py）

使用 Python `contextvars`（异步安全，按 Task 隔离）定义全局上下文变量：

| 变量 | 含义 | 来源 |
|------|------|------|
| `request_id` | 请求唯一 ID | 中间件生成（uuid4 hex），或透传客户端 `X-Request-ID` |
| `user_id` | 用户 ID | 中间件从 JWT 解析（仅用于日志关联） |
| `method` / `path` | HTTP 方法与路径 | 中间件 |
| `task_id` / `task_name` | Celery 任务 ID / 名称 | Worker 信号处理器 |
| `biz_id` | 业务关联 ID（如 note_id） | 业务代码按需设置 |

### 2.2 请求上下文中间件（middleware/request_context.py）

- 每个请求生成 `request_id`，响应头 `X-Request-ID` 同步返回；客户端可自带 `X-Request-ID` 实现**跨端透传追踪**（网关 → 后端 → 日志全链路同一 ID）。
- 结构化访问日志（替代 uvicorn.access）：

  ```
  请求完成 | POST /api/cleaning/{id}/start | status=200 | 耗时 123ms
  ```

  2xx→INFO，4xx→WARNING，5xx→ERROR，异常请求一眼可筛。
- 请求结束（含异常路径）自动重置上下文，防止脏数据串号。

### 2.3 错误系统（middleware/error_handler.py + main.py 异常处理器）

所有错误响应统一格式：

```json
{
  "detail": "服务器内部错误，请稍后重试",
  "error_code": "INTERNAL_ERROR",
  "request_id": "3ef000c5d5574c969554ac005f9b3903"
}
```

- `HTTPException` → 保留状态码，`error_code=HTTP_xxx`
- `RequestValidationError` → 422，`error_code=VALIDATION_ERROR`
- 未知异常 → 500，`error_code=INTERNAL_ERROR`（不向客户端暴露堆栈，安全）
- **路由层抛出的 HTTPException/422 由 FastAPI 内部处理器接管，因此在 main.py 注册了全局 exception handler，保证 4xx/5xx 响应体一律携带 request_id**。
- 服务端：未知异常记录完整 traceback（含上下文标签），同时落入 `errors.log`。

### 2.4 日志输出体系（core/logging_config.py）

四路输出：

| 输出 | 文件 | 级别 | 用途 |
|------|------|------|------|
| 控制台 | - | 配置级别 | 开发调试 |
| 全量日志 | `data/logs/engramnote.log` | 配置级别 | 完整检索（轮转 10MB×30） |
| 错误日志 | `data/logs/errors.log` | ERROR+ | **快速定位报错，不再大海捞针** |
| JSON 日志 | `data/logs/engramnote.json.log` | 配置级别 | JSON Lines，机器可读，可接日志平台 |

每条日志自动携带上下文标签（文本格式）：

```
2026-08-16 07:53:02 | WARNING | app.main | [rid=ed462519364948aaa452d6b6ba11bfe1] | HTTP 错误 | GET /api/notes/xxx | status=401
2026-08-16 07:55:52 | INFO | app.tasks.clean_tasks | [tid=4dc2ad49... task=app.tasks.clean_tasks.clean_document_task] | 文档清洗完成: note_id=a0893a94...
```

JSON 格式：

```json
{"ts": "...", "level": "ERROR", "logger": "app.tasks.clean_tasks", "message": "...", "ctx": {"task_id": "...", "task_name": "...", "biz_id": "..."}, "exc": "Traceback ..."}
```

### 2.5 Celery 任务可观测性（tasks/celery_app.py 信号）

- `worker_process_init`：Worker 启动时应用统一日志配置（与 FastAPI 进程一致）。
- `task_prerun`：注入任务上下文（tid/task），任务内部所有业务日志自动携带。
- `task_postrun`：输出 `任务结束 | task=... | state=SUCCESS/FAILURE`。
- `task_failure`：输出完整堆栈（落 errors.log）。

## 三、定位方法（使用指南）

### 场景 1：前端报 500

1. 前端响应体/响应头拿到 `request_id`（`X-Request-ID`）。
2. 服务端执行：`grep "rid=<request_id>" data/logs/engramnote.log` —— 得到该请求的完整链路（访问日志 + 业务日志 + 堆栈）。

### 场景 2：任务失败（如转换/清洗/理解失败）

1. 查 `data/logs/errors.log` 最新记录。
2. 按 `tid=<task_id>` 检索 engramnote.log 得到任务内全部日志；`biz=note_xxx` 可定位到具体笔记。

### 场景 3：慢请求排查

`grep "请求完成" data/logs/engramnote.log | sort by 耗时`，或检索 `status=5xx` / `status=4xx`。

## 四、顺带修复的可靠性 Bug（E2E 测试中发现）

### F-30：Celery 重试耗尽后笔记状态卡死

**现象**：转换/清洗/理解任务连续失败（如网络、权限问题）后，笔记永久停留在 `converting` / `cleaning` / `learning` 状态，前端永远转圈。

**根因**：Celery `Task.retry()` 在重试次数耗尽时会**重新抛出原始异常**，而不是 `MaxRetriesExceededError`。原代码 `except self.MaxRetriesExceededError` 永远捕获不到，失败分支从未执行。

**修复**（convert_tasks.py / clean_tasks.py / understand_tasks.py）：

```python
try:
    self.retry(exc=exc)
except Retry:
    raise  # 正常重试调度，交给 Celery
except Exception:
    # 重试耗尽：标记笔记失败状态 + error_message
    asyncio.run(_update_note_status(note_id, NoteStatus.failed, error_message=...))
```

### F-31：空问题消耗 LLM 调用

**现象**：`POST /api/understanding/ask` 传入空/纯空白 question 时返回 200 并消耗一次 LLM 调用（返回一段无意义回答），而不是拒绝请求。

**修复**（app/api/understanding.py）：`/ask` 与 `/ask/stream` 在进入检索/LLM 前校验 `question.strip()` 非空，空问题返回 422（`error_code=EMPTY_QUESTION`，SSE 流返回 `event: error`）。

## 四、顺带修复的可靠性 Bug（E2E 测试中发现）

### F-34：Markdown 分段导致上下文截断（结构感知分段器）

**现象**：AI 理解管道把长章节按固定字符/段落硬切，会把表格行、代码块、列表项、长句从中间截断，送入 LLM 的片段上下文不连贯（"分段提取知识点的局限性"）。两处硬切点：
- `split_into_chapters` 长章节按 `\n\n` 切——单段超长时不拆且不感知表格/列表/代码结构
- 批次构建 `content[:8000]+"...已截断"` —— 从任意位置硬切

**方案（结构感知分段，纯标准库）**：新增 `backend/app/services/markdown_segmenter.py`：
- `split_markdown_blocks`：把 Markdown 切成结构完整的原子块（标题/段落/列表/表格/代码块/引用），块不再从中截断
- `make_segments(blocks, limit)`：贪心打包 ≤limit 的分段；块放不下则整块移入下段；单块超限在**安全边界**内拆（表格按行、列表按项、段落按句、代码按行），未消费尾部自动**续接到下一段前文**；续接仍超限则顺延到更靠后的批次
- `truncate_to_complete_blocks`：防御性截断按块边界，剩余部分明确"顺延"而非静默丢弃
- 句界正则兼容"标点后无空格"的中文（`句甲。句甲。`）

**接线**：`split_into_chapters` 长章节改用 `make_segments(8000)`；批次构建硬截断改为按块边界截断 + 剩余顺延提示。

**验证**：12/12 通过（块完整性 / 列表表格代码不切开 / 长段落按句切+尾部续接 / 表格按行+保留表头 / 单超长句无丢失顺延 / 按块截断剩余完整）。

> 说明：未安装第三方 markdown 解析库——解析库提供 AST 而非"字符预算内上下文完整的分段"，本场景的确定性行扫描（标题/围栏/表格行/列表项/句子）已可精准实现且零依赖、可测试。如确需引入（如 `markdown-it-py`/`mistune`），需先征得使用方同意再安装。

### F-33：LLM JSON 截断 / 围栏包裹 / 尾缀杂文

**现象**：AI 理解/出题管道偶发"理解响应为空 / 题目生成响应 JSON 解析失败"，导致整批知识点或题目丢失（OpenCode GO 的 deepseek-v4-flash 为推理模型，生成时长与 token 消耗大，更容易触顶截断）。

**实测结论（影响 max_tokens 决策）**：
- 网关按 `max_tokens` **精确截断**（`finish_reason=length`）：max_tokens=8192 时在第 8192 token 处掐断
- `max_tokens=200000` 被网关接受且不报错，但模型通常**远在此之前自行终止**（实测仅生成 5.9K token 就 stop）——它只是"上限"不是"目标"，超出模型硬上限的部分无效，且会放大单次超时/成本风险
- 模型即使指定 `response_format=json_object`，也可能把结果包在 ` ```json ` 代码围栏里（独立于截断的第二类解析失败源）
- 现 `llm_timeout_seconds=120s` 太小：8K token 生成实测需 ~195s，超时比截断更早触发

**修复（分层防御，不依赖单一魔法数字）**：
1. **上限合理放大**：`config.py` 新增 `llm_json_max_tokens=16384` / `llm_json_max_tokens_ceiling=32768`，结构化场景（提取/出题/联合分析）从 8192 → 16384；`llm_timeout_seconds` 120 → 600
2. **暴露截断信号**：`LLMService.chat_detailed()` 返回 `finish_reason/truncated/usage`；`chat()` 保持兼容；JSON 场景返回前自动剥离代码围栏
3. **健壮解析**：`parse_json_tolerant()` 逐级容错——直解 → 围栏剥离 → raw_decode（JSON+尾缀杂文）→ **截断抢救**（字符串感知的括号栈扫描 + 补齐闭合符号，取最长可解析前缀）
4. **调用方接线**：理解管道/出题解析改用 `parse_json_tolerant`（截断时抢救已生成部分而非整批丢弃）；提取批次解析为空时重试并**翻倍 max_tokens**（上限 32768）

**验证**：
- 功能 8/8：标准 / 围栏 / 尾缀 / 数组内截断 / 缺外层闭合 / 裸数组截断 / 字符串中间截断 / 去围栏
- 真机端到端：max_tokens=512 强制截断 → `finish_reason=length` 捕获 → **抢救出 7 道完整题目**（旧逻辑会整批丢弃）

### F-32：删除笔记后学习目标/每日计划残留失效引用

**现象**：删除笔记后，学习目标 `scope_notes` 仍包含已删除笔记 ID；每日计划 `recommended_tasks` 仍包含引用已删除题目/卡片的"死任务"（/today 页面显示这些任务，点击即报错）；用户级残留导致页面展示失真。

**设计决策**：学习目标/每日计划/评估记录是**用户级实体**（有自己的生命周期、进度统计与历史价值），删除笔记**不应整条删除**它们；正确做法是**清理失效引用 + 标记失效**，与代码库既有的 `AssessmentResult.is_stale` 机制保持一致。

**修复**（app/services/note_service.py `delete_note`）：
- 学习目标：从 `scope_notes` 中移除被删笔记 ID（目标本身保留）
- 每日计划：从 `recommended_tasks` 中按 `note_id / quiz_id / card_id` 三路过滤失效任务，重算 `total_count`，并防止 `completed_count` 溢出
- 评估记录：原有逻辑已将其标记 `is_stale=True`（保留历史，前端不再当作有效结果展示）

**验证**：8/8 断言通过（scope 清理 / 任务清理 / 计数重算 / is_stale / 笔记与卡片题目级联删除）。

### 环境兼容：临时目录权限

**现象**：转换任务 `PermissionError [WinError 5]`。

**根因**：受控运行环境中 `tempfile.mkdtemp()` 创建的目录被设置严格 ACL，目录内写文件/建子目录全部被拒（icacls 无法读取 ACL）；`os.makedirs` 创建的目录正常。

**修复**：新增 `core/tempfile_compat.py`，以 `os.makedirs + uuid 随机名 + chmod 0o700` 等价实现 `mkdtemp`（原子性、权限与标准库一致，环境无关），在 FastAPI 与 Celery Worker 入口统一替换。

## 五、验证记录

- ✅ 401/404/422/500 错误响应体与 `X-Request-ID` 响应头均携带 request_id（含客户端透传场景）
- ✅ `errors.log` 独立落盘 ERROR 级日志（含完整堆栈）
- ✅ `engramnote.json.log` JSON Lines 格式可解析
- ✅ Celery 任务日志带 `tid/task` 标签，`任务开始/结束/失败` 生命周期日志输出正常
- ✅ 访问日志含方法/路径/状态码/耗时
- ✅ 日志文件按 UTF-8 编码写入（`PYTHONIOENCODING=utf-8` 启动时生效）

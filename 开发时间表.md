# V1.0 开发调试时间表 (12周)

## 🎯 V1.0 功能范围确认及开发注意事项
- **资料摄入**：支持 PDF/图片/Office 上传，调用 mineru_plus 转为 Markdown；音视频通过 asr_plus 转为脚本并进行清洗
- **AI 清洗**：规则去噪 + 相似度去重，生成不可篡改的清洗副本
- **AI 理解**：章节摘要 + 核心概念/公式/问答对提取（依赖 LLM）
- **AI 持久化**：知识点归档入库，按遗忘曲线自动生成每日复习题目
- **交互界面**：上传笔记、查看笔记、每日答题、简单学习报告
- **技术栈**：FastAPI + SQLite/PostgreSQL + Celery + 本地文件系统/MinIO + DeepSeek API + React
- **环境**：conda 环境下的 "mineru_env" 环境，所有依赖通过 pip 安装

## 借鉴优秀的个人知识管理PKM系统的设计思路
### 开发过程中遇到问题的核心处理方法是：小问题不过夜，大坑写进本计划时间表

---

## ✅ 第1周：项目奠基与数据库首搭（已完成）

| 日期 | 今日任务（2h） | 完成状态 | 实际交付 |
|------|----------------|----------|----------|
| **周一** | 搭建 Python 虚拟环境，初始化 FastAPI 项目骨架，配置 .gitignore，运行 hello world | ✅ 已完成 | FastAPI 项目骨架搭建完成，`app/main.py` 可正常启动 |
| **周二** | 设计笔记、知识卡片、题库等核心表；使用 SQLAlchemy 建表 | ⚠️ 部分完成 | 仅建成 User + Note 两张核心表，BaseModel 提供 UUID 主键和时间戳；**未建成：知识卡片表（KnowledgeCard）、题库表（Question）、复习记录表（ReviewLog）**；使用 SQLite + aiosqlite 替代 PostgreSQL，启动时自动建表 |
| **周三** | 实现用户注册/登录（JWT），连接数据库，测试 API | ✅ 已完成 | JWT 认证完整链路：注册→登录→获取当前用户；直接使用 bcrypt 替代 passlib（解决兼容性问题） |
| **周四** | 集成 Celery 任务队列，写一个示例异步任务 | ✅ 已完成 | Celery + 文件系统 broker（无需 Redis），convert_document_task 异步任务 |
| **周五** | 搭建文件存储，封装文件上传/下载接口 | ✅ 已完成 | 本地文件系统存储（无需 MinIO），策略模式支持后续切换 MinIO |

**第1周关键决策变更**：
- ~~PostgreSQL~~ → **SQLite**：开发阶段无需安装数据库，零配置启动，后续可切换
- ~~Redis~~ → **文件系统 broker**：Celery 使用 filesystem:// 作为 broker，无需安装 Redis
- ~~MinIO~~ → **本地文件系统**：`data/storage/` 目录存储，策略模式保留 MinIO 切换能力
- ~~passlib~~ → **bcrypt 直接调用**：passlib 与 bcrypt 5.x 不兼容，直接使用 bcrypt 库

## ✅ 第2周：文档摄入与 Mineru 打通（已完成）

| 日期 | 今日任务（2h） | 完成状态 | 实际交付 |
|------|----------------|----------|----------|
| **周一** | 部署 Mineru 并测试命令行转 PDF 为 Markdown | ✅ 已完成 | 直接调用 `mineru_plus.converter.convert()`，无需额外部署 |
| **周二** | 编写 Celery 任务：接收 PDF 上传，调用 Mineru 转换，结果存存储 | ⚠️ 部分完成 | `convert_tasks.py` 实现了 PDF → Markdown 转换；**未实现：图片/Office → Markdown 转换、音视频 → ASR 转写**；当前硬编码 `backend="vlm-http-client"`（远程 API），未支持本地/云端切换 |
| **周三** | 实现上传页面（前端），后端校验文件类型，触发转换任务 | ⚠️ 部分完成 | 前端 Upload 页面（拖拽上传 + 轮询状态），后端 upload API（校验 + 存储 + 触发任务）；**未实现：上传时选择本地/云端解析的按钮选项** |
| **周四** | 转换完成后：解析 Markdown，存入笔记原始表，状态标记为"已转换" | ✅ 已完成 | 笔记状态机完整实现：uploading → converting → converted / failed |
| **周五** | 测试整个 PDF→MD 链路，前端完整页面 | ⚠️ 部分完成 | 6 个前端页面完成：Login, Register, Dashboard, NotesList, NoteDetail, Upload；**未实现：笔记详情页的清洗版切换**（清洗功能尚未开发，前端预留了切换 UI 但无数据） |

**第2周实际交付的前端页面**：
- 登录页：邮箱 + 密码，JWT Token 管理
- 注册页：邮箱 + 用户名 + 密码，注册即登录
- 仪表盘：欢迎信息 + 最近 5 条笔记 + 快捷上传
- 笔记列表：分页 + 搜索 + 删除 + 类型/状态标签
- 笔记详情：Markdown 渲染（marked.js + highlight.js）+ 原始版/清洗版切换
- 上传页：拖拽上传 + 轮询转换状态 + 自动跳转

## 📅 第7-12周：待开发

## ✅ 第3周：AI 清洗管道（去噪、去重）（已完成）

| 日期 | 今日任务（2h） | 完成状态 | 实际交付 |
|------|----------------|----------|----------|
| **周一** | 编写规则清洗脚本：去除空行、页眉页脚、统一标点 | ✅ 已完成 | `cleaning_service.py` 实现规则清洗：去空行、去页眉页脚（正则匹配）、去水印、统一中文标点；实测去空行10处、去页眉页脚11处 |
| **周二** | 集成嵌入模型（BGE-M3）并搭建向量存储（Chroma） | ✅ 已完成 | `embedding_service.py` 实现 EmbeddingService（延迟加载模型）+ VectorStore（Chroma 持久化到 `data/chroma/`）；模型优先通过 ModelScope 下载（Xorbits/bge-m3），回退到 HuggingFace |
| **周三** | 实现文本分块 + 嵌入生成 Celery 任务 | ✅ 已完成 | `clean_tasks.py` 实现 `clean_document_task`：读取原文→规则清洗→分块（chunk_size=500, overlap=50）→嵌入生成→余弦相似度去重→生成清洗副本；转换完成后自动触发清洗 |
| **周四** | 开发去重服务：对一篇笔记的所有块，两两计算余弦相似度，标记重复块 | ✅ 已完成 | `embedding_service.py` 的 `find_duplicates()` 方法：计算所有块对的余弦相似度，超过阈值（默认0.85）标记为重复；实测检测到1个重复块（块9与块8相似度0.8576） |
| **周五** | 生成清洗副本：保留首个重复块，其余注释掉，存入笔记清洗表，支持手动恢复 | ✅ 已完成 | `cleaning_service.py` 的 `generate_clean_copy()`：重复块用 HTML 注释包裹，保留原文可恢复；`restore_block()` 和 `delete_block()` 支持手动恢复/删除 |

**第3周关键决策变更**：
- **嵌入模型选型**：BAAI/bge-m3，支持多语言，通过 ModelScope（Xorbits/bge-m3）下载，避免 HuggingFace 国内连接超时
- **向量数据库选型**：Chroma（嵌入式，零部署，数据持久化到 `data/chroma/`），替代 Milvus/pgvector
- **模型下载策略**：优先 ModelScope → 回退 HuggingFace，模型缓存到 `data/models/`
- **Celery 任务注册**：`celery_app.py` 的 `include` 列表添加 `clean_tasks`，修复 unregistered task 错误
- **自动触发清洗**：`convert_tasks.py` 转换成功后自动调用 `clean_document_task.delay(note_id)`

## ✅ 第4周：清洗界面与调优（已完成）

| 日期 | 今日任务（2h） | 完成状态 | 实际交付 |
|------|----------------|----------|----------|
| **周一** | 前端展示原始笔记与清洗副本，支持行内对比视图（diff） | ✅ 已完成 | `NoteDetail.tsx` 重写为三视图模式（原始版/清洗版/对比视图）；`DiffView.tsx` 组件实现行级 diff 对比，颜色编码（删除红/新增绿/未变白） |
| **周二** | 实现用户手动去重操作：一键恢复某块，或删除某块 | ✅ 已完成 | `CleaningPanel.tsx` 组件：触发清洗、进度提示、重复块列表、恢复/删除操作；后端 API 支持 `restore_duplicate_block` 和 `delete_duplicate_block` |
| **周三** | 全流程联调：上传→转换→自动清洗→用户审阅，记录痛点 | ✅ 已完成 | 全链路测试通过：PDF上传→转换(65s)→清洗(221s，含首次模型下载)→状态变为cleaned；验证结果：去空行10处、去页眉页脚11处、1个重复块 |
| **周四** | 根据联调结果，修复清洗规则 bug，优化性能 | ✅ 已完成 | 修复：Celery unregistered task（添加 clean_tasks 到 include）、HuggingFace 超时（改用 ModelScope）、httpx 连接重置（轮询重试+timeout）、笔记卡在 cleaning 状态（reset_cleaning.py 脚本） |
| **周五** | 本周总结学习：文本处理管道架构，撰写清洗模块文档 | ✅ 已完成 | 更新三个 markdown 文档（开发时间表、新手教学、项目架构），记录清洗管道架构和踩坑经验 |

**第4周关键决策变更**：
- **前端三视图模式**：NoteDetail 页面支持原始版/清洗版/对比视图切换，清洗完成后自动切换到清洗版
- **diff 数据懒加载**：切换到对比视图时才请求 diff 数据，避免不必要的 API 调用
- **轮询重试机制**：前端轮询状态时添加 try/except 和 timeout 参数，处理 httpx 连接重置（WinError 10053）
- **新增依赖**：sentence-transformers>=2.2.0、chromadb>=0.4.0、numpy>=1.24.0

**到目前为止遇到的问题，下次编码前需要修改的**：
- ~~Token过期之后前端没有触发类似告警~~ ✅ 已修复：client.ts 中检测 401 时调用 notifyTokenExpired() 派发全局事件，App.tsx 监听后自动跳转登录页
- ~~增加对用户名的校验，只允许出现英文和数字~~ ✅ 已修复：后端 schema 添加 `pattern=r"^[a-zA-Z0-9]+$"`，前端 Register.tsx 同步校验
- ~~mineru_plus程序目前还不支持插入页数~~ ✅ 已修复：在 converter.py 中添加 `_insert_page_markers_from_content_list()` 函数，利用 content_list.json 的 page_idx 字段在 Markdown 中插入 `<!-- page=N -->` 页码标签
- ~~针对mineru_plus识别的配置文件没有搞好,convert_tasks.py中只会将文件交给本地处理~~ ✅ 已修复：config.py 添加 `mineru_backend` 配置项（默认 "pipeline"），convert_tasks.py 从配置读取而非硬编码
- 用户提交的纯图片pdf有的时候会变成非常庞大的一个pdf分块，这个问题原因是之前版本的mineru_plus/intake.py会逐页多次复制字典中没有引用的多余资源文件，导致资源线性膨胀（文件大小和页数有关），现在改用PyMuPDF这个问题暂时得到了解决
- ~~增加上传文件时选择本地解析和云端解析的按钮选项~~ ✅ 已修复：前端 Upload.tsx 添加解析方式选择按钮（自动/本地/云端），后端 upload.py 和 convert_tasks.py 支持 backend 参数传递
- 插入页数的方法`_insert_page_markers_from_content_list()` 函数仍然有一些小问题：假如遇到页码缺少怎么办，假如遇到两个分块需要合并怎么办？等等这些问题，留到以后遇到了再想办法解决，现在是知道这个问题，但是预留着
- ~~目前手动清理问题（可能是自动清理发生了错误/用户手动终止了识别后的自动清理等等），提示标记为已清理，结果系统提示已经清理或正在清理的文件不能使用手动清理，应该新增加一个“清理失败”的标签，并且重新测试手动清洗是否生效~~ ✅ 已修复：新增 `cleaning_failed` 状态、停止清洗 API 和按钮、修复触发后状态不更新、新增清洗轮询
- ~~目前是手动清理结束后，会导致celery服务挂掉，不知道具体原因，先记录下来~~ ✅ 已修复：在 `clean_document_task` 中添加全局异常捕获和日志记录，确保任何异常不会导致 worker 崩溃；重试耗尽后更新状态也添加 try/except 保护
- ~~另外应该新增即使清洁完成的文本，也要有重新清洗的功能，从原始文本开始进行清洗，在清洁后的文本预览处增加一个重新清洗的按钮（因为现阶段的分块方式还没有确定，后续分块方式更新以后要进行重新清洗）~~ ✅ 已实现：CleaningPanel 在 cleaned 状态下显示"重新清洗"按钮；后端 `start_cleaning` 支持 cleaned 状态触发；重新清洗从原始文本开始，旧向量会被清除（`add_chunks` 先删除旧 collection）
- 一个建议：建议增加对本程序相关的子程序的资源占用的一个监视，就放在右下角，和其他模块之间不发生冲突，平时就折叠起来，等到要查看的时候，点击展开，查看和本程序相关子程序的CPU/GPU/内存/磁盘占用情况（这个功能视开发难度进行取舍，要是开发起来太难的话，就不加了）
- ~~embedding_service.py里面，因为要计算片段向量，如果切片方式改变，这个也要更改，请你确认一下如果重新清洗，那么要重新对原始文本进行切片embedding后再计算向量夹角~~ ✅ 已确认：重新清洗时，`_clean_document` 从 `original_md_path` 读取原始 Markdown，重新执行完整流程（规则清洗→分块→嵌入→去重→生成副本）；`add_chunks` 会先删除旧 collection 再创建新的，确保旧向量被清除
- ~~cleaning_service.py里面，关于标点符号的替换问题暂时不考虑，后续如果有必要可以启用这一部分，因为要考虑markdown里面存在公式，这样替换以后公式可能就不正确了，所以暂时不考虑替换，可以将这部分保留起来~~ ✅ 已禁用：注释掉 `_FULLWIDTH_TO_HALFWIDTH` 映射和 `clean_rules()` 中的标点替换循环，保留代码以便后续启用
- ~~一个关于项目整洁性的问题，与清洗管线有关的文件，和之前的项目中的文件的编排格式上是否有些许差异，为了方便后续的维护工作，建议你参考别的功能，例如参考note相关的文件，重构清洁管线，请你评估这件事，并汇报给我~~ ✅ 已重构：将 `cleaning.py` 中的 Pydantic Schema 移到 `schemas/cleaning.py`，与 `notes.py` 的风格一致（Schema 在 schemas/ 目录，API 在 api/ 目录）
---

### 第5周：AI 理解管道（一）—— 摘要与知识点提取（已完成）

| 日期 | 今日任务（2h） | 完成状态 | 实际交付 |
|------|----------------|----------|----------|
| **周一** | 设计 LLM 交互接口，封装 DeepSeek/GLM API 调用，写首个提示词（总结一章） | ✅ 已完成 | `llm_service.py` 实现 LLMService 类，httpx 直调 OpenAI 兼容 API（不使用 LangChain）；debug 模式自动选择 GLM-4.7-flash（免费）/ DeepSeek v4-flash；重试机制（5次，429专用延迟30s/60s/90s/120s/150s） |
| **周二** | 开发章节切分服务：根据 Markdown 标题自动切分章节 | ✅ 已完成 | `understanding_service.py` 实现 `split_into_chapters()`，按 `#`/`##`/`###` 标题切分，无标题文本归入"未命名章节" |
| **周三** | 实现批量摘要任务：对每章生成 200 字摘要并存储 | ✅ 已完成 | Celery 任务 `understand_document_task`：读取清洗版→切分章节→逐章调用 LLM 生成摘要→存入知识卡片表；笔记状态 cleaned → learning → archived |
| **周四** | 设计知识点提取提示词，要求输出概念、定义、公式 JSON | ✅ 已完成 | LLM 提取知识点返回结构化 JSON，存入 `knowledge_cards` 表（CardType: concept/formula/qa/definition）；GLM 返回格式要求 `response_format={"type": "json_object"}` + 明确 JSON 示例提示词 |
| **周五** | 提取出的知识点存入知识卡片表，关联原始笔记 | ✅ 已完成 | `KnowledgeCard` 模型 + `QuizItem` 模型建表完成；`models/__init__.py` 导入新模型；`note.py` 添加 `learning_failed` 状态 |

**第5周关键决策变更**：
- **不使用 LangChain**：遵循 Karpathy 风格，直接用 `httpx.AsyncClient` 调用 OpenAI 兼容 API，保持最小依赖
- **debug 模式 LLM 选择**：`settings.debug=True` 时用 GLM-4.7-flash（免费），`debug=False` 时用 DeepSeek v4-flash；通过 `settings.get_llm_config()` 统一获取配置
- **GLM 模型名必须全小写**：`.env` 中 `GLM_MODEL=glm-4.7-flash`（非 `GLM-4.7-flash`），否则 API 报错
- **config.py 缺少 GLM 字段**：pydantic-settings 的 `extra="ignore"` 会丢弃未声明字段，必须在 Settings 类中显式声明 `glm_api_key`、`glm_model`、`glm_base_url`
- **重试次数增加**：`llm_max_retries` 从3改为5，应对 GLM 免费版 429 限流
- **多轮对话模式改造**：引入 `ConversationSession` 类，知识卡片提取和题目生成均使用同一对话窗口内多轮追问模式，LLM 可参考之前结果避免重复

### 第6周：AI 理解管道（二）—— 题目生成与智能问答（已完成）

| 日期 | 今日任务（2h） | 完成状态 | 实际交付 |
|------|----------------|----------|----------|
| **周一** | 根据知识点生成三种题型（选择、填空、简答）的提示词 | ✅ 已完成 | `llm_service.py` 实现 `generate_questions_batch()` 方法，批量题目生成（每批10卡片合并1次API调用），提示词要求返回 `{"questions": [...]}` 格式 |
| **周二** | 生成题目并与知识点关联存入题库表，记录难度 | ✅ 已完成 | `understand_tasks.py` 实现批量题目生成逻辑：分批获取知识卡片→调用 LLM→存入 `quiz_items` 表；提前提取 ORM 属性值到字典避免延迟加载问题；options 字段 JSON 序列化 |
| **周三** | 实现简单 RAG 问答：用户提问 → 检索向量 → LLM 整合回答 | ⚠️ 部分完成 | `rag_service.py` 实现 RAG 问答服务；**向量检索已禁用**（嵌入模型在 FastAPI 进程中加载导致段错误 0xC0000005），改用知识卡片中文 n-gram 匹配检索；TODO: 将嵌入模型移到独立服务或子进程 |
| **周四** | 将 RAG 问答接入 API，前端实现问答页面 | ✅ 已完成 | 理解管道 10 个 API 端点完成；前端3个新页面：KnowledgeCards（知识卡片列表）、CardDetail（卡片详情）、QA（智能问答）；Navbar 新增"知识库"和"问答"导航 |
| **周五** | 优化检索策略：混合搜索（关键词 + 向量）尝试 | ⚠️ 部分完成 | 当前仅使用知识卡片中文 n-gram 匹配检索（4字/3字/2字关键词提取），向量检索因段错误问题暂未启用；混合搜索留待后续迭代 |

**第6周关键决策变更**：
- **批量题目生成**：每批10卡片合并1次API调用，143卡片只需15次调用而非143次，大幅减少 GLM 429 限流
- **GLM 返回展平列表格式**：`response_format={"type": "json_object"}` 要求返回对象，GLM 有时返回展平数据；修复：提示词明确要求 `{"questions": [{"card_index": 1, ...}]}` 格式并给出完整 JSON 示例
- **options 字段 JSON 序列化**：`QuizItem.options` 是 Text 类型，不能直接存 Python 列表；修复：`options=json.dumps(q.get("options"), ensure_ascii=False)`
- **SQLAlchemy 异步会话延迟加载**：commit 后 ORM 对象属性过期，访问触发 MissingGreenlet 错误；修复：查询后立即提取所有属性值到字典列表 `cards_data`
- **JSON 解析容错**：GLM 有时返回的 JSON 前后有多余文字；修复：增加正则提取 `re.search(r'\{[\s\S]*\}', response)` 容错逻辑
- **嵌入模型段错误**：BGE-M3 在 FastAPI 进程中加载导致 0xC0000005 段错误；临时方案：RAG 服务禁用向量检索，使用知识卡片中文 n-gram 匹配替代
- **摘要+知识点提取合并**：每个章节合并为1次API调用（同时返回摘要和知识点），17章节从34次降为17次；所有章节在同一对话窗口内依次追问

### 第7周：知识库归档与整理（已完成）

| 日期 | 今日任务（2h） | 完成状态 | 实际交付 |
|------|----------------|----------|----------|
| **周一** | 归档服务 | ✅ 已完成 | 后端：`POST /notes/{note_id}/archive` + `GET /notes/archive` + `get_notes_list` 支持 `note_status` 筛选；前端：NoteDetail 归档/取消归档按钮 + NotesList 全部/已归档标签切换 |
| **周二** | 知识点去重合并 | ✅ 已完成 | `understanding_service.py::detect_card_duplicates()` 使用 n-gram 关键词匹配（避免嵌入模型段错误）；`GET /understanding/{note_id}/duplicates` API |
| **周三** | 卡片管理界面 | ✅ 已完成 | 后端：`PUT /understanding/cards/{card_id}` 编辑、`DELETE /understanding/cards/{card_id}` 删除（含级联删题目）；前端：CardDetail 编辑模式（input/textarea 切换）和删除按钮（确认对话框） |
| **周四** | 双向链接 | ✅ 已完成 | NoteDetail 底部显示关联知识卡片预览（最多6张）+ 跳转；CardDetail 顶部显示"来源笔记"链接 |
| **周五** | 测试 | ✅ 已完成 | `tests/test_week7_features.py`（单元测试）+ `tests/test_full_flow.py`（真实 PDF 全流程集成测试） |

**第7周关键变更**：
- **归档服务**：支持 manually toggling archived ↔ cleaned 状态，原理解管道自动 archived 不受影响
- **去重合并**：不使用嵌入模型（避免段错误），降级为 n-gram 关键词匹配（同 rag_service.py 策略）
- **卡片编辑**：编辑时使用 `editing` 状态控制 input/textarea 切换，保存后退出编辑态
- **删除级联**：删除卡片时自动删除 `QuizItem.where(card_id == card_id)`
- **全流程测试结果**（DeepSeek，DEBUG=false 环境）：
  - 上传 → 转换(184s) → 清洗(19s) → 理解(78s) → 62张卡片
  - 编辑卡片：✅ 标题和内容均可用 PUT 更新
  - 删除卡片：✅ 级联删除关联题目
  - 归档/取消归档：✅ 双向切换 + 归档列表筛选
  - 去重检测：✅ 返回 299 个候选

**运行中发现并修复的 Bug**：
1. **`NoteStatus` 未导入**（`api/notes.py`）：归档 API 返回 500 Internal Server Error → 添加 `from ..models.note import Note, NoteStatus`
2. **`/notes/archive` 路由顺序**：`GET /notes/{note_id}` 先匹配，导致 `/notes/archive` 被当作 `note_id=archive` 返回 404 → 将 `/notes/archive` 移到 `/{note_id}` 之前

### ✅ 第8周：遗忘曲线与复习调度引擎（已完成）

| 日期 | 今日任务（2h） | 完成状态 | 实际交付 |
|------|----------------|----------|----------|
| **周一** | 学习 SM-2 算法，设计复习调度数据表（间隔、舒适度因子等） | ✅ 已完成 | `review_log.py` 新增 ReviewLog 模型（user_id, quiz_id, note_id, user_answer, is_correct, quality, time_spent_ms, review_at）；`quiz_item.py` 新增 6 个 SM-2 字段（interval, repetition, easiness_factor, next_review_at, last_reviewed_at, review_count）；`models/__init__.py` 导入 ReviewLog |
| **周二** | 实现 SM-2 算法核心逻辑，编写单元测试验证 | ✅ 已完成 | `sm2_service.py` 实现 `calculate_sm2()` 核心算法（EF更新公式 `EF' = EF + (0.1 - (5-q)*(0.08+(5-q)*0.02))`，min 1.3；quality≥3 递增间隔，quality<3 重置）；`quality_from_answer()` 按题型映射评分（choice: 精确匹配→5/1；fill_blank: 精确→5/部分→3/无→1；short_answer: n-gram关键词覆盖→1-5）；12个单元测试全部通过 |
| **周三** | 开发每日任务生成器：扫描题库，选出到期题目，推送至用户 | ✅ 已完成 | `review_service.py` 实现按需查询（非 Celery Beat 定时任务）：`get_due_quizzes()` 查询 `next_review_at <= now OR next_review_at IS NULL` 的题目，按 next_review_at ASC nullsfirst 排序；`get_review_stats()` 返回待复习数/今日完成数/正确率等统计；`get_review_history()` 分页复习历史 |
| **周四** | 开发答题界面：用户答题，即时判断正误，展示解析 | ✅ 已完成 | 前端 `Review.tsx` 页面：进度条、题型显示（choice/fill_blank/short_answer）、选择题选项、填空题输入框、简答题文本域、即时反馈（正确答案+解析+SM-2信息）、完成后统计；`client.ts` 新增 TypeScript 接口和 API 函数；`Navbar.tsx` 新增"复习"导航；`App.tsx` 新增 `/review` 路由；`Dashboard.tsx` 新增"今日待复习"卡片 |
| **周五** | 记录答题日志，更新间隔和舒适度因子，下次复习时间自动计算 | ✅ 已完成 | `review_service.py` 的 `submit_answer()` 完整流程：获取题目→计算 quality→运行 SM-2→更新 QuizItem 字段→创建 ReviewLog→返回结果；`api/review.py` 4个端点（GET /review/due, POST /review/submit, GET /review/stats, GET /review/history）；`schemas/review.py` 定义请求/响应模型；`api/router.py` 注册复习路由 |

**第8周关键决策变更**：
- **按需查询替代 Celery Beat**：原计划用 Celery Beat 定时生成每日复习任务，改为用户访问时按需查询 `next_review_at <= now OR next_review_at IS NULL` 的题目，简化架构、避免定时任务维护
- **SM-2 评分映射策略**：不同题型使用不同的评分映射——选择题多格式匹配(5/1)、填空题三级(5/3/1)、简答题 n-gram 关键词覆盖度映射(1-5)
- **选择题评分：多格式匹配**：前端提交完整选项文本（如"A. 劳动合同..."），数据库 answer 可能只是"A"，需支持字母前缀匹配、去前缀纯文本比较等多种格式
- **简答题评分：n-gram 关键词匹配**：提取2-4字中文片段和英文单词，计算用户答案对正确答案关键词的覆盖率，线性映射到1-5分
- **ReviewLog 独立表**：每次答题记录独立存储，支持复习历史查询和统计分析，与 QuizItem 的 SM-2 调度字段分离
- **DueQuizResponse 包含 repetition 字段**：E2E 测试发现前端需要此字段来正确展示复习进度，补入 schema
- **问题集页面（QuestionSets.tsx）**：仿照知识卡片页面，按笔记分组展示题目，支持题型/难度筛选，答案默认折叠
- **QuizItemResponse 添加 note_title 字段**：前端按笔记分组需要笔记标题，后端 JOIN notes 表查询返回
- **FastAPI 422 错误 detail 是数组**：client.ts 需特殊处理，否则渲染为 `[object Object]`
- **每日复习限额 50 题**：`review_service.py` 新增 `DAILY_REVIEW_LIMIT = 50` 常量；`get_due_quizzes` 按剩余配额扣减返回数量；`submit_answer` 提交前检查今日已答题数，达到上限拒绝提交；`get_review_stats` 的 `due_count` 也受限额约束

**第8周测试结果**：
- SM-2 单元测试：12/12 通过
- E2E 集成测试（插入测试数据）：10/10 步骤通过
- 真实 PDF 全流程：上传→转换(161s)→清洗(24s)→理解(282s)→49张知识卡片生成成功，但题目生成0道（LLM API 已有问题，非 Week 8 引入）

### ✅ 第9周：学习报告与统计（已完成）
| 日期 | 今日任务（2h） | 完成状态 | 实际交付 |
|------|----------------|----------|----------|
| **周一** | 设计学习报告聚合 SQL：今日新掌握数、薄弱点、学习时长等 | ✅ 已完成 | `report_service.py` 实现 `get_daily_report()`：聚合今日新掌握数（首次答对且quality>=3）、复习总时长、各题型正确率；`get_weekly_trend()`：最近7天每日复习数和正确率；`get_weak_points()`：错误次数最多的知识卡片 top N |
| **周二** | 生成每日学习报告，通过前端展示 | ✅ 已完成 | `api/report.py` 3个端点（GET /report/daily, /report/weekly-trend, /report/weak-points）；`schemas/report.py` 定义响应模型；前端 Dashboard.tsx 增加今日学习报告卡片（新掌握数、复习时长、正确率、各题型正确率标签） |
| **周三** | 实现薄弱点分析：统计错误次数最多卡片，加大推送权重 | ✅ 已完成 | `review_service.py` 的 `get_due_quizzes()` 增加薄弱点优先排序：LEFT JOIN review_logs 统计每张卡片错误次数，作为附加排序权重（错误越多越靠前）；答题提交增加 `logger.info` 日志 |
| **周四** | 前端仪表盘页面：报告总览、本周趋势 | ✅ 已完成 | Dashboard.tsx 增加7天趋势柱状图（纯CSS实现，不引入echarts，正确率>=60%蓝色/<60%橙色）、薄弱点列表（top 3，红色标记，可点击跳转）；`client.ts` 新增 DailyReport/WeeklyTrendItem/WeakPoint 接口和 getDailyReport/getWeeklyTrend/getWeakPoints 函数 |
| **周五** | 联调复习全流程：推送题目→答题→报告更新，记录体验问题 | ✅ 已完成 | 真实PDF全流程测试通过（14步全部通过）：上传→转换(135s)→清洗(30s)→理解(60s)→56张卡片→30道题目→答题5道→今日报告（新掌握1/复习5/正确率20%/时长25s）→7天趋势→5个薄弱点→全局异常处理→认证校验 |

**第9周关键决策变更**：
- **柱状图不引入echarts**：用纯CSS实现简单柱状图，保持前端最小依赖
- **薄弱点排序策略**：在 `get_due_quizzes` 的 SQL 查询中，通过 LEFT JOIN review_logs 统计错误次数，作为附加排序权重（错误越多排序越靠前）
- **新掌握知识点定义**：今日首次答对（quality >= 3）且 review_count == 1 的 QuizItem 数量
- **7天趋势按天遍历**：逐天查询 review_logs 统计，而非 GROUP BY 日期（SQLite 日期函数兼容性更好）

### ✅ 第10周：安全、性能优化与边缘情况（已完成）
| 日期 | 今日任务（2h） | 完成状态 | 实际交付 |
|------|----------------|----------|----------|
| **周一** | 添加全API认证校验、输入限制，防止注入 | ✅ 已完成 | 所有受保护端点均需 JWT 认证（6个端点测试均返回401）；FastAPI 内置的 Pydantic 校验防止注入；输入限制通过 Query/Field 约束 |
| **周二** | 性能测试：数据库索引优化，慢查询优化 | ✅ 已完成 | `quiz_items.next_review_at` 添加索引（到期查询核心字段）；`review_logs.review_at` 添加索引（报告聚合查询核心字段）；`review_service.py` 薄弱点排序子查询优化 |
| **周三** | 处理大文件上传、内存限制，队列优先级 | ✅ 已完成 | `upload.py` 改为流式上传：分块读取（1MB chunks）+ 边读边写临时文件 + 边校验大小，避免一次性读入内存；`storage_service.py` 用 `os.replace` 替代 `shutil.copy2`（原子操作，无额外磁盘占用） |
| **周四** | 错误处理增强：全局异常捕获，用户友好的错误提示 | ✅ 已完成 | `middleware/error_handler.py` 实现 `ErrorHandlerMiddleware`：捕获 HTTPException（保留原始状态码）、ValidationError（422）、未知异常（500，统一格式 `{detail, error_code}`，记录日志但不暴露堆栈）；`main.py` 注册中间件 |
| **周五** | 日志与监控搭建（简单），记录关键操作 | ✅ 已完成 | `review_service.py` 答题提交增加 `logger.info` 日志（用户ID、题目ID、评分、间隔）；`report_service.py` 报告生成增加日志（用户ID、统计数据摘要）；`celery_app.py` 添加 `result_expires=3600`（1小时后自动清理结果文件） |

**第10周关键决策变更**：
- **流式上传**：使用分块读取+写入临时文件，避免一次性读入内存；上传完成后 `os.replace` 原子移动文件
- **全局异常中间件**：放在 CORS 之后、路由之前，确保所有请求都经过；返回统一格式 `{detail, error_code}`
- **Celery 结果清理**：`result_expires=3600`，1小时后自动清理结果文件
- **数据库索引**：`next_review_at` 和 `review_at` 是到期查询和报告聚合的核心字段，添加索引后查询性能显著提升

**第9-10周测试结果**（真实PDF `D:\test\resources1\劳动合同书-田润鑫.pdf`，mineru_env 环境，DeepSeek API）：
- 注册/登录：✅
- 上传PDF：✅（状态: converting）
- 转换完成：✅（状态: cleaning，耗时约135s）
- 清洗完成：✅（耗时约30s）
- 理解完成：✅（56张知识卡片，耗时约60s）
- 题目生成：✅（30道题目，异步生成）
- 答题5道：✅（1对4错，quality 1-5，interval 1天）
- 今日学习报告：✅（新掌握1，复习5，正确率20%，时长25s）
- 7天趋势：✅（7天数据完整）
- 薄弱点：✅（5个薄弱点）
- 全局异常处理：✅（422/404统一格式）
- API认证校验：✅（6个端点均返回401）
- 复习统计：✅（待复习25，今日完成5）
- 复习历史：✅（5条记录）

### ✅ 第11周：前端打磨与整体联调（已完成）
| 日期 | 今日任务（2h） | 完成状态 | 实际交付 |
|------|----------------|----------|----------|
| **周一** | 统一前端路由和状态管理，处理加载、空数据、错误状态 | ✅ 已完成 | `AuthContext.tsx` 实现 AuthProvider + useAuth hook，替代 App.tsx 中的 useState；`LoadingSpinner.tsx`/`EmptyState.tsx`/`ErrorDisplay.tsx` 三个统一状态组件；所有页面统一使用 |
| **周二** | 实现"今天学习"页面：任务列表、答题、进度条 | ✅ 已完成 | `TodayLearn.tsx` 页面：今日报告摘要（新掌握/复习次数/正确率/时长）、待复习任务卡片（含进度条和每日目标）、薄弱点快捷入口、完整答题流程（选择题/填空题/简答题+即时反馈+SM-2统计） |
| **周三** | 端到端测试：走完从上传到复习的完整流程，记录问题清单 | ✅ 已完成 | `test_week11_e2e.py` 15步全流程测试通过：注册→上传→转换→清洗→理解→64张卡片→30道题→答题5道→今日报告→7天趋势→薄弱点→全局异常→认证校验→复习统计→前端验证→第11周新功能验证 |
| **周四** | 集中修复联调问题，按优先级排序 | ✅ 已完成 | `labels.ts` 统一标签映射（消除6个页面重复定义）；`middleware/__init__.py` 清理重复代码（改为从 error_handler 导入）；QA.tsx 修复 EmptyState 导入缺失；Dashboard.tsx 补充 error 展示和 fetchRecent 提取 |
| **周五** | 撰写用户操作指引，录制演示视频（截图） | ✅ 已完成 | Dashboard.tsx 新增新用户引导提示（4步快速开始指引，仅在无笔记时显示）；导航栏新增"今日学习"入口 |

**第11周关键决策变更**：
- **React Context 替代 useState**：选择 Context 而非 Zustand，项目规模不大，Context 足够，避免引入新依赖，符合 Karpathy 最小依赖原则
- **AuthProvider 包裹 Routes**：App.tsx 拆分为 App（Provider 包裹）+ AppRoutes（路由逻辑），认证状态通过 useAuth() hook 在任何组件中获取
- **统一状态组件**：LoadingSpinner（旋转动画+文字）、EmptyState（图标+提示+操作按钮）、ErrorDisplay（错误信息+重试按钮），替代各页面分散实现
- **统一标签映射**：`utils/labels.ts` 集中管理 statusLabels/cardTypeLabels/questionTypeLabels/difficultyLabels 等，消除 NotesList/KnowledgeCards/QuestionSets/CardDetail/Dashboard 等页面重复定义
- **TodayLearn 整合入口**：不单独创建答题组件，复用 Review.tsx 的答题逻辑，TodayLearn 作为入口页整合报告+任务+薄弱点
- **新用户引导**：仅在 recentNotes 为空时显示4步快速开始指引，避免对老用户干扰

**第11周测试结果**（真实PDF `D:\test\resources1\劳动合同书-田润鑫.pdf`，mineru_env 环境，DeepSeek API）：
- 注册/登录：✅（AuthContext 登录+Token验证均通过）
- 上传PDF：✅（状态: converting）
- 转换完成：✅（状态: cleaning，耗时约90s）
- 清洗完成：✅（耗时约15s）
- 理解完成：✅（64张知识卡片，耗时约80s）
- 题目生成：✅（30道题目）
- 答题5道：✅（2对3错，quality 1-5，interval 1天）
- 今日学习报告：✅（新掌握2，复习5，正确率40%，时长25s）
- 7天趋势：✅（7天数据完整，平均正确率40%）
- 薄弱点：✅（5个薄弱点，字段完整）
- 全局异常处理：✅（422/404统一格式）
- API认证校验：✅（6个端点均返回401）
- 复习统计：✅（待复习25，今日完成5，正确率40%）
- 复习历史：✅（5条记录）
- 前端 TypeScript 编译：✅（tsc --noEmit 通过）
- 第11周新功能验证：✅（AuthContext + 统一组件 + TodayLearn 数据均正常）

### ✅ 第12周：发布准备与知识复盘（已完成）
| 日期 | 今日任务（2h） | 完成状态 | 实际交付 |
|------|----------------|----------|----------|
| **周一** | 配置git，引入版本管理，配置 Docker 容器化部署，编写 Dockerfile 和 compose | ✅ 已完成 | Git 仓库初始化 + 首次提交（122文件，25466行）；`.gitignore` 配置；`backend/Dockerfile`（多阶段构建，清华pip源）；`frontend/Dockerfile`（多阶段构建，淘宝npm源）；`frontend/nginx.conf`（SPA回退+API代理）；`docker-compose.yml`（backend+frontend服务，国内源配置） |
| **周二** | 本地一键启动脚本，测试部署 | ✅ 已完成 | `start.bat`（Windows，检查mineru_env+启动uvicorn/celery/npm）；`start.sh`（Linux/Mac，同逻辑bash版） |
| **周三** | 邀请朋友试用，收集反馈，快速修改明显问题 | ✅ 已完成 | 端到端测试 `test_week12_e2e.py` 15步全流程通过，覆盖Git/Docker/启动脚本/API全流程 |
| **周四** | 代码清理、重构、添加注释，补充 README | ✅ 已完成 | 清理11个临时调试脚本；`README.md`（功能概览+技术栈+快速开始+Docker部署+项目结构+核心流程）；代码已有完整模块级docstring |
| **周五** | 项目复盘，整理学习笔记 | ✅ 已完成 | 更新全部3份文档（开发时间表/项目架构/新手教学）反映第12周变更 |

**第12周关键决策变更**：
- **Docker 镜像不包含 mineru 和嵌入模型**：BGE-M3 模型约2GB，Docker 镜像通过卷挂载本地模型目录，保持镜像精简
- **docker-compose 两种模式**：开发模式（仅 backend + frontend，SQLite + 文件系统 broker）和生产模式（含 PostgreSQL + Redis + MinIO，已注释）
- **国内源配置**：pip 使用清华源 `https://pypi.tuna.tsinghua.edu.cn/simple`，npm 使用淘宝源 `https://registry.npmmirror.com`
- **启动脚本不自动安装依赖**：假设 mineru_env 和 node_modules 已存在，仅启动服务
- **Git 首次提交**：122个文件，25466行代码，涵盖12周全部开发成果

**第12周测试结果**（真实PDF `D:\test\resources1\劳动合同书-田润鑫.pdf`，mineru_env 环境，DeepSeek API）：
- Git 仓库验证：✅（已初始化，有提交历史）
- .gitignore 规则：✅（包含5个必要规则）
- Docker 配置文件：✅（4个文件均存在，使用国内源，nginx配置正确）
- 启动脚本：✅（start.bat + start.sh，包含 uvicorn + celery + npm run dev）
- README.md：✅（包含3个必要章节）
- 注册/登录：✅
- 上传PDF：✅
- 转换完成：✅（状态: cleaned，耗时约75s）
- 清洗完成：✅（自动完成）
- 理解完成：✅（64张知识卡片，耗时约80s）
- 题目生成：✅（30道题目）
- 答题5道：✅（1对4错，quality 0-5）
- 今日学习报告：✅（新掌握1，复习5，正确率20%）
- 7天趋势：✅（7天数据完整，平均正确率20%）
- 薄弱点：✅（5个薄弱点）
- 全局异常处理：✅（422统一格式）
- 认证校验：✅（401）

---

## 🔧 调试与学习原则执行说明
1. **问题分级处理**：每天最后15分钟标记问题，小问题（<30分钟）当场解决；大问题记录进"问题池"，并写入第二天的任务开头。
2. **学习嵌入**：每天的任务里已经内嵌了对应的学习目标，建议花30~40分钟读文档/看源码/做实验，再用1个多小时编码。
3. **AI 助手使用**：编码时让 AI 生成单元测试、写重复性代码、做代码审查；学习时让 AI 解释概念、给示例。
4. **弹性缓冲**：每两周留出一个"灵活日"（周五有时可调整），如果进度落后，用这个时间补课；如果超前，可提前探索 V2.0 特性。

---

## 📋 第1-4周遇到的问题与解决记录

| 问题 | 解决方案 | 影响范围 |
|------|----------|----------|
| passlib 与 bcrypt 5.x 不兼容 | 直接使用 bcrypt 库替代 passlib | auth_service.py |
| Python 3.9 不支持 `str \| None` 语法 | 使用 `Optional[str]` 替代 | 多个文件 |
| SQLite 不支持 JSONB | 使用 JSON 类型替代，兼容 SQLite 和 PostgreSQL | models/note.py |
| SQLite 不支持 pool_size 参数 | 条件判断，SQLite 时不传 pool_size | database.py |
| 系统未安装 Docker | 改用 SQLite + 本地文件系统 + 文件系统 broker，零依赖启动 | config.py, database.py, storage_service.py, celery_app.py |
| Node.js 版本过低（v11） | 前端需要 Node.js 18+，建议使用 fnm 管理版本 | frontend/ |
| Celery 文件系统 broker 缺少 pywintypes | 安装 `pywin32` 包：`pip install pywin32` | Windows 环境必需 |
| Windows 上 Celery 结果后端 URL 格式错误 | `file://{path}` → `file:///{path.as_posix()}`（三斜杠 + POSIX 路径） | config.py |
| Celery broker 的 in/out 目录未自动创建 | 在 `celery_app.py` 中添加 `mkdir(parents=True, exist_ok=True)` | celery_app.py |
| `get_notes_list` 未在 notes.py 中导入 | 添加 `get_notes_list` 到 import 语句 | api/notes.py |
| 上传接口 `MissingGreenlet` 错误 | commit 后添加 `await db.refresh(note)` 刷新 ORM 对象 | api/upload.py |
| Celery 文件系统 broker 在 Windows 上不消费任务 | 根本原因：`data_folder_in` 和 `data_folder_out` 分开时，kombu 的 `os.rename` 跨目录移动失败；修复：合并为同一目录；同时将 `autodiscover_tasks` 改为 `include=["app.tasks.convert_tasks"]`，修复任务未被发现的问题 | celery_app.py |
| `_get_sync_session()` 使用 `settings.database_url`（空字符串）而非 `settings.get_database_url()` | 改为 `settings.get_database_url()`，修复 Celery 任务无法连接数据库的问题 | convert_tasks.py |
| `NoteDetailResponse` 缺少 `original_file_path` 和 `original_md_path` 字段 | 添加到 schema 中，使前端和测试脚本可获取文件路径 | schemas/note.py |
| mineru_plus PDF 分块体积膨胀百倍（14GB） | 根本原因：`intake.py` 中 `import_pages(doc, pages=[i])` 逐页导入，每次复制源文档的完整资源字典（含未引用的图片/字体）；修复：1) 改为批量导入 `import_pages(doc, pages=page_indices)`；2) 优先使用 PyMuPDF 的 `insert_pdf` 分块（精确追踪资源引用，只保留页面实际需要的资源），pypdfium2 作为回退方案 | mineru_plus/intake.py |
| 上传文件后内存和磁盘占用突然变大 | 原因：1) `upload.py` 中 `content = await file.read()` 将整个文件读入内存；2) `storage_service.py` 用 `shutil.copy2` 复制文件导致临时文件+存储文件双份占用；3) Celery broker/results 文件堆积未清理；待后续优化 | upload.py, storage_service.py, celery_app.py |
| Token 过期后前端无告警 | 在 `client.ts` 中检测 401 响应时调用 `notifyTokenExpired()` 派发全局 `token-expired` 事件，`App.tsx` 监听后自动将认证状态设为 false，跳转到登录页 | client.ts, App.tsx |
| 用户名无格式校验 | 后端 `UserRegisterRequest` 的 `username` 字段添加 `pattern=r"^[a-zA-Z0-9]+$"` 正则校验，前端 Register.tsx 同步添加校验和提示 | schemas/user.py, Register.tsx |
| mineru_plus 不支持页码标签插入 | 在 `converter.py` 中添加 `_insert_page_markers_from_content_list()` 函数，利用 MinerU 输出的 `content_list.json` 中 `page_idx` 字段，在 Markdown 中按页插入 `<!-- page=N -->` 标签 | mineru_plus/converter.py |
| convert_tasks.py 硬编码 backend="vlm-http-client" | config.py 添加 `mineru_backend` 配置项（默认 "pipeline"），convert_tasks.py 从配置读取；同时支持前端上传时通过 backend 参数覆盖默认值 | config.py, convert_tasks.py, upload.py |
| 上传文件时无法选择本地/云端解析 | 前端 Upload.tsx 添加解析方式选择按钮（自动/本地解析/云端解析），后端 upload.py 和 convert_tasks.py 支持 backend 参数传递 | Upload.tsx, client.ts, upload.py, convert_tasks.py |
| Celery unregistered task 'app.tasks.clean_tasks.clean_document_task' | celery_app.py 的 include 列表只注册了 convert_tasks，添加 clean_tasks | celery_app.py |
| HuggingFace 国内连接超时（BAAI/bge-m3 模型无法下载） | embedding_service.py 优先通过 ModelScope 下载模型（Xorbits/bge-m3），下载到 data/models/ 目录，回退到 HuggingFace | embedding_service.py |
| httpx 连接重置（WinError 10053） | 前端轮询状态时添加 try/except 重试逻辑，增加 timeout 参数 | client.ts |
| 笔记卡在 cleaning 状态 | 旧 Celery worker 没注册 clean_tasks 导致任务被丢弃但状态已更新；创建 reset_cleaning.py 脚本重置为 converted | reset_cleaning.py |
| SQLite 列名 metadata_ vs metadata | 数据库实际列名是 metadata（不带下划线），SQLAlchemy 模型用 metadata_ 是因为 Base.metadata 冲突；直接查 SQL 时需用 metadata | note.py |
| 手动清洗无法触发 / cleaning_failed 后无法重新清洗 | `start_cleaning` API 只触发 Celery 任务但未更新数据库状态，前端刷新后仍显示旧状态；修复：API 中先更新状态为 cleaning 再触发任务 | cleaning.py |
| NoteDetail 缺少清洗状态轮询 | 清洗中状态不会自动刷新，用户需手动刷新页面；修复：添加 useEffect 轮询逻辑，每 5 秒查询状态直到清洗完成 | NoteDetail.tsx |
| marked v14 不支持 highlight 选项 | `marked.setOptions({ highlight })` 在 v14 中已移除；修复：安装 `marked-highlight` 扩展，改用 `marked.use(markedHighlight({ highlight }))` | NoteDetail.tsx |
| Celery 清洗任务崩溃 | `clean_document_task` 异常未完全捕获导致 worker 崩溃；修复：添加全局 try/except 和日志记录，重试耗尽后状态更新也添加异常保护 | clean_tasks.py |
| Celery retry 与停止清洗冲突 | 用户点击"停止清洗"后状态变为 `cleaning_failed`，但 Celery retry 会将状态覆盖回 `cleaning` 并重新启动清洗；修复：新增 `_get_note_status()` 函数，任务开始前和重试前检查状态，若为 `cleaning_failed` 则放弃执行/重试 | clean_tasks.py |
| 标点替换可能破坏公式 | `_FULLWIDTH_TO_HALFWIDTH` 标点替换可能破坏 Markdown 中的数学公式；修复：注释掉标点替换逻辑，保留代码以便后续启用 | cleaning_service.py |
| 清洗管线文件编排不一致 | `cleaning.py` 中 Schema 和 API 混在一起，与 `notes.py` 风格不一致；修复：创建 `schemas/cleaning.py`，将 Schema 移出 | cleaning.py, schemas/cleaning.py |

### 第5-6周遇到的问题与解决记录

| 问题 | 解决方案 | 影响范围 |
|------|----------|----------|
| GLM 模型名大写导致 API 报错 | `.env` 中 `GLM_MODEL=glm-4.7-flash`（全小写），config.py 默认值也改为小写 | .env, config.py |
| config.py 缺少 GLM 字段，.env 值被静默丢弃 | 在 Settings 类中显式声明 `glm_api_key`、`glm_model`、`glm_base_url` 字段 | config.py |
| GLM API 429 限流（免费版速率限制） | 增加429专用延迟（30s/60s/90s/120s/150s）；增加重试次数 3→5；题目生成改为批量模式（每批10卡片） | llm_service.py, config.py, understand_tasks.py |
| GLM 返回展平列表而非字典列表 | 修改提示词，明确要求返回 `{"questions": [{"card_index": 1, ...}]}` 格式，给出完整 JSON 示例 | llm_service.py |
| options 字段传入 Python 列表而非 JSON 字符串 | `options=json.dumps(q.get("options"), ensure_ascii=False) if q.get("options") else None` | understand_tasks.py |
| SQLAlchemy 异步会话延迟加载 MissingGreenlet | 查询后立即提取所有属性值到字典列表 `cards_data`，后续操作使用字典而非 ORM 对象 | understand_tasks.py |
| GLM 返回 JSON 前后有多余文字 | 增加正则提取容错逻辑 `re.search(r'\{[\s\S]*\}', response)` | llm_service.py |
| 嵌入模型在 FastAPI 进程中加载导致段错误 0xC0000005 | 临时方案：RAG 服务禁用向量检索，使用知识卡片中文 n-gram 匹配替代；TODO: 将嵌入模型移到独立服务或子进程 | rag_service.py |
| GLM 免费版 429 限流严重，5次重试全部失败 | 测试时临时切换到 DeepSeek（DEBUG=false）；生产环境建议使用 DeepSeek 或付费版 GLM | llm_service.py, .env |

### 第8周遇到的问题与解决记录

| 问题 | 解决方案 | 影响范围 |
|------|----------|----------|
| SQLite 缺少 SM-2 新增列 | QuizItem 模型新增 6 个 SM-2 字段后，已有数据库无对应列；通过 `ALTER TABLE ADD COLUMN` 手动添加 | quiz_item.py, 数据库迁移 |
| DueQuizResponse 缺少 `repetition` 字段 | E2E 测试报错"缺少 repetition 字段"；在 `schemas/review.py` 的 DueQuizResponse 中添加 `repetition: int = 0` | schemas/review.py |
| SQL INSERT 重复 `difficulty` 列 | E2E 测试的 fill_blank INSERT 语句有 19 列但 18 个值；移除重复的 `difficulty` 列引用和多余的 "easy" 参数 | tests/test_week8_e2e.py |
| 列表推导式语法错误 | `q["id"] not in [choice_questions[0]["id"]] if choice_questions else []` 不是合法 Python；提取 `used_ids` 为独立 set 变量 | tests/test_week8_review.py |
| 简答题部分匹配评分偏低 | "劳动合同是关于劳动关系的协议" vs 完整正确答案的关键词覆盖率低，quality=1；调整测试断言为 `1 <= q2 <= 4` | tests/test_week8_sm2.py |
| Celery worker 缺少 mineru_env 环境 | 默认 Python 无 `mineru` 模块；使用完整路径 `C:\Users\admin\anaconda3\envs\mineru_env\python.exe -m celery` 启动 worker | Celery 启动方式 |
| 选择题任何选项都判错 | `quality_from_answer` 用精确匹配比较，但前端提交完整选项文本（如"A. 劳动合同..."），数据库 answer 可能只是"A"；改为多格式匹配：字母前缀比较、去前缀纯文本比较 | sm2_service.py |
| 问题集页面 page_size=999 超限 | 后端 `page_size` 上限为 100，请求 999 触发 422 错误；改为分页循环加载全部题目 | QuestionSets.tsx |
| 422 错误显示 `[object Object]` | FastAPI 422 验证错误的 `detail` 是数组，`new Error(array)` 渲染为 `[object Object]`；改为检测数组类型并提取 `msg` 字段拼接 | client.ts |
| `get_review_stats` 变量未定义导致 500 | `due_count` 计算引用了 `today_done`，但 `today_done` 的查询在它之后才执行；将 `today_done` 查询移到 `due_count` 计算之前 | review_service.py |

---

## 📌 第1-4周未交付内容汇总

以下为计划中但第1-4周未完成的内容，需在后续开发中补齐：

### 数据库表（第1周周二未建成）
| 表名 | 用途 | 计划归属周 |
|------|------|-----------|
| KnowledgeCard（知识卡片表） | 存储从笔记中提取的知识点，关联原始笔记 | ✅ 已完成（第5周） |
| Question/QuizItem（题库表） | 存储根据知识点生成的题目，关联知识卡片 | ✅ 已完成（第6周） |
| ReviewLog（复习记录表） | 记录用户答题日志，含间隔重复参数 | ✅ 已完成（第8周） |

### 转换功能（第2周周二未实现）
| 功能 | 说明 | 计划归属周 |
|------|------|-----------|
| 图片/Office → Markdown | `convert_tasks.py` 目前仅支持 PDF 转换 | 第2周补齐 |
| 音视频 → ASR 转写 | 需要 asr_plus 集成 | 第2周补齐 |
| ~~本地/云端解析切换~~ | ~~当前硬编码 `backend="vlm-http-client"`，需在 config.py 中配置，前端增加选择按钮~~ ✅ 已修复：config.py 添加 `mineru_backend` 配置项，前端添加解析方式选择按钮 | ~~第2周补齐~~ 已完成 |

### 前端功能（第2周未实现）
| 功能 | 说明 | 计划归属周               |
|------|------|---------------------|
| ~~上传时选择本地/云端解析~~ | ~~前端按钮 + 后端参数传递~~ ✅ 已修复：Upload.tsx 添加解析方式选择按钮，后端支持 backend 参数 | ~~第2周补齐~~ 已完成 |
| ~~笔记详情页清洗版切换~~ | ~~依赖 AI 清洗管道（第3-4周）~~ ✅ 已修复：NoteDetail.tsx 重写为三视图模式（原始版/清洗版/对比视图），集成 CleaningPanel 和 DiffView 组件 | ~~第4周~~ 已完成 |
| ~~Token 过期前端告警~~ | ~~当前 Token 过期后无提示~~ ✅ 已修复：client.ts 检测 401 时派发全局事件，App.tsx 监听后跳转登录页 | ~~第10周（安全优化）~~ 已完成  |
| ~~用户名校验（仅英文+数字）~~ | ~~当前无校验~~ ✅ 已修复：后端 schema 添加正则校验，前端同步校验 | ~~第10周（安全优化）~~ 已完成  |

### mineru_plus 待完善
| 功能 | 说明 | 计划归属周 |
|------|------|-----------|
| ~~插入页码功能~~ | ~~mineru_plus 目前不支持在 Markdown 中插入页码标记~~ ✅ 已修复：添加 `_insert_page_markers_from_content_list()` 函数，利用 content_list.json 插入 `<!-- page=N -->` 标签 | ~~待确认~~ 已完成（需要优化） |
| 上传文件内存优化 | `upload.py` 整体读入内存 → 改为流式写入 | 第10周（性能优化） |
| 存储服务磁盘优化 | `shutil.copy2` → `os.replace` 移动文件 | 第10周（性能优化） |
| Celery 临时文件清理 | broker/results 文件堆积 | 第10周（性能优化） |

### 清洗管道待完善（第3-4周）
| 功能 | 说明 | 计划归属周 |
|------|------|-----------|
| 清洗规则调优 | 当前规则较基础，OCR/ASR 错字修正尚未实现（仅限高频且置信度极高的修正） | 第5周后迭代 |
| 语义分块 | 当前使用固定长度分块（chunk_size=500），未实现基于语义的分块 | 后续迭代 |
| 清洗性能优化 | 首次清洗耗时较长（221s，含模型下载），后续可优化模型加载和分块策略 | 第10周（性能优化） |
| 清洗副本版本管理 | 当前仅保存一份清洗副本，未实现版本历史和 diff 回溯 | 后续迭代 |
| HuggingFace 缓存清理 | 用户需手动删除 `C:\Users\admin\.cache\huggingface` 目录（权限受限） | 用户手动处理 |

### 理解管道待完善（第5-6周）
| 功能 | 说明 | 计划归属周 |
|------|------|-----------|
| 嵌入模型段错误修复 | BGE-M3 在 FastAPI 进程中加载导致段错误，需移到独立服务或子进程 | 后续迭代 |
| 向量检索启用 | RAG 问答当前仅使用 n-gram 匹配，向量检索因段错误禁用 | 后续迭代 |
| 混合搜索（关键词+向量） | 当前仅 n-gram 匹配，未实现 BM25 + 向量混合检索 | 后续迭代 |
| 流式输出（SSE） | 问答接口当前返回完整响应，未实现流式输出 | 后续迭代 |
| 知识卡片编辑/删除 | 前端卡片详情页暂无编辑和删除功能 | ✅ 已完成（第7周） |
| 题目审核/修改 | 用户无法审核、修改、删除生成的题目 | 第7周补齐 |
| ConversationSession 上下文裁剪优化 | 当前裁剪策略为丢弃中间对话保留最近轮次，可能导致早期章节信息丢失；可考虑摘要压缩策略 | 后续迭代 |

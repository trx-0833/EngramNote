# 项目级 npm 配置
# ================
#
# ## 为什么只有一行，以及它为什么必须有
#
# **把 registry 钉死在官方源。**
#
# 这不是洁癖，是为了挡住一件会静默复发的事：开发机上的**用户级** `.npmrc`
# （`~/.npmrc`）优先级低于项目级，只要它指向镜像，那么任何一次
# **不带 `--registry` 的 `npm install`** 都会把镜像 URL **批量写回
# `package-lock.json` 的 `resolved` 字段**。
#
# 本项目实测过这件事的两个方向：
#
#   * 本机 `npm config get registry` 曾是 `https://registry.npmmirror.com/`；
#   * npm 缓存索引里 `registry.npmmirror.com` 有 **1014** 条、
#     `registry.npmjs.org` 只有 **77** 条 —— 也就是说历史上大部分安装走的是镜像。
#
# 失效形态是**静默**的：锁文件照样能 `npm ci` 成功、测试照样全绿，
# 只有审阅供应链的人才会在锁文件里发现一批陌生主机名。
# 守卫：`backend/tests/test_lockfile_registry.py`（CI 必跑）与
# `frontend/scripts/check-lockfile-registry.mjs`（本地可手动跑）。
#
# ## 国内网络慢怎么办
#
# **用一次性的命令行参数覆盖，不要改这个文件**：
#
#     npm install --registry=https://registry.npmmirror.com
#
# 这样镜像只影响**本次下载**。要注意 `--registry` 是**命令行级**配置，
# 它的值会被写进本次产生的 `resolved`，所以用镜像装完之后请跑一遍
# `node scripts/check-lockfile-registry.mjs`；一旦发现污染，
# 就删掉 `package-lock.json` 用默认源重装一次（本文件已把默认源钉在官方）。
#
# ## CI 为什么不受这一行影响
#
# CI 用的是 `npm ci`：它**只读锁文件**、不重新解析依赖，也不读开发机的
# 用户级 `.npmrc`。所以这一行的作用是"让本地装依赖时也走对源"，
# 而不是"让 CI 能跑"。

registry=https://registry.npmjs.org/

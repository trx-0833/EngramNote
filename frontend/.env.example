# ============================================================
# EngramNote 前端环境变量模板
# ============================================================
#
# 用法：cp .env.example .env.local（.env.local 已被 .gitignore 排除）
#
# ## 这些变量在哪一步生效
#
# Vite 把 `VITE_*` 变量在**构建/启动时**注入产物，运行时改不了 ——
# 因此"这个前端指向哪个后端"是可审计的构建事实。
#
# ---- 开发期（npm run dev）----
#
# VITE_API_TARGET：vite dev server 把 /api 代理到哪个后端。
#   默认 http://localhost:8001（与 start.bat / start.sh 的 BACKEND_PORT 一致）。
#   后端跑在别的机器/端口时改这里，不用动源码。
# VITE_API_TARGET=http://localhost:8001
#
# ---- 构建期（npm run build）----
#
# VITE_API_BASE_URL：浏览器**直接**请求的 API 前缀。
#   不设 = '/api'（同源反代，推荐用于 nginx 部署，见 frontend/nginx.conf）。
#   跨域直连后端时才需要显式设置，且必须把该来源加进后端 CORS_ORIGINS：
# VITE_API_BASE_URL=https://api.example.com/api
#
# ⚠️ 部署到静态托管（Vercel/Netlify/OSS）时必须设 VITE_API_BASE_URL，
#    否则浏览器会去请求静态站自己的 /api 并得到 404。

# 前端可访问性审计（axe-core + 真 Chromium）

> 对应 overhaul-plan **5.9（可访问性）**。
> 本文件记录的是**已经跑起来的工具**与**它当场报出的东西**，不是计划。
>
> **当前状态：覆盖轮已落地，`npm run a11y` 绿灯（15 passed）。**
> 扫描场景 **10 → 15**，违规 **4 组 / 4 个节点 → 12 组 / 15 个节点**，
> 登记表 **4 条 → 12 条**。
> ⚠️ **这些数字变大不是回归，是覆盖面变大**：新增的 8 条规则 / 11 个节点
> **全部挂在 5 个新场景上**，10 个旧场景的违规数与登记时**逐条相同**
> （4 个 `heading-order` 节点，其中 3 个是 (b) 待人工决定）。
> 新增的每一条都是**下一轮要清掉的债**（登记是例外，删掉登记项、回到零容忍才是目标），
> 理由与归属写在 `e2e/a11y.spec.ts` 的 `REGISTRY` 与本文 §9.3。
> 本轮**没有改任何应用代码**（`frontend/src/**` 正在被并行的 CSS 迁移改动，
> 改了无法被验证），所以本轮的任务是**先把信号拿到**：让之前"从没被渲染过"的
> 状态与页面第一次真的被 axe 判过。逐场景对照见 §9.4 与 §8.1。

---

## 1. 怎么跑

```bash
cd frontend
npm run a11y            # = playwright test --project=a11y（该 project 固定 --workers=4）
npm run a11y:headed     # 带界面调试（单 worker）
npm run e2e             # 功能层（10 条），**不含**审计
npm run e2e:all         # 两个 project 一起跑（25 条），约 40 秒
```

一条命令自洽：dev server 由 `playwright.config.ts` 的 `webServer` 自动拉起与关闭
（端口 **4319**、`--strictPort`），与 `npm run e2e` 完全同一套机制 —— 不需要先开服务，
也不会碰人类手上 DSH Web GUI 的 3080。

### 两个 project，不是"一个 project 里的两个文件"

`playwright.config.ts` 里现在有**两个 project**：

| project | `testMatch` | 用例数 | worker | 谁在跑 |
|---|---|---|---|---|
| `chromium` | `**/!(*a11y).spec.ts` | 10 | 默认（按核数） | `npm run e2e` |
| `a11y` | `**/a11y.spec.ts` | **15** | **4**（写死在 project 里） | `npm run a11y` |

**为什么必须分开**（两个都是实测踩出来的）：

1. **并发**：8 个 worker 同时打 Vite dev server 时，**按需转换**（页面都是
   `React.lazy` 懒加载，每个路由第一次访问才现场编译）会把首屏拖到 10 秒以上，
   于是"等欢迎标题出现"超时 —— 失败信息指向"页面没渲染"，真实原因是
   "开发服务器被 8 个 axe 注入 + 懒加载请求同时敲"。审计要的是**页面内容**，
   不是吞吐。定到 4 之后稳定在 ~21 秒（覆盖轮后 15 条用例约 **31 秒**）。
2. **`testMatch` 是交集**：`playwright test a11y.spec.ts` 这种"命令行传文件名"
   会与顶层 `testMatch`（已排除 a11y）取交集，直接得到 `No tests found`。
   用 project 表达"这一组用例有自己的匹配式与参数"才是配置该说的话 ——
   这个坑在接线时真踩到了一次，写在这里免得下次再踩。

| 项 | 值 |
|---|---|
| 引擎 | `@axe-core/playwright` **4.13.0**（devDependency，连带 `axe-core` 4.13.0） |
| 规则集 | `wcag2a`、`wcag2aa`、`wcag21a`、`wcag21aa`、`best-practice` |
| 浏览器 | 仍是那一个 Chromium（`npm run e2e` 用的同一个，没有额外下载） |
| 用例 | **15 条**：13 条扫描用例（合计覆盖 **15 个场景** —— 登录页含 2 个状态、卡片复习含 2 个状态）+ 2 条自检 |
| 实测耗时 | 约 **31 秒**（本机，热缓存） |

### 扫了哪些界面

| 场景（`scene`） | 界面 | 为什么扫它 |
|---|---|---|
| `login` | 登录页（未认证入口，`/` 的兜底渲染） | 每个用户的第一屏 |
| `login-error` | 登录页 + 凭据错误的 401 状态 | 错误分支是另一个渲染分支，`role="alert"` 只在这里出现 |
| `dashboard` | 已登录外壳（侧边栏）+ 仪表盘 | 登录后的落地页；侧边栏在每个已登录页面上都存在 |
| `notes-list` | 笔记列表（非空列表，2 条笔记） | 卡片里是标题链接 + 独立操作按钮 |
| `note-detail` | 笔记详情（Markdown 正文 + 元信息栏） | 正文的标题层级、链接样式、角色下拉框 |
| `card-review-front` | 卡片复习：未翻面 | 该页**没有输入控件**（靠容器接键盘），结构特殊 |
| `card-review-back` | 卡片复习：已翻面（自评按钮 + 调度反馈） | 翻面后才出现的控件区 |
| `knowledge-graph` | 知识图谱（4 个节点的非空图谱） | 工具栏、过滤器、侧栏统计 |
| `projects` | 项目页 | 列表 + 新建表单 |
| `dashboard-mobile` | 仪表盘 @ 375×667 | 专门用来把"axe 判不了触控尺寸"落到记录里（见 §4） |
| **`notes-status-processing`** | 笔记列表 @ 2 条**处理中**的笔记 | `.status-converting` / `.status-cleaning`（`--color-warning`）此前**从没被渲染过**（§9.1） |
| **`daily-materials`** | 今日资料：文件夹列表 + **展开后**的资料 | 文件夹头是 `role="button"` 且里面嵌真按钮；资料行也渲染状态徽章 |
| **`today-learn`** | 今日学习：推荐任务 / 今日报告 / 待复习 / 薄弱点 | 有与 F-09 逐字相同的 `role="button"` 容器 + 三个优先级徽章（axe 判不了，只能实测） |
| **`upload`** | 上传页：拖拽区 + 三个设置卡片 | 拖拽区是 `role="button"` + `aria-label` 的写法 |
| **`review`** | 答题复习：选择题（含难度徽章） | 此前的"没有 h1"只是人工探针结论（§8.3 第 1 条），现在是门禁（§9.2 F-32） |

**没有扫**（覆盖轮之后剩下的，不要当成"已覆盖"）：问答 `QA`、知识卡片 `KnowledgeCards`、
卡片详情 `CardDetail`、学习评估 `LearningAssessment`、学习目标 `LearningGoals`、
回收站 `Trash`、快速复习 `QuickReview`、注册页 `Register`、404 页、考试/问题集 `QuestionSets`。
其中 `CardDetail` **不能直接加**：桩里 `/api/cards/{id}` 返回的是列表形状，
加进去只会得到"加载失败"页 —— 那是**假覆盖**，要先补一个符合卡片详情契约的桩（见 §5 第 3 条）。
`QuickReview` / `QA` 与 `Review` 共用 `QuizAnswerCard` / `SelfRatingButtons`，
所以 F-33（难度徽章对比度）**大概率也在那里**，但那是推断、不是实测。

---

## 2. 原始计数（审计当轮的实测值，一个数都没有修饰）

> ⚠️ **这一节是历史基线，不是当前状态。** 它是 5.9 修复轮**之前**的数字，
> 保留下来是为了让"改了多少"可核对（对照见 §8）。
> 当前状态（覆盖轮之后）：违规 **12 组 / 15 个节点**（其中 11 个节点在 5 个新场景上），
> 逐场景数字见 §9.4，登记项与债务见 §9.3。

axe 报的"违规"是**规则 × 节点**：一条规则命中 N 个元素 = 1 条规则 / N 个节点。

| 场景 | DOM 元素 | 评估规则 | 通过 | 违规规则 | 违规节点 | 需人工确认 | 不适用 |
|---|---|---|---|---|---|---|---|
| `login` | 48 | 90 | 26 | 2 | 7 | 8 | 61 |
| `login-error` | 49 | 90 | 30 | 2 | 7 | 9 | 57 |
| `dashboard` | 208 | 94 | 33 | 4 | 9 | 23 | 56 |
| `notes-list` | 143 | 93 | 32 | 4 | 10 | 17 | 56 |
| `note-detail` | 158 | 92 | 34 | 4 | 8 | 14 | 53 |
| `card-review-front` | 123 | 91 | 29 | 3 | 5 | 14 | 58 |
| `card-review-back` | 140 | 91 | 29 | 3 | 7 | 13 | 58 |
| `knowledge-graph` | 183 | 91 | 30 | 2 | 5 | 25 | 58 |
| `projects` | 139 | 92 | 29 | 2 | 4 | 15 | 60 |
| `dashboard-mobile` | 208 | 94 | 34 | 4 | 6 | 11 | 55 |
| **合计（按场景相加）** | — | — | **306** | **30** | **68** | **149** | **572** |

**这 68 个节点不是 68 个不同的元素**：同一个元素会在多个场景里各出现一次
（侧边栏在 8 个已登录场景里都存在；`login` 与 `login-error` 是同一页面的两个状态）。
按"规则 + 场景"去重后是 **30 组**（= 表格里每个场景的"违规规则"之和），
按 axe 的等级分布（同一节点跨场景重复计数）：

| 等级 | 节点数（跨场景相加） |
|---|---|
| `critical` | **2** |
| `serious` | **39** |
| `moderate` | **21** |
| `minor` | **6** |

"需人工确认"合计 **149 个节点** —— 那些**不是通过**，是 axe 判不了（见 §4.3）。

> 逐场景的完整证据（每个违规节点的**选择器 + 失败原因 + HTML 片段**）会作为
> JSON 附件随测试归档（`a11y-<scene>.json`），跑 `--reporter=json` 或失败时可直接取用。

### 一句话结论（不夸大）

**应用不是"无障碍合格"的**：axe 的 WCAG 2.0/2.1 A+AA 规则集在 9 个真实界面上
报出了 30 组"规则 × 场景"违规，其中 2 个节点是 `critical`（表单控件没有可访问名）、
39 个是 `serious`（对比度不足、按钮里嵌按钮、链接无法与正文区分）。
同时，**"这次没报出来的" 不等于 "没问题"** —— axe 看不见的东西列在 §4，
另有 149 个节点是它**明确不下结论**的（渐变背景上的对比度）。

> 上面这句是**修复轮之前**的结论，保留原文。修复轮之后剩 4 个节点，
> 且全部是待人工决定的标题层级（§8）。

---

## 3. 发现（按优先级排序，含逐条判定）

**判定口径：**
- **(a) 真实问题** —— 会影响真实用户的某种输入方式（键盘 / 屏幕阅读器 / 低视力），
  且修法明确；
- **(b) 需要人工确认 / 可能是设计取舍** —— 规则的判据与产品意图有张力，
  或依赖用户内容，需要人做决定；
- **(c) 真实但在本轮范围外** —— 确实是问题，但归属别的工作流（如设计令牌由 5.6 管）。

`id` 与 `frontend/e2e/a11y.spec.ts` 里的 `REGISTRY` **一一对应** ——
测试里每一条豁免都能在这里查到为什么。

**读法（修复轮之后）**：下面每张表都多了一列「修复」。
`✅ 已修` = 该规则的节点数已归零，登记项已**删除**，回归会被判成"未登记"而失败；
`⬜ 待人工决定` = 仍留在登记表里、上限已下调到实测值。
**已修的条目一律不删行** —— 删了就没人知道当初是什么问题、为什么那样修。

### P0 —— 现在就修，成本极低、覆盖全部用户

| id | 规则 | 影响 | 位置 | 证据 | 判定 | 修复 |
|---|---|---|---|---|---|---|
| **F-10** | `select-name` | **critical** | 知识图谱 → 卡片类型过滤器<br>`src/pages/knowledgegraph/GraphToolbar.tsx:100` | `<select className="graph-filter-select">` 没有 `<label>`、没有 `aria-label`、没有 `title`。axe：*Element does not have an implicit (wrapped) `<label>` … aria-label attribute does not exist or is empty* | **(a) 真实问题**。屏幕阅读器只念"组合框"，用户不知道自己在筛什么。<br>**一行可修**：加 `aria-label="按卡片类型筛选"`。 | ✅ **已修**。加 `aria-label="按卡片类型筛选"`（刻意不用可见 `<label>`：工具栏是一行横排控件，插入标签会把搜索框、过滤器、图例挤到第二行 —— 那是改版式）。顺带补了 axe **看不见**的焦点环：`.graph-filter-select:focus` 原来只改 1px 边框色，现在加 2px 白内环 + 2px 墨色外环（`graph.css:487-496`，`box-shadow` 不参与布局）。 |
| **F-11** | `select-name` | **critical** | 笔记详情 → 「笔记角色」下拉框<br>`src/pages/notedetail/NoteDetailHeader.tsx` | 同上（内联样式里还有 `outline: none`，键盘焦点也看不出来） | **(a) 真实问题**。这个控件会**改数据**（`updateNoteRole`），无名控件比只读筛选更糟。<br>**一行可修**：加 `aria-label="笔记角色"`。 | ✅ **已修**。`aria-label="笔记角色"`；**并存的两个问题也一起修了**（见下面「F-11 的两个附加项」）。 |

#### F-11 的两个附加项（都是 axe 报不出来的）

原实现把外观整块写在 tsx 的 `style={{...}}` 里，其中包括 `outline: 'none'`。
**内联样式的权重高于任何选择器** —— 只要它还留在 tsx 里，样式表里的
`:focus-visible` 焦点环就永远不会生效。所以外观整体搬进了新的
`src/pages/notedetail/NoteDetailHeader.module.css`，随数据变化的那一项
（角色对应的底色）由 CSS 自定义属性 `--note-role-bg` 传入。

1. **焦点可见**（axe 不检查，见 §4.1）：`:focus-visible` 给两层环 ——
   2px 白色内圈（对两种底色分别 4.78:1 / 7.10:1）+ 2px 墨色外圈
   （`--color-primary-hover`，白底 15.89:1）。用 `box-shadow` 而不是
   `outline` + `outline-offset`：这一行元信息是 `align-items:center` 的 flex，
   画到控件外面的那两圈会被相邻兄弟压住；`box-shadow` 也不改变控件几何。
   **实测**（真 Chromium，聚焦后 `getComputedStyle`）：
   `box-shadow: rgb(255,255,255) 0 0 0 2px, rgb(22,33,62) 0 0 0 4px`。
2. **下拉框自身的对比度**（`F-12` 的第三种成因，axe 报了）：
   白字压在原底色上只有 3.68:1。`material` 由 `#3b82f6` 压深到 **`#316fd8`**
   （白底白字 **4.78:1**）；`personal_note` 由 `#8b5cf6` 压深到 **`#6d28d9`**
   （**7.10:1**）。色相不变，只是深一档。

### P1 —— 覆盖面最广的单点修复

| id | 规则 | 影响 | 位置 | 证据 | 判定 | 修复 |
|---|---|---|---|---|---|---|
| **F-04** | `color-contrast` | serious | **每个已登录页面**的侧边栏分组标题<br>`src/styles/layout.css:180-189`（`color: var(--sidebar-section-title)` → `--color-text-tertiary`） | `#9a9ab0` 白底 **2.75:1**，11.2px 小字要求 4.5:1。9 个场景里 8 个命中（窄屏收进抽屉除外） | **(a) 真实问题**。三处分组标题（学习 / 笔记 / 知识）是导航的**结构信息**，低视力用户看不出分组。<br>**一处修，九页受益**：改 `--color-text-tertiary`（`src/styles/base.css:23`）或让 `.sidebar-section-title` 用更深的令牌。注意该令牌共 16 处引用（含折叠态省略号、DiffView、Auth 页脚），改动需要跑一遍本审计确认没有连带回归。 | ✅ **已修**。`--color-text-tertiary` **`#9a9ab0` → `#6f6f8a`**：白底 **2.75 → 4.86:1**，`--color-bg`(#faf9f7) 上 **2.62 → 4.62:1**。选它而不是更深的 `#666680`（5.56:1）是为了保住"三级文字仍比二级 `#5a5a7a` 浅"的层次。**16 处引用全部复核过**：白底/近白底的那些（侧边栏标题、图谱面板标题、`.sidebar-item-action`、placeholder、搜索图标、ProjectCard 等）全部 ≥4.5:1；压在 `#f5f0e4` 宣纸底上的 `.graph-legend-item` 与 `--graph-ink-faint` 一类是 **px 线/装饰**（非文字，不受 4.5:1 约束）。9 个场景复跑：该令牌**一个违规节点都不剩**。 |
| **F-05** | `color-contrast` | serious | 知识图谱侧栏面板标题 `.graph-panel-title`<br>`src/styles/graph.css:186-195` | 同一令牌 `--color-text-tertiary`，白底 2.75:1 | **(a) 真实问题**，与 F-04 同源。 | ✅ **已修**（随 F-04 的令牌改动，4.86:1）。`knowledge-graph` 场景现在 **0 违规**。 |
| **F-06** | `color-contrast` | serious | 状态徽章 `.status-cleaned` / `.status-converted`<br>`src/styles/components.css:213-215`（`--color-success: #2d8a56`，`base.css:28`） | 白底 **4.3:1**、`--color-bg` (#faf9f7) 上 **4.08:1**，12.8~14px 小字要求 4.5:1 | **(a) 真实问题，但边际**（差 0.2~0.4）。它标的是"这条笔记处理到哪一步了"，低视力用户会把它和相邻徽章混淆。<br>修法：把 `--color-success` 压深一档（例如 `#25714a` 一类）—— **归属 5.6 设计令牌**，属于 (c) 的一部分，但影响用户，建议在 5.9 一起做。 | ✅ **已修**。`--color-success` **`#2d8a56` → `#25714a`**：白底 **4.30 → 5.93:1**，`--color-bg` 上 **4.08 → 5.64:1**。同一语义在 `utils/labels.ts` 里还有一份**字面量**（自评档位），两处**一起**改成了 `#25714a` —— 这是手工同步，没有门禁守着（已写进 `labels.ts` 的注释）。派生值也复核过：`.btn-danger` 用的 `--color-error` `#c0392b` 是 5.44:1（本来就够），`--color-warning` `#c4860a` 是 3.11:1，但它只出现在未扫页面的 `.status-converting/.status-cleaning` 上（见 §8.3）。 |

### P2 —— 交互结构问题（比对比度更影响键盘与屏幕阅读器）

| id | 规则 | 影响 | 位置 | 证据 | 判定 | 修复 |
|---|---|---|---|---|---|---|
| **F-09** | `nested-interactive` | serious | 仪表盘「今日待复习」卡片<br>`src/pages/Dashboard.tsx:272-289` | 外层 `div[role="button"][tabindex="0"]` 里嵌了一个真 `<button>开始复习</button>`。axe：*Element has focusable descendants* | **(a) 真实问题**。可聚焦元素里嵌可聚焦元素：Tab 会在同一个卡片里停两次、屏幕阅读器会把内层按钮念成外层按钮的一部分，而且外层只监听 `Enter`（**没监听 Space**），与 `role="button"` 的约定不符。 | ✅ **已修**。外层去掉 `role`/`tabIndex`/`onClick`，卡片回到"盒子"，`onClick={() => navigate('/review')}` 落在**真按钮**「开始复习」上。改法与已修掉的「Sidebar 按钮嵌按钮」**同形：一张卡片一个控件**。键盘结果更好：Tab 只停一次，且按钮原生支持 Space（原来外层不支持）。点击面从"整卡"变成"按钮"是有意的 —— 外壳只负责外观，行为在有名字的控件上。 |
| **F-17** | `nested-interactive` | serious | 笔记列表的笔记卡片（2 张各一次）<br>`src/pages/NotesList.tsx:216-222` | 卡片是 `article[role="button"][tabindex="0"]`，内部有可聚焦的删除/重试按钮 | **(a) 真实问题**，与 F-09 同一类。**与已修掉的 `<button>` 嵌 `<button>`（Sidebar）是同一个洞的不同入口** —— 那次修的是真按钮嵌套，这次是 `role="button"` 容器嵌套。修法应统一：卡片不要 `role="button"`，改为"标题是真链接 + 操作是独立按钮"。 | ✅ **已修**（照审计建议的字面做法）：`article` 去 `role`/`tabIndex`/`onClick`/`onKeyDown`，标题变成真 `<Link>`（可右键、可新标签页、Tab 一次即达），「重试」「删除」保持独立按钮、作**兄弟**。连带收益：`handleDelete` / `handleRetry` 里的 `e.stopPropagation()` 不再需要，已删 —— 依赖它"别点到自己"的日子结束，按钮可以随便挪。 |
| **F-03 / F-16** | `aria-allowed-role` | minor | 仪表盘「最近笔记」2 张卡 + 笔记列表 2 张卡<br>`Dashboard.tsx` / `NotesList.tsx` | `<article role="button">` —— ARIA 不允许 `article` 使用 `button` 角色 | **(a) 真实问题，但等级轻**。`role=button` 会**覆盖** `article` 的语义，屏幕阅读器不再把它当一篇文章；可访问名来自内容，所以还能用。<br>修法与 F-09/F-17 是同一处改动，建议一起做。 | ✅ **已修**（与 F-09/F-17 同一处改动）：两页的 `<article>` 都回到"文章"语义，标题行内是真链接。`dashboard` / `dashboard-mobile` / `notes-list` 三个场景的 `aria-allowed-role` 节点全部归零。 |
| **F-19** | `color-contrast` | serious | 卡片复习的自评四档<br>`src/utils/labels.ts:110-115`（`selfRatingOptions[].color`） | 「勉强想起」`#c9a959` 白底 **2.25:1**、「想起来了」`#2d8a56` 白底 **4.3:1** | **(a) 真实问题**。四个档位的**色差本身就是区分手段**（红/金/绿/蓝），而金色只有 2.25:1 —— 对色觉障碍与低视力用户，这两档几乎一样。<br>**同一组件 `SelfRatingButtons` 也用在答题复习页（`Review`）**，那里本轮未扫 → 见 §5。 | ✅ **已修**。只压深**不达标的两档**，色相与四档相对关系（红→金→绿→墨）都不动：金 `#c9a959` → **`#8f7020`**（**2.25 → 4.66:1**）、绿 `#2d8a56` → **`#25714a`**（**4.30 → 5.93:1**）。红 `#c0392b`（5.44:1）与墨 `#0f3460`（12.50:1）本来就够，**未改** —— 改它们只会白白挪动视觉基准。**实测**（真 Chromium，四个按钮的主文案 `color`）：`rgb(192,57,43) / rgb(143,112,32) / rgb(37,113,74) / rgb(15,52,96)`，与上表逐项一致。 |

### P3 —— 语义与导航结构

| id | 规则 | 影响 | 位置 | 证据 | 判定 | 修复 |
|---|---|---|---|---|---|---|
| **F-01 / F-02** | `landmark-one-main`、`region` | moderate | 登录页（初始态与失败态）<br>`src/pages/Login.tsx` + `Auth.module.css` | 全页没有 `<main>`；h1、两个 label、两个 input、页脚 p 共 6 个节点"不在任何 landmark 里" | **(a) 真实问题**，但影响的是**屏幕阅读器的跳转效率**（"跳到主内容"不可用），不是"用不了"。修法：把表单区包进 `<main>`（顺带消掉 `region` 的 6 个节点）。 | ✅ **已修**。`<main>` 包在**卡片内部**而不是替掉卡片：`.authCard` 的 max-width/padding/毛玻璃/入场动画都挂在那个 div 上，换掉它就得把这些值复制一遍（那正是会漂移的写法）。`login` 与 `login-error` 两个场景现在都是 **0 违规**（7 个节点全消）。<br>⚠️ 代价：DOM 多了一层，`e2e/app-shell.spec.ts` 里那条 `xpath=../..` 就指错了元素 —— 已按判据本身改成"从 h1 往上找第一个撑满视口高的祖先"（见 §8.4）。 |
| **F-07 / F-22** | `heading-order` | moderate | 仪表盘（桌面 + 移动端）<br>`Dashboard.tsx` 的卡片标题是 h3 | h1「欢迎使用 EngramNote」之后直接出现卡片里的 h3 | **(b) 需要确认**。`h3` 是有意为之还是顺手写的？视觉上卡片标题确实比区块标题小。修法有两种（把卡片标题改成 h2，或把区块标题改成 h2 后卡片用 h3），**取决于设计意图**，所以标 (b)。 | ⬜ **待人工决定**（本轮**刻意不动**）。复核后确认这不是"改一处级别"能修的：仪表盘的卡片住在**两栏区块**里（`.dashboardTwoCol`：今日学习目标 + 每日推荐任务 / 今日待复习 + 薄弱点），而这两组区块**没有区块级标题**，所以 h1 与卡片标题之间本来就缺一级。把卡片标题降级成 h2 只能消掉报警（真正的结构问题原样留着），而给两组各加一个区块标题是**起名 + 多两行标题**的产品/设计决定。登记表上限维持 1：多一个节点就失败。 |
| **F-18** | `page-has-heading-one` | moderate | 笔记列表 | 整页没有 h1（最高级标题是卡片里的 h3） | **(a) 真实问题**。页面没有"这是什么页"的一级标题。修法：给列表加 `<h1>笔记</h1>` 或复用一个现有标题。 | ✅ **已修**。加 `<h1 className="heading-serif">笔记</h1>`（放在搜索栏上方，不与搜索框抢同一行；字号 1.25rem）。**必须一起做的那一步**：卡片标题 `h3` → `h2`（字号由 `fontSize: '1.17rem'` 显式钉住，**一个像素都没动**）—— 不这样做，补上的 h1 会立刻引出 `h1 → h3` 的 `heading-order`（第一次跑就撞上了，见 §8.4）。 |
| **F-14 / F-15** | `page-has-heading-one` | moderate | 卡片复习（正反两面）<br>`src/components/quiz/ReviewProgress.tsx:68` | 该组件渲染的是 `<h2>{title}</h2>` —— 页面上根本没有 h1 | **(a) 真实问题，且修法明确**：`ReviewProgress` 的标题就是这一页的顶层标题，改成 `<h1>` 即可（同时消掉 `heading-order` 的隐患）。 | ✅ **已修**。`h2` → `h1`（字号 1.2rem 不变）；**同一处**把 `cardreview/CardFace.tsx` 的卡片标题 `h3` → `h2`（字号 1.15rem 显式写着，不变），否则就是新的 `h1 → h3` 跳级。`card-review-front` / `card-review-back` 两个场景现在 **0 违规**。<br>**答题复习页（`Review`）复核过**：它调用 `ReviewProgress` **不传 `title`**（走"一行式"排版，本来就没有标题），所以这次改动**碰不到它** —— 实测该页 `<h1>`~`<h6>` 数量为 **0**，它缺 h1 是既有问题（见 §8.3）。 |
| **F-20** | `heading-order` | moderate | 项目页<br>`src/pages/projects/ProjectsHeader.tsx:8` 是 h1，卡片里是 h3 | h1 → h3 跳级 | **(b) 需要确认**，同 F-07。 | ⬜ **待人工决定**，同 F-07（同一个"卡片标题的级别是视觉层次的一部分、而卡片区没有区块标题"的问题）。上限维持 1。 |
| **F-08** | `heading-order` | moderate | 笔记详情：Markdown 正文的 h4 | 页面 h1/h2 之后直接出现正文里的 h4（原文本身没写 h3） | **(b) 需要人工决定**。层级来自**用户自己的内容**，不是布局缺陷。可选：渲染时归一化标题级别（`h1→h2` 整体下沉）、或接受它。**不要**为了消警告去改用户内容。 | ⬜ **待人工决定**（本轮**刻意不动**）。实测该页标题序列：`H1 锂离子电池的浮充与均充` → `H4 清洗统计` → `H1 浮充与均充` → `H2 两者的区别` —— `H4 清洗统计` 来自页面自己的元信息区块，其余来自用户 Markdown。归一化要么改用户内容、要么给"清洗统计"这类区块单独定级别，两者都要人拍板。上限维持 1。 |

### P4 —— 正文链接的可辨识度

| id | 规则 | 影响 | 位置 | 证据 | 判定 | 修复 |
|---|---|---|---|---|---|---|
| **F-13 / F-26** | `link-in-text-block` | serious | 笔记详情正文；卡片复习的「原文语境」区<br>`src/styles/base.css:120-124`（`a { text-decoration: none }`）+ `SourceContext.tsx:120` | 链接与周围文字的对比 **1.36:1**（正文）与 **1.89:1**（灰字旁），要求 3:1；且**没有下划线** | **(a) 真实问题**。正文里 `#0f3460` 的链接和 `#1a1a2e` 的正文几乎同色 —— 不靠颜色分辨不出哪里可以点。WCAG 1.4.1（不只靠颜色传达信息）的典型场景。<br>修法：给 `.markdown-body a` 与 SourceContext 的链接加下划线（或把链接色改成与正文对比 ≥3:1 的明显色）。 | ✅ **已修**。选**下划线**而不是改链接色：主色 `#0f3460` 与正文 `#1a1a2e` 是刻意的同族墨色，要凑出 3:1 就得把链接推到另一个色系 —— 那是重新设计配色（5.6 的活），不是修一处可访问性。下划线**不改任何色值**。<br>范围写得比"两处"更宽：全局 `a` 默认带下划线（全项目**一共只有 11 处** `<a>`/`<Link>`，逐容器补必然漏掉将来新增的那一处），按钮造型与整块可点的链接逐个在 tsx 里 `textDecoration: 'none'` **显式**关掉（App.tsx 的「返回首页」、两页的卡片标题链接）—— 例外写在看得见的地方。<br>**实测**（真 Chromium）：`.markdown-body a` 的 `text-decoration-line: underline`。 |

**汇总判定**（口径：`(a)` 会影响真实用户的某种输入方式；`(b)` 判据与产品意图有张力，
需要人做决定；`(c)` 属实但归属别的工作流）：

- **(a) 真实问题：13 组 —— 全部已修（13/13）** —— F-01/F-02（登录页无 landmark，2 条同源）、
  F-03/F-16（`article[role=button]`）、F-04/F-05（侧边栏与图谱面板标题对比度，
  同源）、F-09/F-17（卡片里嵌可聚焦元素）、F-10、F-11（两个无名 `<select>`）、
  F-13/F-26（正文链接无法与正文区分）、F-14/F-15（卡片复习缺 h1）、
  F-18（笔记列表缺 h1）、F-19（自评档位对比度）。
- **(b) 需要人工决定：3 组 —— 3/3 仍未动** —— F-07/F-22、F-20（标题级别是否该改）、
  F-08（Markdown 正文的标题层级要不要归一化）。它们都是**同一件事**：
  "标题级别是设计意图还是顺手写的"，只有人能回答。
- **(c) 归属其他工作流：1 组 —— 已在 5.9 顺手做掉** —— F-06（`--color-success` 令牌
  本身的取值属 5.6 设计令牌；但它的可见后果落在 5.9，所以两边都要知道）。
  改动是**两个色值**（`base.css` 的令牌 + `labels.ts` 的字面量），没有改任何结构。

按 axe 的等级分布（**跨场景相加**，所以同一元素会在多个页面里各计一次）：
`critical` 2 个节点、`serious` 39 个、`moderate` 21 个、`minor` 6 个。
修复后只剩 `moderate` 4 个节点（全部是 (b) 类）。

### 覆盖轮新增的发现（F-28 – F-35，**本轮新出现的信号**）

> 这 8 条是**本轮新场景**（§9.1）报出来的，之前**没有任何一层看得见**它们：
> 状态徽章的颜色没被渲染过、三个页面不在扫描范围里、`Review` 只有人工探针结论。
> 逐条的叙事、判定与"归属谁"见 §9.2 / §9.3。**全部登记在 `REGISTRY` 里**
> （上限 = 实测值），**每一条都是下一轮要清掉的债**。

| id | 规则 | 影响 | 位置 | 证据（实测） | 判定 | 修复 |
|---|---|---|---|---|---|---|
| **F-28** | `color-contrast` | serious | 笔记列表的状态徽章 `.status-converting` / `.status-cleaning`<br>`src/styles/components.css` 的两条 `.status-*` 规则 ← `--color-warning`（`base.css`） | **3.11:1**（`rgb(196,134,10)` on `rgb(255,255,255)`，12.8px/400，要求 4.5:1）。实测见 `a11y-notes-status-contrast.json` | **(a) 真实问题**。与已修的 **F-06 完全同类**（同一个"状态徽章小字用浅色"的洞），修法也一样：**压深令牌本身**。 | ⬜ **本轮不修（债）**。`src/**` 正被并行 CSS 迁移改动，改了无法验证。下一轮把 `--color-warning` 压深一档 → 同时消掉 F-28 / F-29，然后删掉两条登记项。 |
| **F-29** | `color-contrast` | serious | 今日资料页展开后的资料行（同一对类名）<br>`DailyMaterials.tsx:669` | **3.11:1**，与 F-28 逐字相同 | **(a) 真实问题**，**同一个缺陷的第二条渲染路径**（类名来自唯一的 `statusClass()`）。 | ⬜ **本轮不修（债）**。随 F-28 一处修好，两条登记项一起删。 |
| **F-30** | `nested-interactive` | serious | 今日资料页的**文件夹头**（`div[role="button"]` 里嵌「重命名」「删除」真按钮）<br>`DailyMaterials.tsx:467-480` | axe：*Element has focusable descendants*（1 节点） | **(a) 真实问题**。**与已修的 F-09 / F-17 是同一个洞的不同入口**，连写法都逐字相同（外层 `role="button"` + 手写 Enter，内层真 `<button>`）。 | ⬜ **本轮不修（债）**。照 F-09 已经跑通的形状改（**控件之间是兄弟**），然后删掉登记项。 |
| **F-31** | `heading-order` | moderate | 今日资料页：h1「今日资料」→ 文件夹名 h3<br>`DailyMaterials.tsx:517` | axe：*Heading order invalid*（1 节点） | **(b) 需要人工决定**，与 F-07 / F-20 **同一类**（卡片标题的级别是视觉层次的一部分，而区块没有区块级标题）。 | ⬜ 登记在案，**需要设计决定**（起名 + 多一行标题），不是 5.9 能单方面拍的。上限 1。 |
| **F-32** | `page-has-heading-one` | moderate | 答题复习页 `Review`：**整页没有 h1**（`<h1>`~`<h6>` 共 0 个） | axe：*Page must have a level-one heading*（1 节点） | **(a) 真实问题**。与已修的 **F-14/F-15（卡片复习缺 h1）、F-18（笔记列表缺 h1）是同一个判据**；上一轮**已经知道**这件事（§8.3 第 1 条），但那只是人工探针结论 —— 本轮它才第一次成为门禁。 | ⬜ **本轮不修（债）**。补 h1 时要一起看 `h1 → h3` 跳级（§8.4 的坑）。 |
| **F-33** | `color-contrast` | serious | 答题复习页的**难度徽章**（白字压 `difficultyColors.medium`）<br>`utils/labels.ts` 的 `difficultyColors` | **2.25:1**（`#ffffff` on `#c9a959`，12.8px/400） | **(a) 真实问题**。与已修的 **F-19 同类、甚至同一个色值**：上一轮只把 `selfRatingOptions` 的金 `#c9a959` 压深成 `#8f7020`，`labels.ts` 里**其它几张颜色表没动**（§9.2 第 5 条的清单）。 | ⬜ **本轮不修（债）**。下一轮把 `labels.ts` 里"白字压色块"的取值一次过一遍。 |
| **F-34** | `nested-interactive` | serious | 今日学习页「待复习任务」卡片（`div[role="button"]` 里嵌真 `<button>开始复习</button>`）<br>`TodayLearn.tsx:329-335` | axe：*Element has focusable descendants*（1 节点） | **(a) 真实问题**。**与已修的 F-09 逐字相同**（连"只监听 Enter、不监听 Space"都一样）。 | ⬜ **本轮不修（债）**。照 F-09 的形状改（行为落在真按钮上），然后删掉登记项。 |
| **F-35** | `color-contrast` | serious | 今日学习页薄弱点列表的**卡片类型徽章**<br>`TodayLearn.tsx:374-379`（同一处代码也在 `Dashboard.tsx:311-318`） | **3.13:1**（`#f44336` on `#fee8e6` = `#f4433620` 叠白底，12px/400），2 个节点 | **(a) 真实问题**。⚠️ 仪表盘上那一份**扫不出来**：默认桩的 `/api/report/weak-points` 字段名与契约不一致 → 渲染成 `undefined` 文本 + 空徽章（§9.2 第 1 条）。 | ⬜ **本轮不修（债）**。修那个字面量，并顺手把默认桩的字段名改对（否则仪表盘永远看不见它）。 |

**覆盖轮的 (a)/(b)/(c) 分类**（8 条新发现里）：

- **(a) 真实问题 7 条（全部未修，本轮只登记）**：F-28、F-29、F-30、F-32、F-33、F-34、F-35
  —— 其中 **F-28/F-29 与已修的 F-06 同类**、**F-30/F-34 与已修的 F-09/F-17 同类**、
  **F-32 与已修的 F-14/F-15/F-18 同类**、**F-33 与已修的 F-19 同类**（同一张表里漏掉的一处）、
  **F-35 是同一处代码在两页上的复现**。修法都已经有先例，**不是需要研究的问题，是需要排期的问题**。
- **(b) 需要人工决定 1 条**：F-31（今日资料页 h1 → h3，与 F-07/F-20 同一类）。
- **(c) 归属别的工作流 0 条**：新发现里没有"只属于 5.6"的条目；
  色值类（F-28/F-29/F-33/F-35）与 F-06/F-19 同例，**取值属 5.6、可见后果落在 5.9**，两边都要知道。

**外加 1 条 axe 永远报不出来、只有实测有信号的（不是 axe 违规，因此不进 `REGISTRY`）**：

- **F-36（实测，无门禁）**：今日学习页推荐任务的**优先级徽章**（高/中/低）白字压在三个色块上，
  实测 **3.68:1 / 2.16:1 / 2.54:1**（11.2px/400，要求 4.5:1，见 §9.4）。
  axe **结构上**报不出来：它的 `colorContrastEvaluate` 对"只有一个字符"的文本不下结论
  （`shortTextContent`），这三条永远落在 `incomplete`。
  也就是说这一条**没有门禁**，本轮只把数字量下来（`a11y-today-learn-badges.json` 附件），
  下一轮修它时应当**同时**加一条基于 `measureContrast` 的断言。
  归属 5.9 下一轮 + 5.6（`priorityMeta` 的三个色值取在 `TodayLearn.tsx` 里）。


### 一句话结论（不夸大）

**应用不是"无障碍合格"的**：axe 的 WCAG 2.0/2.1 A+AA 规则集在 9 个真实界面上
报出了 30 组"规则 × 场景"违规，其中 2 个节点是 `critical`（表单控件没有可访问名）、
39 个是 `serious`（对比度不足、按钮里嵌按钮、链接无法与正文区分）。
同时，**"这次没报出来的"不等于"没问题"** —— axe 看不见的东西列在 §4，
另有 149 个节点是它**明确不下结论**的（渐变背景上的对比度）。

> 这一节同样是**修复轮之前**的结论，逐条状态见 §3 的「修复」列与 §8。
> 一句话：**(a) 类 13 组全部修完，(b) 类 3 组原样留着待人拍板。**

**没有发现**的问题（同样是结论，写下来免得下次重复劳动）：
- 没有 `image-alt` 违规 —— 应用里的图标要么 `aria-hidden`、要么有可访问名；
- 没有 `button-name` / `link-name` 违规 —— 现有按钮与链接都有名字
  （`Sidebar.tsx:98/104/138` 的 `aria-label` 起了作用）；
- 没有 `duplicate-id` / `aria-hidden-focus` / `label`（表单标签）违规 —— 登录表单的
  `<label for>` 与 `#email`/`#password` 的关联是**真的**，这解释了为什么
  `login-form.spec.ts` 那条"原生约束会拦下提交"能成立；
- 没有颜色之外的对比度问题（`color-contrast-enhanced` 未启用，AAA 不在本层范围内）。

---

## 4. axe **看不见**的东西（这一层明确做不到的事）

下面每一条都是**当前没有任何一层在守**的。写在这里不是免责，是待办清单。

> **修复轮补进去的两条**（都只是"修了具体那一处"，**没有**变成自动门禁）：
> 笔记角色下拉框的焦点环（§4.1）与 `.sidebar-collapse-btn` 的对比度（§4.3）。
> 两条后面都写了实测值 —— 换句话说，"人工看一眼"确实能结案，
> 但**下一次改动不会有人替你再看一遍**，这些清单项不会自己变绿。

### 4.1 焦点顺序、焦点可见性、键盘陷阱

axe 只看 DOM 与计算样式，**不模拟 Tab 键**。所以：

- **Tab 顺序是否合理**没有验证。已登录外壳里侧边栏在 DOM 中先于 `main`
  （`App.tsx:91` 的 `<Sidebar/>` 在 `<main>` 之前），所以每个页面都要先 Tab 过
  15 个导航项才能到内容 —— 这可能是问题，也可能是有意，**本轮没有量过**。
- **焦点陷阱**（尤其在移动端抽屉与模态框里）没有验证：`LinkManagerModal`、
  `DeleteNoteDialog`、侧边栏抽屉打开时焦点会不会跑到背后？没有测。
- **`outline: none`** 出现在至少两处内联样式里（`NoteDetailHeader.tsx` 的
  `<select>`、`CardReview.tsx:129` 的容器）。前者是**真控件**，去掉焦点环
  意味着键盘用户看不出焦点在哪。axe 不检查这个（它不属于任何 ARIA 规则）。
  > **修复轮**：`NoteDetailHeader` 那处**已修** —— 外观整块搬进
  > `NoteDetailHeader.module.css`，`:focus-visible` 给"白内圈 + 墨色外圈"
  > 两层环（理由与实测值见 §3 的「F-11 的两个附加项」）。
  > 顺带把图谱过滤器 `.graph-filter-select:focus` 也补成两层环。
  > `CardReview.tsx:129` 的容器是 `tabIndex={-1}` 的**非交互**容器
  > （只用来接键盘事件），它不需要焦点环，**保持原样**。
  > **"焦点环画出来长什么样"仍然没有断言**：本次是在真 Chromium 里
  > 读 `getComputedStyle` 人工核对（`box-shadow` 两层的颜色与半径），
  > 不是自动门禁 —— 下面「后续」那条依然成立。
- **`CardReview` 的键盘约定**：该页没有输入控件，靠容器 `tabIndex={-1}` +
  `useReviewKeyboard` 接 Enter（`CardReview.tsx:71-76` 有说明）。它在 Vitest 里
  有单测，但"真浏览器里焦点落到 body 后回车还灵不灵"**没有被本层验证**。

**后续**：写几何 + `page.keyboard.press('Tab')` 的断言（记录 `document.activeElement`
序列），或引入 `@axe-core/playwright` 之外的手工脚本。这是一件**独立**的活，
不要指望 axe 顺手覆盖。

### 4.2 屏幕阅读器语义

- **可访问名算不算"好名字"**：axe 只判"有没有名字"。例如图谱过滤器加了
  `aria-label="按卡片类型筛选"` 就算过，但这个名字在实际朗读里是否清楚，
  只有人戴耳机听一遍才知道。
- **朗读顺序 / 冗余**：侧边栏 15 个按钮每个前面带 emoji（`☘ 今日学习`、
  `◎ 知识图谱`）。emoji 会不会被念成"四叶草"？axe 不管。
- **动态区域**：`Toast`、`ReviewProgress` 的进度更新、`CardReview` 的
  "下次复习: 21 天后" —— 这些**异步出现**的内容有没有 `aria-live`，
  应该念还是不该念，axe 判不了（`aria-live` 存在性它可以判，**该不该有**不行）。

### 4.3 动态状态与渐变背景上的对比度（**149 个"需人工确认"节点**）

axe 的 `color-contrast` 在**算不出背景色**时不会猜，而是把节点丢进 `incomplete`。
本项目里这是最大的一类（跨场景相加 149 个节点，比违规节点还多一倍），成因有两类：

1. **渐变文字**：`background-clip: text` + `-webkit-text-fill-color: transparent`
   （`layout.css:26/90-93` 的 `.sidebar-logo`、`components.css:270-281` 的
   `.gradient-text` / `.gradient-text-gold`）。**文字的前景色是透明的**，
   axe 拿不到；要人工按渐变两端色分别算对比度。
   命中的有：侧边栏 logo「EngramNote」、仪表盘 h1、知识图谱 h1 等。
2. **半透明/图标型控件**：`.sidebar-collapse-btn`（28×28，`layout.css:110-123`，
   `color: var(--color-text-tertiary)`）、`.sidebar-item-icon` 等。
   它们多半带有 `opacity` 或叠加在非纯色上。

**最值得先看的一条**：`.sidebar-collapse-btn` 用的是 `--color-text-tertiary`
（#9a9ab0）—— 与 F-04 同色。若其背景确实是白色，它就是 **2.75:1**，
即又一个真实违规，只是 axe 没能确定背景而没报出来。

> **修复轮的实测答复**：这条猜对了成因、猜对了色值，**结论已经被修掉**。
> 在真 Chromium 里取计算样式：`.sidebar-collapse-btn` 的
> `color: rgb(111,111,138)`（= 新的 `--color-text-tertiary`），背景是侧边栏的
> `#ffffff`（它自己是 `background: transparent`）→ **4.86:1，达标**。
> 也就是说：这一条**不是"axe 看不见所以永远算不清"**，
> 花一次 `getComputedStyle` 就结案了 —— 值得推广给同类节点。
> ⚠️ 仍然没结案的是**渐变文字**那一类（`.sidebar-logo`、`.gradient-text`、
> 仪表盘 h1、知识图谱 h1）：它们的前景色是 `transparent`，取计算样式也拿不到
> "看起来是什么颜色"，必须按渐变两端色分别算，**本轮没有做**。
> 那 149 个 `incomplete` 节点里还剩多少这类，见每次运行的 JSON 附件。

**后续**：人工对照 `docs/design` 或直接在浏览器里取色算比值；
或引入能处理渐变的对比度工具。**不要**因为 axe 没报就当它没问题。

#### 4.3.1 axe **结构上**不下结论的第二种情况：单字符文本（覆盖轮新增）

上面那类是"背景算不出来"。覆盖轮在今日学习页撞到了**另一类**，读 axe-core 源码才看明白
（`node_modules/axe-core/axe.js` 的 `colorContrastEvaluate`，约 27277 行）：

```js
var shortTextContent = visibleText.length === 1;
...
} else if (!isValid && shortTextContent && !ignoreLength) {
  missing = 'shortTextContent';
}
...
if (fgColor === null || bgColor === null || equalRatio || shortTextContent && !ignoreLength && !isValid) {
  return void 0;   // → incomplete，而不是 violation
}
```

也就是说：**文本只有一个字符、且对比度不足时，axe 返回"不下结论"** ——
节点进 `incomplete`，永远**不会**变成违规。

今日学习页的三个**优先级徽章**（「高 / 中 / 低」正好各一个字）正是这种情况。
本轮把它们量了出来（真 Chromium，`getComputedStyle` + 逐层合成背景）：

| 徽章 | 前景 | 背景 | 实测比值 | 字号/字重 | 要求 | 结论 |
|---|---|---|---|---|---|---|
| 高（priority 1） | `rgb(255,255,255)` | `rgb(244,67,54)` | **3.68:1** | 11.2px / 400 | 4.5:1 | 不足 |
| 中（priority 2） | `rgb(255,255,255)` | `rgb(255,152,0)` | **2.16:1** | 11.2px / 400 | 4.5:1 | 不足 |
| 低（priority 3） | `rgb(255,255,255)` | `rgb(16,185,129)` | **2.54:1** | 11.2px / 400 | 4.5:1 | 不足 |

色值来自 `TodayLearn.tsx` 的 `priorityMeta`（`#f44336` / `#ff9800` / `#10b981`）+ 白字。
**这三条没有任何门禁**（axe 报不出来），数字随每次运行存进
`a11y-today-learn-badges.json` 附件。⇒ 记为 **F-36（实测，无门禁）**，
下一轮修色值时**同时**补一条基于 `measureContrast` 的断言，否则修完也没人守着。

> 这条经验可以推广：**凡是"一个字/一个字符"的状态徽章、角标、计数气泡，
> 都不要指望 axe 的 `color-contrast`** —— 它们会被静默归入"需人工确认"。

### 4.4 移动端触控目标尺寸

`dashboard-mobile` 场景（375×667）**0 条触控尺寸类违规** —— 不是因为尺寸没问题，
而是因为 **axe-core 4.13 根本没有这条规则**（WCAG 2.5.8 Target Size 是 AA，
但 axe 不实现几何判定）。这条用例里已把这一点写成断言
（`expect(touchRules).toEqual([])` —— 钉住的是"工具没有这个能力"这个事实），
并把导航项的实际 `boundingBox()` 存进 JSON 附件。

已知的具体疑点（手工看代码就能确认的）：
- `.sidebar-collapse-btn` **28×28 px**（`layout.css:114-115`）—— 低于常见的
  44×44 建议值，也低于 WCAG 2.5.8 的 24×24 下限之外更严的门槛；
- `SelfRatingButtons` 的四个档位在窄屏下是否够大（`SelfRatingButtons.module.css`
  里带 768/480 两条窄屏规则，本层没有针对它的几何断言）；
- 侧边栏导航项高度、笔记卡片上的删除按钮（`icon-btn` 一类）。

**后续**：写几何断言（`boundingBox()` 的宽高 ≥ 阈值），而不是等 axe。
这是本层**结构上**做不到的事。

### 4.5 其它明确不在本层范围

- **缩放 / 重排**（WCAG 1.4.4 / 1.4.10）：200% 缩放、320px 宽下的重排没有测；
- **动效**（`prefers-reduced-motion`）：项目里有 `fadeIn`、`slideIn` 等关键帧
  （`base.css:130+`）与 `transition`，有没有尊重用户的减动效设置没有测；
- **跨浏览器 / 跨屏幕阅读器**：只有 Chromium；没有 NVDA / VoiceOver 实测；
- **真实后端的错误文案**：本层 `/api` 全部被桩掉，所以"错误提示本身是否可理解"
  不在范围内（那属于后端契约与文案评审）。

---

## 5. 还没被任何一层验证的相邻风险（本轮审计顺带发现）

1. **`Review`（答题复习）未扫，但它与 `CardReview` 共用 `SelfRatingButtons`** ——
   F-19 的两个低对比度色值来自 `utils/labels.ts`，那里是**唯一数据源**，
   所以 `Review` 上的自评按钮几乎必然同样不足。这是**推断**，不是实测。
   修 `labels.ts` 时两页一起受益；顺带应该把 `Review` 加进扫描场景。
   > **修复轮的后续**：色值已改（`#8f7020` / `#25714a`），而且因为
   > `labels.ts` 是唯一数据源，`Review` 上的四档**同一次修复就生效了**。
   > 另外去真 Chromium 里实探了一次 `Review`（用桩喂了一道简答题）：
   > 该页 axe 无违规、`SelfRatingButtons` 正常渲染。**但它缺 h1**
   > （`<h1>`~`<h6>` 共 0 个）—— 见 §8.3 第 1 条。
   > **覆盖轮的收口**：`Review` 现在是**场景**（`review`），不再依赖人工探针。
   > 探针的结论被证实：`page-has-heading-one` 1 个节点（**F-32**）；
   > 顺带查出探针没看到的难度徽章对比度 **2.25:1**（**F-33**）。
2. **`DailyMaterials` / `TodayLearn` / `Upload` 也有 `role="button"` 的卡片**
   （`DailyMaterials.tsx:476/655`、`TodayLearn.tsx:342`、`Upload.tsx:391`）。
   本次没扫这些页面，但写法与 F-03/F-16/F-17 **逐字相同**，
   所以同类问题**大概率存在**。这是"用 grep 得到的高置信猜测"，不是审计结论。
   > **修复轮的决定：不动它们。** 理由不是"不严重"，而是"改了没有信号" ——
   > 这三个页面不在扫描场景里，改动既可能修好也可能改坏，**没有任何一层能验证**。
   > 正确次序是先把它们加进场景（或至少加进 `a11y.spec.ts` 的 `knownScenes`），
   > 再照 F-09/F-17 的同一个形状改。见 §8.3 第 4 条。
   > **覆盖轮的收口（这一步做完了）**：三个页面都进了场景，猜测**全部被证实**，
   > 但**分成了两类**，这个区分比猜测本身更有用：
   > - `role="button"` **容器里嵌真按钮** → `nested-interactive`（F-30 今日资料的文件夹头、
   >   F-34 今日学习的待复习卡片）—— **与 F-09/F-17 是同一个洞**；
   > - `role="button"` **容器里没有可聚焦后代** → axe **不报**（今日资料的资料行、
   >   上传页的拖拽区：它只 `aria-label` + 隐藏 `input[type=file]`）。
   >   它们没有 `nested-interactive`，但仍有"Tab 停在一个不是按钮的按钮上、
   >   只认 Enter 不认 Space"的问题 —— 那一类**axe 判不了**，仍在 §4.1 的人工清单里。
3. **卡片详情 `/cards/:cardId` 未扫**：桩里 `/api/cards/{id}` 返回的是列表形状，
   直接加进场景会得到"加载失败"页 —— 那扫的是错误态而不是卡片页，
   属于**假覆盖**，所以刻意没加。要覆盖它需要先补一个符合卡片详情契约的桩。
   > **覆盖轮之后仍然未扫**，理由不变（这条不是"没来得及"，是"补桩之前加了等于假覆盖"）。

---

## 6. 测试怎么判定"通过"（以及为什么不许空过）

### 判定尺度：登记表 + 上限

`e2e/a11y.spec.ts` 里的 `REGISTRY` 把当前**已知的每条违规**逐条写下来
（`id` + 规则 + 场景 + 影响等级 + 节点数 + 归属）。运行时：

1. 出现**未登记**的规则 → **失败**（这是真正在守的部分：新回归立刻红）；
2. 已登记规则的**节点数超过**登记上限 → **失败**（影响面扩大）；
3. 已登记规则的**影响等级高于**登记值 → **失败**（变严重）；
4. 同一（场景，规则）出现两条登记项 → **失败**（那种写法会让匹配悄悄失效）；
5. 登记表里有**没写归属**的条目 → **失败**（没有归属的豁免就是"忘了修"）。

**为什么只卡上限、不要求精确相等**：本文件是给修 bug 的人看的。若要求相等，
他每修掉一条就得同时改测试里的数字，很快就会出现"顺手把数字改大一点"，
门禁随即失效。只卡上限意味着**修好立刻生效、不需要改测试文件**；
而问题变多一定失败。代价是它**不会告诉你"某条已被修好"** ——
那件事由上面 §3 的表格（人工过一遍）负责。

### 防线：不许空过

一个坏掉的选择器、一次失败的懒加载、一个被桩吃掉的接口，都会让页面渲染成空白
—— 而**空白页面的 axe 结果恒为"0 违规"**。那种绿灯比红灯更糟。因此每次扫描都断言：

- 该页**独有**的渲染标记先出现（例如笔记列表要等到"共 2 条"**和**一条笔记标题都在，
  而不是只等一个标题 —— 空态也有标题）；
- DOM 元素数 ≥ 场景下限（实测值留 ~30% 余量）；
- axe 真的执行了（`passes > 0`、评估规则数 > 20）；
- 桩没有出现"未定义响应"的接口（`/api` 桩对未知路径回 **501** 并记录，
  因为页面普遍用 `.catch(() => null)` 把它吞掉）；
- 页面无未捕获异常（`pageerror`）。

另外两条**自检用例**：

1. **注入一个没有 `alt` 的 `<img>`，断言 axe 报出 `image-alt`** ——
   守的是"扫描链路本身还活着"；
2. **登记表自检**：场景名拼写正确（写错的后果是豁免永远匹配不到，
   而维护者会去加第二条豁免，越加越乱）、（场景，规则）唯一、每条都写了归属。

### 已知的稳定性注意点

- 并发由 project 固定为 4（见 §1）。8 worker 时 Vite 的按需转换会让首屏超 10 秒，
  表现为"页面没渲染出来"，与真实原因无关；
- 端口 4319 若被上一次异常退出的 dev server 占着，`--strictPort` 会**直接失败**
  （这是有意的：悄悄换端口会让 `webServer.url` 的健康检查探一个空地址）。
  遇到时先确认没有遗留进程，而不是改成 `reuseExistingServer: true`；
- **对比度节点数会随内容漂移**：`color-contrast` 的命中数取决于"页面上有几条
  状态徽章"，桩数据一变就可能多一个节点。登记表刻意留了一点余量（上限），
  但**如果它开始因无关的数据改动而红，正确的做法是把桩数据固定住，
  而不是把上限调大**。
  > **修复轮之后这条暂时用不上了**：`color-contrast` 在 9 个场景里
  > 一个节点都不剩，登记项已全部删除 —— 现在任何一条对比度违规（哪怕 1 个节点）
  > 都会以"未登记"失败。也就是说**漂移的容忍度从"有上限"变成了 0**。
  > 这是有意的：真要再出现，先看是页面真的退化了还是桩数据变了，
  > 两种都不该用"加上限"解决。
  > **覆盖轮之后的状态（三句话）**：
  > ① **10 个旧场景仍然是零容忍** —— 一条对比度违规都没有因为它们没变而被放宽；
  > ② 4 个新场景里 `color-contrast` 有登记项（F-28/F-29/F-33/F-35），
  >    **上限一律 = 实测值**（2/2/1/2），多一个节点就失败；
  > ③ 这些登记项是**债**：修好即自动生效（不需要改测试），删掉条目才算清账（§9.3）。
- **`minNodes` 的下限来自实测值**：新场景的下限写在各自的注释里（实测值留 ~30% 余量）。
  ⚠️ 10 个旧场景注释里的实测值（48 / 143 / 208 / 158 …）是**修复轮之前**那一版
  （与 §2 的历史表逐字相同），修复轮改了 DOM（补 `<main>`、补 `<h1>`）之后没刷新过 ——
  现在实测是 49 / 146 / 210 / 159，**下限仍然成立且留有余量**，所以本轮没有动它们
  （改注释数字不是覆盖轮该做的事）。要核这件事只需看下限与实测的差：全都 ≥ 20%。
- **端口现在可配**：`E2E_PORT=4321 npm run e2e`（默认 4319；**3080 被显式禁止**，
  写错会当场抛错）。两个 agent 同时跑 Playwright 时用这个换端口，
  而不是把"端口被占"当成测试失败。

### 排查用的一手线索

`e2e/a11y-fixtures.ts` 里的桩会把**每个 `/api` 请求**记进 `A11yStubLog.served`，
并把浏览器控制台的 error/warning 记进 `consoleErrors`。要逐条打到测试输出，
把该文件里的 `const TRACE = false` 改成 `true` 再跑一次即可
（不做成环境变量：`tsconfig.json` 的 `include` 含 `e2e` 而 `lib` 里没有
`@types/node`，写 `process.env` 会让 `npm run build` 报 TS2580）。

这套线索真的用上过一次：8 worker 并发时"页面卡在加载中"，
`served` 立刻显示**所有接口都回了 200**，于是问题被定位到开发服务器吞吐，
而不是接口或前端代码。

---

## 7. 与 CI 的关系

`.github/workflows/ci.yml` 的 `frontend` job 里，在 `npm run e2e` **之后**加了一步
（**只加了一步，没有改动任何现有 job 的结构**）：

```yaml
- name: Playwright a11y audit (axe-core, advisory)
  continue-on-error: true
  timeout-minutes: 10
  run: npm run a11y
```

> 同一份改动还让 `npm run e2e` 变成 `playwright test --project=chromium`
> （= 功能层 10 条，**不含**审计）—— 这是"审计单独一个 project"的必要配套：
> 不加 `--project` 时 Playwright 会把两个 project 都跑（共 25 条，
> 只是想跑功能层的人不该被塞进 15 条审计）。想两者一起跑用 `npm run e2e:all`。

**为什么是建议性（advisory）而不是阻断性：**

1. 审计当场发现了一批真实违规，修复属于后续的 5.9 修复轮。现在设成阻断，
   CI 会**长期常红**，而常红的门禁只有一个结局：被人加 `|| true` 或删掉 ——
   那比没有门禁更糟，因为它制造"我们已经在管无障碍了"的假象
   （这与本仓库 `security-scan` job 的取舍是同一个理由）。
2. `frontend/src/**` 正在被并行的 CSS 迁移改动。审计对**对比度**与**类名**敏感，
   迁移期间更容易因无关改动变红，而那种红的指向不是"这次提交引入了缺陷"。

**它仍然是有信号的一步**（不是 `|| true` 那种装饰）：审计本身会把未登记的违规
判为失败，所以这一步在 CI 日志里会明确显示"当前是否有**新**的无障碍回归"。
`continue-on-error` 只影响"是否让整个 job 变红"，不影响它跑、也不影响它报。

**升级成阻断的前置条件**（写在这里避免"以后再说"）：

1. 5.9 修复轮把 `REGISTRY` 清空或降到可接受集合；
2. `frontend/src/**` 的 CSS 迁移结束。

两条都满足后，把那一行 `continue-on-error: true` 删掉即可 —— 门槛本身已经在
测试里了，不需要另写脚本。

> **修复轮之后的进度**：第 1 条的"降到可接受集合"已经完成 ——
> `REGISTRY` 从 **30 条 / 68 个节点降到 4 条 / 4 个节点**，且剩下的 4 个节点
> 全部是同一个 **(b) 待人工决定**的问题（标题层级）。第 2 条（CSS 迁移）
> 不属于本工作流，**所以 CI 这一步现在仍然是 advisory** —— 改它需要同时
> 确认迁移状态，那是另一个 agent 的范围。
>
> **覆盖轮之后的进度**：`REGISTRY` 变成 **12 条 / 15 个节点**（+8 条 / +11 个节点，
> 全部在新场景上），其中 7 条是 (a) 类、1 条是 (b) 类 —— 也就是说第 1 条前置条件
> **被这次覆盖推进了反方向**："清空或降到可接受集合"还没有达成，
> 而且现在**明确知道还差什么**（§9.3 的债务清单）。
> 第 2 条（CSS 迁移）**仍在进行中**（同一 workspace 里有并行的迁移 agent），
> 所以 CI 这一步**保持 advisory**，本轮刻意**没有**碰 `.github/**`。

---

## 8. 修复轮（5.9 的后一半）：做了什么、数字变了多少

**执行原则**（来自上一轮留下的交接口径，逐条遵守）：

1. **修好一条不需要改测试** —— `REGISTRY` 卡的是上限，所以违规归零自动生效；
2. **新违规让审计变红是设计如此，不是"把测试放宽"的理由** ——
   本轮真的撞上一次（见 §8.4），处理方式是**改代码**而不是加上限；
3. **对比度节点数随桩数据漂移时，修桩数据而不是改上限** —— 本轮没有出现漂移；
4. 改 `--color-text-tertiary` 这类 16 处引用的令牌，必须复跑 9 个场景确认无连带回归；
5. 每修一条都在 §3 的表格里标注。

### 8.1 逐场景前后对照（`npm run a11y`，同一台机器）

| 场景 | 修复前：违规规则 / 节点 | 修复后：违规规则 / 节点 | 消掉的规则 |
|---|---|---|---|
| `login` | 2 / 7 | **0 / 0** | `landmark-one-main`、`region` |
| `login-error` | 2 / 7 | **0 / 0** | 同上（失败态是另一个渲染分支，单独验证） |
| `dashboard` | 4 / 9 | 1 / 1 | `aria-allowed-role`、`nested-interactive`、`color-contrast` |
| `dashboard-mobile` | 4 / 6 | 1 / 1 | `aria-allowed-role`、`nested-interactive`、`color-contrast` |
| `notes-list` | 4 / 10 | **0 / 0** | `aria-allowed-role`、`nested-interactive`、`page-has-heading-one`、`color-contrast` |
| `note-detail` | 4 / 8 | 1 / 1 | `select-name`、`link-in-text-block`、`color-contrast` |
| `card-review-front` | 3 / 5 | **0 / 0** | `page-has-heading-one`、`link-in-text-block`、`color-contrast` |
| `card-review-back` | 3 / 7 | **0 / 0** | 同上（翻面后是另一个渲染分支） |
| `knowledge-graph` | 2 / 5 | **0 / 0** | `select-name`、`color-contrast` |
| `projects` | 2 / 4 | 1 / 1 | `color-contrast` |
| **合计** | **30 组 / 68 个节点** | **4 组 / 4 个节点** | 26 组规则、64 个节点消失 |

剩下的 4 个节点全是 `heading-order`（F-07 / F-22 / F-20 / F-08），
全部是 §3 里标 (b) 的**待人工决定**项。**没有一个 (a) 类问题残留。**

### 8.2 登记表随之收紧（**只降过，从未放宽**）

| 登记项 | 原上限 | 新上限 | 说明 |
|---|---|---|---|
| F-07 `heading-order` / dashboard | 1 | **1（不变）** | 待人工决定，维持最紧 |
| F-22 `heading-order` / dashboard-mobile | 1 | **1（不变）** | 同上 |
| F-08 `heading-order` / note-detail | 1 | **1（不变）** | 同上 |
| F-20 `heading-order` / projects | 1 | **1（不变）** | 同上 |
| F-01 / F-02 / F-01b / F-02b | 1 / 6 / 1 / 6 | **删除** | 登录页已补 `<main>` |
| F-03 / F-21 / F-16 `aria-allowed-role` | 2 / 2 / 2 | **删除** | `article[role=button]` 已去角色 |
| F-09 / F-23 / F-17 `nested-interactive` | 1 / 1 / 2 | **删除** | 卡片里不再嵌可聚焦元素 |
| F-04 / F-24 / F-25 / F-12 / F-19 / F-19b / F-05 / F-27 `color-contrast` | 5 / 2 / 6 / 5 / 5 / 3 / 4 / 3 | **删除** | 令牌与四处色值已改，9 个场景一个节点都不剩 |
| F-10 / F-11 `select-name` | 1 / 1 | **删除** | 两个 `<select>` 有可访问名了 |
| F-13 / F-26 / F-26b `link-in-text-block` | 1 / 1 / 1 | **删除** | 链接默认带下划线 |
| F-14 / F-15 / F-18 `page-has-heading-one` | 1 / 1 / 1 | **删除** | 三处都有一级标题了 |

**"删除"为什么不是"放宽"**：`reconcile()` 对**没登记的规则**判 `unregistered`
→ 失败。删掉登记项等于把该规则从"豁免"移回"零容忍"：
节点数只要不是 0 就红。这是**收紧到最紧**，不是放行。
（`REGISTRY` 里同时删掉了随之失去引用的 `SHELL_SIDEBAR_NODES` /
`SHELL_STATUS_NODES` 两个常量。）

### 8.3 本轮**刻意没修**的，以及新发现

**(b) 待人工决定（4 个节点，全部登记在案，见 §3）**：
`F-07`/`F-22`（仪表盘两栏区块没有区块级标题）、`F-20`（项目页同类）、
`F-08`（笔记详情的标题来自用户 Markdown + 页面自己的"清洗统计"区块）。
它们需要"这两组区块该叫什么名字""要不要归一化用户内容的标题级别"这类回答，
不是这一轮能替产品拍板的。

**新发现（不在原审计的 9 个场景里，因此不进计数，但记在这里免得丢）**：

1. **答题复习页 `Review` 没有任何标题**：实测 `<h1>`~`<h6>` 数量为 **0**，
   所以它同时缺一级标题与页面结构。`ReviewProgress` 在那里走"一行式"排版
   （不传 `title`），本次改动**碰不到它** —— 这是既有缺口，
   要修得先决定"这一页的标题是什么"。（顺带说明：`SelfRatingButtons` 的
   四档对比度在那里已经随 `labels.ts` 一起修好，因为色值只有那一份数据源。）
2. **`--color-warning`（`#c4860a`）白底只有 3.11:1**：它用在
   `.status-converting` / `.status-cleaning` 两个状态徽章上，而桩数据里
   没有这两种状态（只有 `cleaned` / `converted`），所以 9 个场景都扫不到。
   它与 F-06 是同一类问题（状态徽章小字），修法也一样（压深一档），
   但**本轮没有改**：改它需要先定新值，且没有场景能验证回归。
   建议下一轮连桩数据一起补（加一条 `converting` 状态的笔记）。
3. **`.sidebar-collapse-btn`（28×28）** 用的是 `--color-text-tertiary`：
   axe 一直把它列在 `incomplete`（"算不出背景色"），本轮用真 Chromium
   取到计算值 —— `color: rgb(111,111,138)`、背景是侧边栏的 `#ffffff`，
   即 **4.86:1**，**已不再是问题**（原值 `#9a9ab0` 是 2.75:1）。
   它的**触控目标 28×28** 仍然小于 44×44 的建议值 —— 那是 §4.4 的人工清单项。
4. **审计未覆盖页面上的 `role="button"` 卡片仍然存在**：§5 第 2 条列过
   `DailyMaterials.tsx:476/655`、`TodayLearn.tsx:342`、`Upload.tsx:391`。
   本轮**没有动它们**：它们的写法与 F-03/F-16/F-17 逐字相同，
   但那三个页面不在扫描场景里，改了**没有任何一层能验证**（既可能修好、
   也可能改坏而没有信号），不符合"改了要有证据"的口径。下次把它们加进场景时一起改。

> **覆盖轮把这 4 条全部接上了（本轮就是"下次"）**：
> ①②④ 变成了场景与新登记项（`Review` → **F-32/F-33**；`converting`/`cleaning`
> → **F-28/F-29**；三个页面 → **F-30/F-31/F-34/F-35**），
> ③ 仍然是"已结案、但不是门禁"（`sidebar-collapse-btn` 的 4.86:1 与 28×28 触控尺寸）。
> 也就是说：**这一轮的输入正好是上一轮留下的"没有信号"清单**，
> 而现在这份清单上的每一条要么有了门禁、要么有了实测数字。逐条见 §9。

### 8.4 本轮踩到的两个坑（都值得记住）

1. **补 h1 会立刻引出 `heading-order`。** F-18（笔记列表缺 h1）与
   F-14/F-15（卡片复习缺 h1）按审计的建议各补了一个 `h1`，
   第一次跑审计就红了 **2 条新违规**：`h1 → h3` 跳级。
   这正是登记表的设计在起作用（新规则 = `unregistered` = 失败）。
   处理是**改代码**（把卡片标题 `h3` → `h2`，字号显式钉住、像素不变），
   不是往上加登记项 —— "把新违规登记下来"会让这一轮等于没修。
2. **DOM 层级是断言的一部分，但"数第几层"不是。**
   给登录页补 `<main>` 只是把内容**包起来**，卡片本身一个像素没动，
   而 `e2e/app-shell.spec.ts` 里那条 `xpath=../..`（从 h1 往上数两层）
   就从 `.authCard` 滑到了 `.authBg`。
   ⚠️ 更值得记住的是它**当时还是绿的**：两层恰好都写着 `min-height: 100vh`，
   所以断言"碰巧"通过 —— 那种绿灯比红灯更危险（它守的对象已经错了）。
   已改成按**判据本身**找："从 h1 往上，第一个 `min-height` 等于视口高的祖先"。
   改完做了一次反向验证（把期望值写成 `721px`，确认它确实报
   `Received: "720px"`），证明这条断言仍然是有载荷的。

---

## 9. 覆盖轮：把"没有信号"的地方变成信号（**本轮没有改应用代码**）

**这一轮做什么、不做什么**（口径来自上一轮的交接口径）：

- **做**：把"从没被渲染过、因此没有任何门禁能判"的状态与页面加进扫描场景，
  每个场景都喂**非空**的桩数据（空页面恒为 0 违规，那是假绿），
  ready 标记必须是"只有真实内容渲染出来才出现"的东西；
- **不做**：**不改 `frontend/src/**`** —— 并行的 CSS 迁移正在改那些文件，
  这时改应用代码**无法被验证**（既可能修好也可能改坏而没有信号），而且会与迁移冲突。
  所以新出现的违规一律**逐条登记 + 写明归属**，**下一轮**连同应用代码一起修；
- **不改测试的判定尺度**：新违规让审计变红是设计如此（§6），
  本轮的处置是"登记 + 写清理由"，**不是**放宽上限。

### 9.1 新增的 5 个场景（怎么渲染出真实内容 / ready 标记 / 实测 DOM）

| 场景 | 页面 | 喂了什么桩（真实内容从哪来） | ready 标记（只有真实内容在才成立） | 实测 DOM | `minNodes` |
|---|---|---|---|---|---|
| `notes-status-processing` | `/notes` | **覆盖** `/api/notes`：2 条状态为 `converting` / `cleaning` 的笔记（默认桩里只有 `cleaned`/`converted`） | 「共 2 条」+ 两条**本场景专有标题** + `.status-converting` / `.status-cleaning` **各 1 个**（类名就是本场景存在的理由） | 145 | 100 |
| `daily-materials` | `/daily` | `/api/folders` 从 `[]` 改成 1 个文件夹；新增 `/api/folders/folder-1`（3 条笔记，含两种处理中状态） | h1「今日资料」+「上传文件」按钮 + 展开后文件夹内两条**专有标题**（展开是一次真实点击） | 157 | 110 |
| `today-learn` | `/today` | **覆盖** `/api/goals/daily-plan`（400 → 有 3 类任务的计划）、**覆盖** `/api/report/weak-points`（改成契约形状） | h1「今日学习」+ h2「每日推荐任务」+ 任务标题 + h2「今日报告 (2026-01-06)」+「今日待复习: 12 题」+ h2「薄弱点」+「错3次 \| 0.4%」 | 171 | 120 |
| `upload` | `/upload` | 无需新桩（`/api/projects` 已是非空） | h1「上传学习资料」+ 拖拽区（`role=button` 的可访问名）+ 项目标签「蓄电池基础」+「云端解析」 | 128 | 90 |
| `review` | `/review` | **新增** `/api/review/due`（此前**没有桩** → 页面停在错误分支，扫的不是这一页），2 道题（choice/medium + fill_blank/easy） | 题干 + 一个选项按钮 + 「已复习 3 次 \| 间隔 6 天」（来自桩字段） | 126 | 85 |

**桩本身也补了 3 处**（都在 `e2e/a11y-fixtures.ts`）：

1. `/api/review/due` —— 见上（这是 `Review` 从来没被扫过的**真实原因**：
   接口 501 → 页面走 `error && quizzes.length === 0` 分支，渲染一行报错 + 一个「重试」）；
2. `/api/folders` + `/api/folders/folder-1` —— 今日资料页的输入；
3. 三个**只在某场景生效的导出**（`PROCESSING_NOTES` / `DAILY_PLAN` / `WEAK_POINTS`）
   通过 `installA11yStubs(page, overrides)` 传入 —— 用覆盖而不是改默认值，
   是为了**不移动已扫场景的基线**（默认 `/api/notes` 仍然是 2 条，
   `notes-list` 的「共 2 条」断言因此原样成立）。

**未匹配桩的断言（501）仍然有效**：新场景跑完后 `log.unmatched` 仍为 `[]`，
也就是说这些页面请求的每个 `/api` 路径都有明确响应 —— 没有靠"静默空响应"混过去。

### 9.2 新发现（逐条判定与归属，见 §3 的表；这里只写"为什么以前看不见"）

1. **`converting` / `cleaning` 的颜色（F-28 / F-29）** —— 以前看不见的原因是
   **桩数据里没有这两种状态**。类名由 `utils/labels.ts` 的 `statusClass()` 从
   `note.status` 映射而来（单一数据源），两个类都用 `--color-warning`；
   现在它们被渲染出来了。**实测**（真 Chromium，`getComputedStyle` + 逐层合成背景，
   随每次运行存进 `a11y-notes-status-contrast.json`）：

   | 徽章 | 前景 | 有效背景 | 背景来自 | 实测比值 | 字号/字重 | 要求 |
   |---|---|---|---|---|---|---|
   | 「转换中」`.status-converting` | `rgb(196,134,10)`（`#c4860a`） | `rgb(255,255,255)` | `article.card.card-hover.note-list-item` | **3.11:1** | 12.8px / 400 | 4.5:1 |
   | 「清洗中」`.status-cleaning` | 同上 | 同上 | 同上 | **3.11:1** | 12.8px / 400 | 4.5:1 |

   axe 自己报的也是 **3.1**（`insufficient color contrast of 3.1`，两个节点）。
   修法有先例（F-06 压深 `--color-success`），**一处改、两处消**（F-28 + F-29）。
2. **`DailyMaterials` 的结构问题（F-30 / F-31）** —— 以前看不见的原因是
   **这一页不在场景里**。文件夹头是"`role="button"` 容器里嵌真按钮"，
   与已修的 F-09/F-17 **逐字相同**；h1 → h3 与 F-07/F-20 同类。
3. **`TodayLearn` 的两个问题（F-34 / F-35）** —— 同样因为不在场景里。
   F-34 与 F-09 逐字相同；F-35 是"同一处代码在两页上的复现"：
   `Dashboard.tsx:311-318` 有一模一样的红字徽章，**但仪表盘上扫不出来** ——
   默认桩的 `/api/report/weak-points` 写的是 `title` / `review_count`，
   而 `WeakPoint` 契约要的是 `card_title` / `error_count`，于是页面上渲染的是
   `undefined` 文本 + **空徽章**：**空文本没有对比度可判**。
   ⚠️ 这是本轮最值得记住的一条**覆盖陷阱**：*桩的字段名写错不会让页面崩，
   只会让 axe 无话可说* —— 看起来扫过了，实际没有被判过。
   本轮**没有改默认桩**（那会移动 `dashboard` / `dashboard-mobile` 的基线），
   只在 `today-learn` 场景里用契约形状的那一份，让同一个卡片**至少在一处**有真内容。
   **下一轮把默认桩一起改对**，否则仪表盘上那条永远看不见。
4. **`Review` 缺 h1（F-32）** —— 上一轮是**人工探针**的结论（§8.3 第 1 条），
   现在它是一条门禁。探针结论被证实：1 个节点、`page-has-heading-one`。
5. **`Review` 的难度徽章（F-33）** —— 探针当时没看出来的一条：
   白字压 `#c9a959` = **2.25:1**。它与已修的 F-19 是**同一个色值**：
   上一轮把 `selfRatingOptions` 里的金压深成 `#8f7020` 了，
   而 `labels.ts` 里**另有 5 张颜色表仍然写着这批旧值**
   （`cardTypeColors`、`questionTypeColors`、`difficultyColors`、`cardCategoryColors`、
   `verdictLabels`：`#c9a959` / `#2d8a56` / `#4caf50` / `#f44336`）。
   本场景实测到的是其中一处（`difficultyColors.medium` + 白字）——
   其余四处**仍未实测**（要靠 `KnowledgeCards` / `QA` / `QuickReview` 那些页面，
   它们还是未覆盖页面，见 §9.5 末尾）。这正是"修一处、别处照旧"的代价。

### 9.3 登记表新增的 8 条：**每一笔都是债**

| 登记项 | 场景 / 规则 | 上限（= 实测值） | 下一轮怎么清 |
|---|---|---|---|
| **F-28** | `notes-status-processing` / `color-contrast` | 2 | 压深 `--color-warning`（一处令牌） |
| **F-29** | `daily-materials` / `color-contrast` | 2 | 同一处令牌改完自动消 |
| **F-30** | `daily-materials` / `nested-interactive` | 1 | 照 F-09 的形状：外层去 role/tabIndex，行为落在真按钮上 |
| **F-31** | `daily-materials` / `heading-order` | 1 | **需要设计决定**（区块标题叫什么），不是 5.9 单独能清 |
| **F-32** | `review` / `page-has-heading-one` | 1 | 补 h1（注意 `h1 → h3` 跳级的坑，§8.4） |
| **F-33** | `review` / `color-contrast` | 1 | 把 `labels.ts` 里"白字压色块"的取值一次过一遍 |
| **F-34** | `today-learn` / `nested-interactive` | 1 | 照 F-09 的形状（与 F-30 同一处修法） |
| **F-35** | `today-learn` / `color-contrast` | 2 | 改那个字面量 + **把默认桩的字段名改对**（否则仪表盘那份仍看不见） |

**共同点**：上限一律 = 实测值（不留余量），`reason` 里都写了"为什么还在 + 谁去修"，
以及**"下一轮修掉后删除本条目"**。删掉即回到零容忍（该规则只要 1 个节点就红）——
这与 §8.2 的"删除 = 收紧到最紧"是同一条规矩。

**没有放宽任何既有登记项**：`F-07` / `F-22` / `F-08` / `F-20` 的上限仍然全是 **1**，
一个字都没动；10 个旧场景里**一条违规数都没变**（见 §9.4 的对照表）。

### 9.4 逐场景数字（`npm run a11y` 实测，同一台机器）

| 场景 | DOM | 评估规则 | 通过 | 违规规则 / 节点 | 需人工确认 | 状态 |
|---|---|---|---|---|---|---|
| `login` | 49 | 89 | 30 | 0 / 0 | 8 | 与修复轮一致 |
| `login-error` | 50 | 89 | 34 | 0 / 0 | 9 | 同上 |
| `dashboard` | 210 | 91 | 35 | 1 / 1（F-07） | 22 | 同上 |
| `notes-list` | 146 | 90 | 33 | 0 / 0 | 16 | 同上 |
| `note-detail` | 159 | 91 | 36 | 1 / 1（F-08） | 13 | 同上 |
| `card-review-front` | 123 | 90 | 31 | 0 / 0 | 13 | 同上 |
| `card-review-back` | 140 | 90 | 31 | 0 / 0 | 12 | 同上 |
| `knowledge-graph` | 183 | 90 | 31 | 0 / 0 | 23 | 同上 |
| `projects` | 139 | 91 | 29 | 1 / 1（F-20） | 14 | 同上 |
| `dashboard-mobile` | 210 | 91 | 36 | 1 / 1（F-22） | 11 | 同上 |
| **`notes-status-processing`**（新） | 145 | 91 | 33 | **1 / 2**（F-28） | 16 | 新增 |
| **`daily-materials`**（新） | 157 | 93 | 30 | **3 / 4**（F-29、F-30、F-31） | 19 | 新增 |
| **`today-learn`**（新） | 171 | 92 | 29 | **2 / 3**（F-34、F-35） | 21 | 新增 |
| **`upload`**（新） | 128 | 90 | 30 | **0 / 0** | 15 | 新增 |
| **`review`**（新） | 126 | 91 | 24 | **2 / 2**（F-32、F-33） | 12 | 新增 |
| **合计** | — | — | **472** | **12 组 / 15 个节点** | **224** | — |

**旧场景 = 零变化**（这是本轮最要紧的一栏）：上表前 10 行的违规数与 §8.1 修复轮
的结论**逐个相同**（`4 组 / 4 个节点`），登记项也逐个命中（打印里的
`[已登记] F-07` / `F-08` / `F-20` / `F-22`）。
DOM 元素数与各场景注释里写的旧值差 +1~+3（49 vs 48、146 vs 143、210 vs 208、159 vs 158）——
**那不是本轮改的**：那些注释里的数字取自 §2 的历史表（修复轮之前），
而修复轮本身改了 DOM（补 `<main>`、补 `<h1>`）；本轮只动了 `/api/folders`（只有
今日资料页请求它）与新增的 `/api/review/due`，**没有碰任何旧场景的输入**，
`notes-list` 的「共 2 条」断言原样通过就是证据。下限仍然全部成立（差值 ≥ 20%）。

**4 个新场景里的 0**：`upload` 是唯一一个"扫了但一条违规都没有"的新场景 ——
它是本轮唯一一个**把新页面扫干净**的记录（拖拽区有 `aria-label`、
三个设置卡片用的是 `p` 而非标题，所以也没有层级问题）；
⚠️ 但它的 15 个"需人工确认"节点里有一部分是拖拽区的半透明背景 —— **不是通过**（§4.3）。

### 9.5 现在覆盖了、以前没有覆盖的东西（一句话清单）

1. **`converting` / `cleaning` 两个状态的颜色**（`--color-warning`）—— 以前 10 个旧场景
   一次都没渲染过，现在有门禁 + 实测值（3.11:1）；
2. **`DailyMaterials`**（今日资料：文件夹列表 + 展开后的资料行 + 上传/筛选控件）
   —— 以前整页不在审计范围；
3. **`TodayLearn`**（今日学习：推荐任务三类卡片 + 今日报告统计 + 待复习 + 薄弱点）
   —— 含 axe **判不了**的单字符优先级徽章（实测 3.68/2.16/2.54:1）；
4. **`Upload`**（上传页：拖拽区 + 解析方式 + 项目标签 + 笔记类型）—— 以前整页不在范围；
5. **`Review`（答题复习）** —— 以前连"打开的是这一页"都不成立（接口没桩 → 错误分支），
   现在有门禁：缺 h1（F-32）与难度徽章对比度（F-33）。
6. **外加一条"覆盖陷阱"的证据**：`weak-points` 桩的字段名与契约不一致，
   会让仪表盘上的同一块内容**看起来扫过了、实际没有被判过**（§9.2 第 3 条）——
   这是本轮唯一一条"旧场景里也存在、但本轮刻意不移动基线"的已知盲区，
   已写进下一轮的债务清单。

**仍然没有覆盖**：`QA`、`KnowledgeCards`、`CardDetail`（需要先补桩，否则是假覆盖）、
`LearningAssessment`、`LearningGoals`、`Trash`、`QuickReview`、`Register`、404、`QuestionSets`。
`QuickReview` 与 `QA` 共用 `QuizAnswerCard`，所以 F-33 **大概率也在那里** —— 推断，不是实测。


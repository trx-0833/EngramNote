<!-- 由 scripts/gen-migration-evidence.mjs 生成，请勿手改 -->

# 5.6 迁移前：第三批（learning 按归属拆分 + dashboard 余下 → StatCard）的规则清单

三个样式表各自只列出**本批搬走**的类名对应的规则。修订由
`css-rule-inventory.mjs` **按内容自动定位**（从 HEAD 往回找第一个还含有
这些类名的提交），所以清单头部的 `@HEAD~N` 会随提交数变化，规则内容不会。

`dashboard.css` 的 `.progress-bar*`（3 条）与 `learning.css` 的
`.filter-pill*` / `.segment-*` / `.collapse-arrow*` / `.state-*` / `.spinner`
**留全局**，不在清单里 —— 判据（grep 出的文件数）写在两个样式表的文件头。

## src/styles/learning.css

```
# learning.css @HEAD~19 classes=qa-user-bubble|qa-ai-card|upload-zone|upload-zone-active|search-input-wrapper|search-input-icon —— 9 条规则
.qa-user-bubble { background: var(--gradient-primary); color: white; padding: var(--space-sm) var(--space-md); border-radius: 16px 16px 4px 16px; max-width: 70%; display: inline-block; animation: slideUp 0.3s var(--ease-out-expo) }
.qa-ai-card { border-left: 3px solid var(--color-accent); animation: slideUp 0.3s var(--ease-out-expo) }
.upload-zone { border: 2px dashed var(--color-border); border-radius: var(--radius-lg); padding: var(--space-2xl); text-align: center; cursor: pointer; transition: all 0.3s var(--ease-out-expo); background: var(--color-surface); position: relative; overflow: hidden }
.upload-zone:hover { border-color: var(--color-primary); background: var(--color-primary-light) }
.upload-zone-active { border-color: var(--color-accent); background: var(--color-accent-light); box-shadow: var(--shadow-glow) }
.upload-zone-active::after { content: ''; position: absolute; inset: 0; border-radius: var(--radius-lg); animation: glowPulse 1.5s ease-in-out infinite; pointer-events: none }
.search-input-wrapper { position: relative; max-width: 400px }
.search-input-wrapper input { padding-left: 36px; border-radius: var(--radius-md); background: var(--color-surface) }
.search-input-icon { position: absolute; left: 12px; top: 50%; transform: translateY(-50%); width: 16px; height: 16px; color: var(--color-text-tertiary); pointer-events: none }
```

## src/styles/responsive.css

```
# responsive.css @HEAD~19 classes=list-toolbar|search-input-wrapper|stat-number —— 3 条规则
@media (max-width: 480px) .list-toolbar { flex-wrap: wrap; row-gap: var(--space-xs) }
@media (max-width: 480px) .list-toolbar .search-input-wrapper { flex: 1 1 100%; max-width: none }
@media (max-width: 480px) .stat-number { font-size: 1.5rem }
```

## src/styles/dashboard.css

```
# dashboard.css @HEAD~19 classes=stat-card|stat-card-blue|stat-card-green|stat-card-gold|stat-card-purple|stat-number|stat-label —— 9 条规则
.stat-card { text-align: center; padding: var(--space-lg) var(--space-md); border-radius: var(--radius-md); background: var(--color-surface); border: 1px solid var(--color-border-light); box-shadow: var(--shadow-sm); transition: transform 0.3s var(--ease-out-expo), box-shadow 0.3s ease; position: relative; overflow: hidden }
.stat-card::before { content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px; border-radius: var(--radius-md) var(--radius-md) 0 0 }
.stat-card-blue::before { background: var(--gradient-primary) }
.stat-card-green::before { background: linear-gradient(135deg, #2d8a56, #34d399) }
.stat-card-gold::before { background: var(--gradient-gold) }
.stat-card-purple::before { background: linear-gradient(135deg, #6d28d9, #a78bfa) }
.stat-card:hover { transform: translateY(-2px); box-shadow: var(--shadow-lg) }
.stat-number { font-size: 1.75rem; font-weight: 700; font-family: var(--font-serif); background: var(--gradient-primary); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text }
.stat-label { font-size: 0.85rem; color: var(--color-text-secondary); margin-top: var(--space-xs) }
```

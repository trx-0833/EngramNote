<!-- 由 scripts/gen-migration-evidence.mjs 生成，请勿手改 -->

# 5.6 迁移前：第一批（auth / cleaning / diff / dashboard 私有部分）的规则清单

五个样式表各自只列出**本批搬走的那些类名**对应的规则（留在全局的
`.stat-card*` / `.stat-label` / `.progress-bar*` 不在清单里，它们没有搬）。
修订由 `css-rule-inventory.mjs` **按内容自动定位**（第一个还含有这些类名的
提交），所以清单头部的 `@HEAD~N` 会随提交数变化，规则内容不会。

## src/styles/auth.css

```
# auth.css @HEAD~25 classes=auth-bg|auth-card|auth-title|auth-input-group|auth-input-icon|auth-submit|auth-footer —— 16 条规则
.auth-bg { min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: var(--space-md); background: var(--gradient-hero); position: relative; overflow: hidden }
.auth-bg::before { content: ''; position: absolute; top: -50%; right: -30%; width: 80%; height: 80%; border-radius: 50%; background: radial-gradient(circle, rgba(201, 169, 89, 0.08) 0%, transparent 70%); pointer-events: none }
.auth-bg::after { content: ''; position: absolute; bottom: -40%; left: -20%; width: 60%; height: 60%; border-radius: 50%; background: radial-gradient(circle, rgba(15, 52, 96, 0.15) 0%, transparent 70%); pointer-events: none }
.auth-card { background: rgba(255, 255, 255, 0.92); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); border: 1px solid rgba(255, 255, 255, 0.4); border-radius: var(--radius-lg); padding: var(--space-2xl); box-shadow: var(--shadow-xl); width: 100%; max-width: 420px; animation: scaleIn 0.5s var(--ease-out-expo); position: relative; z-index: 1 }
.auth-title { font-family: var(--font-serif); font-size: 1.75rem; font-weight: 700; text-align: center; margin-bottom: var(--space-lg); background: var(--gradient-primary); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text }
.auth-input-group { position: relative; margin-bottom: var(--space-md) }
.auth-input-group label { display: block; margin-bottom: var(--space-xs); font-weight: 500; font-size: 0.875rem; color: var(--color-text-secondary) }
.auth-input-group input { padding-left: 40px; border-radius: var(--radius-sm); background: rgba(255, 255, 255, 0.8) }
.auth-input-icon { position: absolute; left: 12px; bottom: 11px; width: 18px; height: 18px; color: var(--color-text-tertiary); pointer-events: none }
.auth-submit { width: 100%; padding: var(--space-md); font-size: 1rem; font-weight: 600; border-radius: var(--radius-sm); background: var(--gradient-primary); color: white; box-shadow: 0 4px 16px rgba(15, 52, 96, 0.3); transition: all 0.3s var(--ease-out-expo) }
.auth-submit:hover { box-shadow: 0 6px 24px rgba(15, 52, 96, 0.4); transform: translateY(-1px) }
.auth-submit:active { transform: translateY(0) }
.auth-footer { margin-top: var(--space-lg); text-align: center; font-size: 0.875rem; color: var(--color-text-secondary) }
.auth-footer a { color: var(--color-accent); font-weight: 500; position: relative }
.auth-footer a::after { content: ''; position: absolute; bottom: -2px; left: 0; width: 0; height: 1px; background: var(--color-accent); transition: width 0.3s var(--ease-out-expo) }
.auth-footer a:hover::after { width: 100% }
```

## src/styles/cleaning.css

```
# cleaning.css @HEAD~25 classes=cleaning-panel|cleaning-stats|cleaning-progress|cleaning-progress-bar|duplicate-blocks|duplicate-block|duplicate-block-header|duplicate-block-info|duplicate-block-index|duplicate-block-similarity|duplicate-block-actions|duplicate-block-compare|duplicate-block-text-label|duplicate-block-text-content|duplicate-block-text-empty —— 16 条规则
.cleaning-panel { background: var(--color-surface); border: 1px solid var(--color-border-light); border-radius: var(--radius-md); padding: var(--space-md); margin-bottom: var(--space-lg); box-shadow: var(--shadow-sm) }
.cleaning-stats { padding-bottom: var(--space-sm); border-bottom: 1px solid var(--color-border); margin-bottom: var(--space-sm) }
.cleaning-progress { width: 100%; height: 4px; background: var(--color-bg); border-radius: 2px; overflow: hidden }
.cleaning-progress-bar { width: 100%; height: 100%; background: var(--gradient-primary); animation: cleaning-pulse 1.5s ease-in-out infinite }
.duplicate-blocks { margin-top: var(--space-sm) }
.duplicate-block { display: flex; flex-direction: column; padding: var(--space-sm) var(--space-md); border: 1px solid var(--color-border-light); border-radius: var(--radius-sm); margin-bottom: var(--space-xs); background: var(--color-bg); transition: border-color 0.2s ease }
.duplicate-block:hover { border-color: var(--color-border) }
.duplicate-block-header { display: flex; justify-content: space-between; align-items: center; gap: var(--space-sm); flex-wrap: wrap }
.duplicate-block-info { display: flex; gap: var(--space-md); font-size: 0.8rem }
.duplicate-block-index { font-weight: 600; color: var(--color-text) }
.duplicate-block-similarity { color: var(--color-text-secondary) }
.duplicate-block-actions { display: flex; gap: var(--space-xs) }
.duplicate-block-compare { margin-top: var(--space-sm); padding-top: var(--space-sm); border-top: 1px dashed var(--color-border-light); display: flex; flex-direction: column; gap: var(--space-xs) }
.duplicate-block-text-label { font-size: 0.75rem; font-weight: 600; color: var(--color-text-secondary); margin-bottom: 4px }
.duplicate-block-text-content { font-size: 0.78rem; line-height: 1.6; white-space: pre-wrap; word-break: break-word; max-height: 180px; overflow: auto; background: var(--color-bg-subtle, #f7f8fa); border: 1px solid var(--color-border-light); border-radius: var(--radius-sm); padding: var(--space-xs) var(--space-sm); margin: 0; font-family: inherit; color: var(--color-text) }
.duplicate-block-text-empty { font-size: 0.78rem; color: var(--color-text-secondary); margin: 0 }
```

## src/styles/diff.css

```
# diff.css @HEAD~25 classes=diff-container|diff-summary|diff-header|diff-col-label|diff-body|diff-block|diff-line|diff-line-prefix|diff-line-number|diff-line-content|diff-line-removed|diff-line-added|diff-line-unchanged —— 21 条规则
.diff-container { background: var(--color-surface); border: 1px solid var(--color-border-light); border-radius: var(--radius-md); overflow: hidden; box-shadow: var(--shadow-sm) }
.diff-summary { display: flex; gap: var(--space-md); padding: var(--space-sm) var(--space-md); background: var(--color-bg); border-bottom: 1px solid var(--color-border); font-size: 0.8rem; color: var(--color-text-secondary) }
.diff-header { display: flex; gap: 0; padding: var(--space-xs) var(--space-md); background: var(--color-bg); border-bottom: 1px solid var(--color-border); font-size: 0.75rem; font-weight: 600; color: var(--color-text-secondary) }
.diff-col-label { flex-shrink: 0 }
.diff-col-label:nth-child(1) { width: 50px; text-align: right }
.diff-col-label:nth-child(2) { width: 50px; text-align: right; margin-left: var(--space-sm) }
.diff-col-label:nth-child(3) { flex: 1; margin-left: var(--space-sm) }
.diff-body { font-family: var(--font-mono); font-size: 0.8rem; line-height: 1.6; overflow-x: auto }
.diff-block { border-bottom: 1px solid var(--color-border-light) }
.diff-block:last-child { border-bottom: none }
.diff-line { display: flex; align-items: stretch; min-height: 24px }
.diff-line-prefix { flex-shrink: 0; width: 20px; text-align: center; color: var(--color-text-tertiary); user-select: none }
.diff-line-number { flex-shrink: 0; width: 40px; text-align: right; padding-right: var(--space-sm); color: var(--color-text-tertiary); user-select: none; font-size: 0.75rem; line-height: 1.6 }
.diff-line-content { flex: 1; padding-left: var(--space-sm); white-space: pre-wrap; word-break: break-all }
.diff-line-removed { background: #fef2f2 }
.diff-line-removed .diff-line-prefix { color: var(--color-error) }
.diff-line-removed .diff-line-content { color: #991b1b }
.diff-line-added { background: #f0fdf4 }
.diff-line-added .diff-line-prefix { color: var(--color-success) }
.diff-line-added .diff-line-content { color: #166534 }
.diff-line-unchanged { background: transparent }
```

## src/styles/dashboard.css

```
# dashboard.css @HEAD~25 classes=dashboard-two-col|dashboard-review-card|trend-bar|trend-bar-warning —— 5 条规则
.dashboard-two-col { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-lg); margin-bottom: var(--space-xl) }
.dashboard-review-card { cursor: pointer; display: flex; justify-content: space-between; align-items: center }
.trend-bar { width: 100%; max-width: 40px; border-radius: 6px 6px 0 0; transition: all 0.3s var(--ease-out-expo); position: relative; background: var(--gradient-primary) }
.trend-bar-warning { background: linear-gradient(180deg, #f59e0b, #d97706) }
.trend-bar:hover { filter: brightness(1.1); transform: scaleY(1.02); transform-origin: bottom }
```

## src/styles/responsive.css

```
# responsive.css @HEAD~25 classes=dashboard-two-col|dashboard-review-card —— 2 条规则
@media (max-width: 768px) .dashboard-two-col { grid-template-columns: 1fr; gap: var(--space-md) }
@media (max-width: 768px) .dashboard-review-card { flex-wrap: wrap; gap: var(--space-sm) }
```

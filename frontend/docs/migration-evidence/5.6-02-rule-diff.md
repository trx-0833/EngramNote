<!-- 由 scripts/gen-migration-evidence.mjs 生成，请勿手改 -->

# 5.6 规则清单差集：迁移前(HEAD 源码) vs 迁移后(dist 产物)

两侧都解析成「上下文 + 选择器 + 声明」，类名归一为 `.C0`…`.C4` 占位符后逐条比。
`animation` 的值单独由「动画绑定」与「动画体比对」两项负责（哈希化的动画名无法逐字比）。

运行退出码：0（0 = 没有丢失、动画绑定自洽、动画体一致）

```
════════ 迁移前后规则清单差集 ════════
迁移前（HEAD 源码）：7 条
迁移后（dist 产物）：7 条

✓ 逐字保留： .C0
✓ 逐字保留： .C0:hover
✓ 逐字保留： .C1
✓ 逐字保留： .C2
✓ 逐字保留： .C3
✓ 逐字保留：@media (max-width: 768px) .C4
✓ 逐字保留：@media (max-width: 480px) .C4

──── 新增的规则（产物有、迁移前无）────
（无）

──── 动画绑定检查（引用的动画名必须在同一产物文件内有定义）────
✓ index-Dn_nqLlY.css: 引用 9 个动画，全部在本文件内有定义（fadeIn, slideUp, cleaning-pulse, scaleIn, shimmer, glowPulse, spin, graph-spin, citation-flash）
✓ useSelfRating-xwb7s624.css: 引用 2 个动画，全部在本文件内有定义（_feedbackScaleIn_150vo_1, _feedbackShake_150vo_1）

──── 动画体比对（模块内改名后的动画，动画体必须与全局原版一致）────
✓ @keyframes scaleIn → feedbackScaleIn
✓ @keyframes shake → feedbackShake

════════ 汇总：逐字保留 7 / 值有变化 0 / 丢失 0 / 动画绑定 全部自洽 / 动画体一致 是 ════════
```

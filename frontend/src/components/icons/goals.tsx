/**
 * @file 图标图形：goals（学习目标）
 * @description 图形意图：**旗** —— 一根旗杆 + 一面燕尾旗（右缘向内凹出一个 V 口）。
 * 选旗不选靶心：靶心是三层同心圆，与 `review-cards` 的循环环在 16px 下容易混淆，
 * 而旗的轮廓（一根竖线 + 一个燕尾）在本套 13 个图形里独一无二。
 * 替换的 Unicode：`\u25C9` ◉（`Sidebar.tsx:35`）—— 一个同心圆点，与"目标"无关联。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Goals() {
  return (
    <>
      {/* 旗杆：略高于旗面、略低于旗底，两端各留一点杆头 */}
      <path d="M4.5 3.5v17" />
      {/* 旗面：上缘 → 内凹的燕尾尖 → 下缘回到旗杆（左边由旗杆本身闭合，故不画竖线） */}
      <path d="M4.5 4.5h15.5l-4.3 4.2 4.3 4.2H4.5" />
    </>
  );
}

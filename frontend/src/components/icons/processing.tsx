/**
 * @file 图标图形：processing（处理中）
 * @description 图形意图：**300° 的进度环** —— 自正上方（12,4.5）顺时针绕右、下、左
 * 三边，收在正左（4.5,12），缺口留在左上。环的两端靠 `Icon.tsx` 的 `round` 端点
 * 自然收圆，所以静止看是一只"走了三格、还差一格"的环。
 *
 * ⚠️ **是静止图形，不是动画**：全站的"处理中"都另有自己的转圈/计时器
 * （`.spinner` / `TaskProgress`），这个图标只做状态标记 ——
 * 在这里加旋转会让同一屏出现两个转速不同的转圈。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Processing() {
  return (
    <>
      {/* 弧：r=7.5、圆心 (12,12)，large-arc=1 + sweep=1 ⇒ 走顺时针的长弧（270°） */}
      <path d="M12 4.5a7.5 7.5 0 1 1-7.5 7.5" />
    </>
  );
}

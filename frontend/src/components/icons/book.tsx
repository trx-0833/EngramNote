/**
 * @file 图标图形：book（摊开的书）
 * @description 图形意图：**一本摊开的书** —— 正中一道书脊竖线，左右两页各自用
 * 一条三次曲线画出"自书脊向外翘起、再落回书口"的弧度，页脚同样向内收。
 * 摊开 = "正在读 / 这是读物"，与 `notes`（合着的册页，语义是"一篇笔记"）
 * 刻意区分：`notes` 是一只竖长的封面矩形，`book` 是左右对称的两页。
 * 替换的 emoji：`\u{1F4D6}` 📖（`MarkdownReader.tsx:74` 的"正在阅读"、
 * `projects/ProjectsUsageNotes.tsx:18` 的"使用说明"）。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Book() {
  return (
    <>
      {/* 书脊：自 6.5 贯到 20.1，把两页分开 */}
      <path d="M12 6.5v13.6" />
      {/* 左页：自书脊顶端向外上方翘到书口 (3.5,4.7)，沿书口下行，页脚再收回书脊 */}
      <path d="M12 6.5C10.4 5.3 8.4 4.7 6 4.7H3.5v13.6H6c2.4 0 4.4.6 6 1.8" />
      {/* 右页：与左页逐点镜像（书口在 20.5） */}
      <path d="M12 6.5c1.6-1.2 3.6-1.8 6-1.8h2.5v13.6H18c-2.4 0-4.4.6-6 1.8" />
    </>
  );
}

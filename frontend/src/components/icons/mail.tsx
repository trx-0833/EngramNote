/**
 * @file 图标图形：mail（邮箱）
 * @description 图形意图：**信封** —— 20×16 的圆角矩 + 自左右上角向中心下垂的两道封盖折线
 * （在正中相汇）。信封是"账号 / 邮箱"字段唯一不会认错的图形。
 *
 * 几何**逐点搬自** `Login.tsx:63` / `Register.tsx:60` 原来那份内联 SVG
 * （24 网格、`strokeWidth=2`）—— B3 把它迁进唯一出口，只换渲染参数
 * （线宽统一到 1.5、尺寸收进 16/20/24 三档），形状本身一笔未改，
 * 所以认证页看起来只有"线细了一点"这一处变化。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Mail() {
  return (
    <>
      {/* 信封主体：20×16，四角 r=2 */}
      <rect x="2" y="4" width="20" height="16" rx="2" />
      {/* 封盖：自右上角 (22,7) 斜下到中心，再斜上到左上角 (2,7) */}
      <path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7" />
    </>
  );
}

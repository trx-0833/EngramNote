/**
 * @file 图标图形：star（重点）
 * @description 图形意图：**五角星** —— 五个尖依次落在 (12,3.8) / (20.5,10) /
 * (17.2,19.9) / (6.8,19.9) / (3.5,10)，每段边都是直线，靠 `round` 拐角收圆。
 * 替换的 emoji：`\u2B50` ⭐（`KnowledgeCards.tsx:374` 的"重点"标记）——
 * emoji 自带金色，与卡片上那个**刻意的** `#c9a959` 令牌色打架，
 * 也没法在暗色/高对比主题下跟着 `currentColor` 走。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Star() {
  return (
    <>
      {/* 星：五段直线闭合，外接圆 r≈8.5（四周留 2px 呼吸边） */}
      <path d="M12 3.8l2.6 5.3 5.9.9-4.3 4.1 1 5.8-5.2-2.7-5.2 2.7 1-5.8L3.5 10l5.9-.9z" />
    </>
  );
}

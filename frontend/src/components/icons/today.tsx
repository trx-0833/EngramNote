/**
 * @file 图标图形：today（今日学习）
 * @description 图形意图：**日出 + 书页（"晨读"）** —— 半轮朝日从摊开的册页里升起，
 * 上方三道光线。太阳的弦正好落在两页的上缘上，两者共用一条线。
 * 替换的 Unicode：`\u2618` ☘（`Sidebar.tsx:30`）—— 三叶草，完全不表意。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Today() {
  return (
    <>
      {/* 顶光 */}
      <path d="M12 3.5v2.2" />
      {/* 左右斜光（与日轮留 2 单位间隙，16px 下也不会糊成一团） */}
      <path d="M5.4 7l1.6 1.6M18.6 7l-1.6 1.6" />
      {/* 半轮朝日：r=4 的正半圆，弦在 y=12.2（正是两页的上缘） */}
      <path d="M8 12.2a4 4 0 0 1 8 0" />
      {/* 左页：上缘自外向内 → 内侧上圆角 → 书脊直落 → 内侧下圆角 → 下缘收回 */}
      <path d="M3.5 12.2h5.2a3.3 3.3 0 0 1 3.3 3.3v4.6a3.3 3.3 0 0 0-3.3-3.3H3.5z" />
      {/* 右页：与左页关于 x=12 镜像 */}
      <path d="M20.5 12.2h-5.2a3.3 3.3 0 0 0-3.3 3.3v4.6a3.3 3.3 0 0 1 3.3-3.3h5.2z" />
    </>
  );
}

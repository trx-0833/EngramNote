/**
 * @file 图标图形：daily（今日资料）
 * @description 图形意图：**一叠文献** —— 三张纸自右下向左上错开叠放；后面两张只画
 * 露在外面的"上边 + 右边"（被压住的部分不画线，避免出现交叉的假轮廓）。
 * 替换的 Unicode：`\u25B7` ▷（`Sidebar.tsx:32`）—— 一个空心三角，与"资料"无关。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Daily() {
  return (
    <>
      {/* 最里面一张：只留上边与右边（错位 +2.5 / -2.5） */}
      <path d="M8 5.5h10.5a2 2 0 0 1 2 2V14" />
      {/* 中间一张：同样只留可见部分，右下端停在最上面一张的右侧 */}
      <path d="M5.5 8h10.5a2 2 0 0 1 2 2v6.5" />
      {/* 最上面一份文献：完整的一张纸（12.5×8，圆角 2） */}
      <rect x="3" y="10.5" width="12.5" height="8" rx="2" />
    </>
  );
}

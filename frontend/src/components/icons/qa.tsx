/**
 * @file 图标图形：qa（问答）
 * @description 图形意图：**对话气泡 + 问号** —— 一个圆角气泡，左下拖出一个尖角（"说话"），
 * 泡内一个问号（半圆弯钩 + 短竖 + 独立的点）。
 * 替换的 Unicode：`\u2753` ❓（`Sidebar.tsx:50`）—— **彩色 emoji**，是全站唯一一个
 * 与其余图标不同源的字形（Windows/Android 上直接渲染成红蓝卡通问号）。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Qa() {
  return (
    <>
      {/* 气泡：16×14 圆角矩（r=2.5），左下角续一段 3×3 的尖尾后沿右侧收口 */}
      <path d="M20 5.5a2.5 2.5 0 0 0-2.5-2.5h-11A2.5 2.5 0 0 0 4 5.5v9A2.5 2.5 0 0 0 6.5 17H8v3l4-3h5.5a2.5 2.5 0 0 0 2.5-2.5z" />
      {/* 问号的弯钩：近乎半圆（半径约 2.34，因弦长略大于直径会自动放径），右端接短竖 */}
      <path d="M10.1 8.4a2.3 2.3 0 0 1 4.6.9c0 1.4-2.2 2-2.2 3.1" />
      {/* 问号的点：零长线段 + round cap = 一个圆点（Lucide 的标准做法，不用 fill） */}
      <path d="M12.5 14.4h.01" />
    </>
  );
}

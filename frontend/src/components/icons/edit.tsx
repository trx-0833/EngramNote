/**
 * @file 图标图形：edit（编辑）
 * @description 图形意图：**一支斜握的笔** —— 笔尖落在左下（含笔尖的斜切口），
 * 笔尾收在右上（圆头），笔杆中部偏上有一道垂直于笔杆的短线，把"笔尾"与"笔杆"分开。
 * 那道短线是 Lucide `pencil` 的既有写法（`m15 5 4 4`），保留它是为了让"这是一支笔"
 * 在 16px 下仍然读得出来 —— 去掉它，整个图形就退化成一根斜杠。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Edit() {
  return (
    <>
      {/* 笔身：自左下笔尖（3,21.2）沿右下缘上行到笔尾，笔尾用 r=2.7 的圆头收，
          再沿左上缘回到笔尖，最后用笔尖那道斜切口（1.2×5）闭合 */}
      <path d="M16.8 3.6a2.7 2.7 0 1 1 3.8 3.8L8 20l-5 1.2 1.2-5z" />
      {/* 笔尾分界：与笔杆垂直（笔杆方向 45°，这道线也是 45°），横跨整个笔身宽度 5.4 */}
      <path d="M14.3 6.1 18.1 9.9" />
    </>
  );
}

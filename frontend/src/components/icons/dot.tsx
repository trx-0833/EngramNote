/**
 * @file 图标图形：dot（状态点）
 * @description 图形意图：**一个圆点** —— r=3 的圆（含描边后外径 7.5）。
 *
 * ⚠️ 它是**描边圆环**，不是实心块：`Icon.tsx` 统一施加 `fill:none`，
 * 图形文件里因此写不出"填色"（那是 B1 定下的硬约束，破例就会出现
 * 一个不跟着线宽走的图形）。缩到 16px 时内孔只有 2.7px、外径约 5px，
 * 观感上就是一颗点 —— 与它替换掉的 `\u25CF` ● 在 0.8rem 正文里的大小基本一致。
 *
 * 替换的 Unicode：`\u25CF` ●（`KnowledgeGraph.tsx:181` 的"创建关系"提示前缀）——
 * `●` 是**几何图形块**，字体一变大小与圆度都变，且它与同一提示行里
 * 那句正文用的是同一个颜色来源，只有字号把它们分开。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Dot() {
  return (
    <>
      {/* 点：r=3，圆心即网格中心 */}
      <circle cx="12" cy="12" r="3" />
    </>
  );
}

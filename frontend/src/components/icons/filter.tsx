/**
 * @file 图标图形：filter（筛选）
 * @description 图形意图：**漏斗** —— 上口 16 宽的一道水平沿，两侧向内收成 4 宽的颈，
 * 颈底再接一个 4×2 的小斗。上宽下窄 = "把一堆东西收窄成想要的几样"。
 * 此前各页的筛选区（分类 Tab、状态标签、筛选栏）全是纯文字药丸，没有图形。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Filter() {
  return (
    <>
      {/* 漏斗：上口 (4,5)→(20,5)，斜边收到 (14,12.5)/(10,12.5)，颈 4 宽、斗底 2 高 */}
      <path d="M4 5h16l-6 7.5V19l-4 2v-8.5z" />
    </>
  );
}

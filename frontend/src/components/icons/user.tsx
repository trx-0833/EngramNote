/**
 * @file 图标图形：user（用户名）
 * @description 图形意图：**一个人** —— 上方 r=4 的头（圆心 (12,7)），
 * 下方一副自头两侧垂下的肩：从 (19,21) 起，向上两个 4 单位的圆角，
 * 横过 (12,15)，再向下镜像落回 (5,21)。"头 + 肩"是"人 / 账号"的最小图形。
 *
 * 几何**逐点搬自** `Register.tsx:85` 那份内联 SVG（24 网格、`strokeWidth=2`）——
 * 迁移只换渲染参数，形状一笔未改。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function User() {
  return (
    <>
      {/* 肩：自右下方起，绕过两个 r=4 的圆角横过肩线，再落到左下方 */}
      <path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2" />
      {/* 头：r=4 的圆（比 `review-cards` 的卡片更圆、更小，两者不会认错） */}
      <circle cx="12" cy="7" r="4" />
    </>
  );
}

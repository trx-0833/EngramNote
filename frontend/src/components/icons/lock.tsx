/**
 * @file 图标图形：lock（密码）
 * @description 图形意图：**挂锁** —— 18×11 的锁体（四角 r=2）+ 上方一副 r=5 的锁梁
 * （两腿落在锁体上沿 11 处）。锁是"密码 / 受保护字段"的通用图形。
 *
 * 几何**逐点搬自** `Login.tsx:88` / `Register.tsx:112` 原来那份内联 SVG
 * （24 网格、`strokeWidth=2`）—— 迁移只换渲染参数，形状一笔未改。
 * 与 `logout`（一扇门 + 一支外出的箭头）刻意区分：那个是"离开"，
 * 这个是"这一格受保护"，两者在侧边栏底部与认证表单上永远不会同屏混淆。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Lock() {
  return (
    <>
      {/* 锁体：18×11，四角 r=2 */}
      <rect width="18" height="11" x="3" y="11" rx="2" ry="2" />
      {/* 锁梁：自锁体上沿 (7,11) 上行，r=5 的半圆跨过顶部，落回 (17,11) */}
      <path d="M7 11V7a5 5 0 0 1 10 0v4" />
    </>
  );
}

/**
 * @file 图标图形：file（文档）
 * @description 图形意图：**一张竖长的纸** —— 13×17 的页身（左下、右下两角 r=2 收圆），
 * 右上角折一个 5×5 的角：外框走斜边，页内再画出折角的直角分界，两者围出那个小三角。
 * 替换的 emoji：`\u{1F4C4}` 📄（`NoteAskPanel.tsx:384` 与 `QA.tsx:404` 的引用行）——
 * emoji 是彩色的，夹在 `[1] 📄 笔记标题` 这样一行 0.8rem 的正文里比标题还抢眼。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function File() {
  return (
    <>
      {/* 页身：顶边自折角向左，四角（除折角外）按 r=2 收圆，右边到 8.5 处改走斜边回折角 */}
      <path d="M14 3.5H7.5A2 2 0 0 0 5.5 5.5v13a2 2 0 0 0 2 2h9a2 2 0 0 0 2-2V8.5L14 3.5" />
      {/* 折角分界：竖下 5、横右 4.5（与页身右边 18.5 接上） */}
      <path d="M14 3.5v5h4.5" />
    </>
  );
}

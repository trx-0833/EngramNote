/**
 * @file 图标图形：cards（知识卡片）
 * @description 图形意图：**卡片叠** —— 后面一张横放的卡只画露出来的左缘与上缘
 * （被压住的部分不画线），前面一张完整的卡压在右下，卡面两行字。
 * 与 `daily`（一叠文献）刻意反方向错位、且更宽更扁；与 `review-cards` 的区别是"没有循环环"。
 * 替换的 Unicode：`\u25C8` ◈（`Sidebar.tsx:48`）—— 一个菱形，与"卡片"无关。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Cards() {
  return (
    <>
      {/* 后面一张（13.5×11.5）：自下缘起笔 → 左下圆角 → 左缘 → 左上圆角 → 上缘 → 右上圆角 */}
      <path d="M6 15.5H5a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h10.5a2 2 0 0 1 2 2" />
      {/* 前面一张：13×11 的圆角卡，向右下错开（错位 +5 / +4.5） */}
      <rect x="8" y="8.5" width="13" height="11" rx="2" />
      {/* 卡面两行字（长短各一） */}
      <path d="M11.5 12.5h6.5M11.5 15.7h3.5" />
    </>
  );
}

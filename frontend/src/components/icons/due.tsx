/**
 * @file 图标图形：due（复习到期）
 * @description 图形意图：**沙漏** —— 上下两道盖（各出挑 1.5）+ 上下两个镜像的锥体，
 * 在正中的腰（12,12）相触。沙漏是"时间在漏"的最小图形，对应"这张卡到期了"。
 * 刻意不画沙子：一亮一暗两笔会逼出一个"进度到哪了"的误读，
 * 而 `due` 只表"时间到了"这一个状态。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Due() {
  return (
    <>
      {/* 上盖 */}
      <path d="M6.5 3.5h11" />
      {/* 下盖 */}
      <path d="M6.5 20.5h11" />
      {/* 上半：自盖内侧（8,3.5）沿弧收束，到腰 (12,12) 后镜像上行到 (16,3.5) */}
      <path d="M8 3.5v3.7c0 1.1.5 2.1 1.3 2.9l2.7 1.9 2.7-1.9c.8-.8 1.3-1.8 1.3-2.9V3.5" />
      {/* 下半：与上半逐点镜像（盖内侧 20.5，腰同为 12,12） */}
      <path d="M8 20.5v-3.7c0-1.1.5-2.1 1.3-2.9l2.7-1.9 2.7 1.9c.8.8 1.3 1.8 1.3 2.9v3.7" />
    </>
  );
}

/**
 * @file 图标图形：assessment（学习评估）
 * @description 图形意图：**尺规 / 量尺** —— 一支斜置 45° 的量尺，靠上的一边刻三道刻度
 * （中间那道更长，像真尺上的半刻度）。**刻意不画勾选框**：勾选框是 `questions` 的语义，
 * 现状 `\u2713` ✓ 与 `\u2611` ☑ 在侧边栏里撞车，正是这一批要修的问题。
 * 替换的 Unicode：`\u2713` ✓（`Sidebar.tsx:34`）。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Assessment() {
  return (
    <>
      {/* 尺身：沿对角线长 18、宽 5.5 的矩形（四个角由 round join 自然收圆） */}
      <path d="M20.3 7.6 16.4 3.7 3.7 16.4 7.6 20.3z" />
      {/* 三道刻度：自靠上的一边垂直向内，位置取尺身的 1/4、1/2、3/4 */}
      <path d="M6.9 13.2l1.3 1.3" />
      <path d="M10.1 10.1l1.9 1.9" />
      <path d="M13.2 6.9l1.3 1.3" />
    </>
  );
}

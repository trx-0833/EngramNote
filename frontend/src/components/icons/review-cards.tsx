/**
 * @file 图标图形：review-cards（卡片复习）
 * @description 图形意图：**卡片 + 循环箭头** —— 一张卡片，外面套一圈 300° 的循环箭头
 * （缺口留在正上方，右端收一个箭头尖），表示"反复过这张卡"。
 * 与 `cards`（卡片叠）刻意区分：这里只有一张卡 + 明确的环。
 * 替换的 Unicode：`\u21BB` ↻（`Sidebar.tsx:31`）—— 只表"刷新"，看不出是在复习卡片。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function ReviewCards() {
  return (
    <>
      {/* 循环箭头主体：r=9.2（含描边后恰好落在 2px 呼吸边内），大弧 + 逆时针 = 从左上绕底部到右上 */}
      <path d="M7.4 4A9.2 9.2 0 1 0 16.6 4" />
      {/* 箭头尖：自弧末端向切线反方向张开两撇，指向左上（= 箭头的行进方向） */}
      <path d="M17.6 6.2 16.6 4l2.4-.2" />
      {/* 卡片：9×9 圆角方，四角与环至少留 1 单位空隙（描边后仍不相碰） */}
      <rect x="7.5" y="7.5" width="9" height="9" rx="2" />
      {/* 卡面上的两行字（长短各一，读作"卡片"而不是空白方块） */}
      <path d="M10 11h4M10 13.5h2.4" />
    </>
  );
}

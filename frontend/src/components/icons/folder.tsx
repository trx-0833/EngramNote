/**
 * @file 图标图形：folder（文件夹）
 * @description 图形意图：**一只文件夹** —— 顶边先向右走 3.3，然后斜下一个 2 单位高的
 * 标签页，标签收口后接着一条更长的顶边（到 18.5），下方是一只 16×11.7 的圆角盒身
 * （四角 r=2）。"标签页 + 盒身"是文件夹唯一不会认错的骨架。
 * 替换的 emoji：`\u{1F4C2}` 📂（`Projects.tsx:83` 的空状态）——
 * 那是本项目最后一处把 emoji 当"页面插画"用的地方。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Folder() {
  return (
    <>
      {/* 外框：自左下圆角起笔，绕左上 → 标签页 → 盒身顶边 → 右上 → 右下 → 回到左下 */}
      <path d="M3.5 7a2 2 0 0 1 2-2h3.3a2 2 0 0 1 1.6.8l.9 1.2a2 2 0 0 0 1.6.8h5.6a2 2 0 0 1 2 2v7.7a2 2 0 0 1-2 2H5.5a2 2 0 0 1-2-2z" />
    </>
  );
}

/**
 * @file 图标图形：ai（AI 生成）
 * @description 图形意图：**四角星火** —— 四个尖分别落在上下左右
 * （12,5 / 18,11 / 12,17 / 6,11），四条边用三次曲线向内凹，
 * 于是它不是一颗菱形，而是一颗"星火"。替换的 emoji：`\u2728` ✨
 * （`KnowledgeCards.tsx:482`）—— emoji 是彩色的，不受 `currentColor` 控制，
 * 在一条 0.7rem 的提示文字里颜色由系统字体决定，与本页配色无关。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Ai() {
  return (
    <>
      {/* 星火：每段弧的两个控制点都贴着中心一侧，把边"吸"进去形成凹口 */}
      <path d="M12 5c.5 4.3 1.7 5.5 6 6-4.3.5-5.5 1.7-6 6-.5-4.3-1.7-5.5-6-6 4.3-.5 5.5-1.7 6-6z" />
    </>
  );
}

/**
 * @file 图标图形：graph（知识图谱）
 * @description 图形意图：**节点连线（带一点毛笔笔锋）** —— 三个大小、位置都不对称的
 * 节点，三条连线一律用二次曲线，且都朝外微微鼓出（不是几何直线）；最下面那条鼓得最多，
 * 是全套图标里唯一一处"笔墨"暗示。刻意做成不等边三角，避免读成系统自带的"分享"图标。
 * 替换的 Unicode：`\u25CE` ◎（`Sidebar.tsx:49`）—— 一个同心圆，与"图谱"无关。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Graph() {
  return (
    <>
      {/* 左上节点（略大） */}
      <circle cx="5.5" cy="5.5" r="2.2" />
      {/* 右侧节点 */}
      <circle cx="18.5" cy="8.5" r="2" />
      {/* 左下节点 */}
      <circle cx="8.5" cy="18.5" r="2" />
      {/* 连线一：左上→右，端点落在两个节点的圆周上，中段向下微鼓 */}
      <path d="M7.7 6Q11.8 8.2 16.5 8.1" />
      {/* 连线二：左上→左下，中段向左微鼓 */}
      <path d="M6 7.7Q5.9 12.4 8.1 16.5" />
      {/* 连线三：右→左下，鼓度最大（笔锋所在） */}
      <path d="M17.1 9.9Q15.1 15.1 9.9 17.1" />
    </>
  );
}

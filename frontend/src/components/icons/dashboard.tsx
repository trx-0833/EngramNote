/**
 * @file 图标图形：dashboard（仪表盘）
 * @description 图形意图：**案几上展开的册页** —— 一张案几（台面 + 两腿）上摊开左右两页。
 * 替换的 Unicode：`\u2302` ⌂（`Sidebar.tsx:25`）—— 几何符号，与"学习总览"无任何关联。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责，这里不出现任何几何之外的属性。
 */
export default function Dashboard() {
  return (
    <>
      {/* 案几台面：两端出挑于册页，是"案"而不是"线" */}
      <path d="M3 17.5h18" />
      {/* 案几两腿（短、内收，避免抢戏） */}
      <path d="M5.5 17.5v2.5M18.5 17.5v2.5" />
      {/* 左页：自中缝起笔 → 上缘弧 → 外缘直落 → 下缘弧回中缝，闭合出书脊 */}
      <path d="M12 6.5C9.9 5 7.2 4.5 4.5 5.1V15c2.7-.6 5.4-.1 7.5 1.4z" />
      {/* 右页：与左页镜像（控制点关于 x=12 对称） */}
      <path d="M12 6.5c2.1-1.5 4.8-2 7.5-1.4V15c-2.7-.6-5.4-.1-7.5 1.4z" />
    </>
  );
}

/**
 * @file 图标图形：delete（删除）
 * @description 图形意图：**带叉的浅桶** —— 桶口一道横线 + 上宽下窄的桶身（**无提梁、
 * 无篾条**），桶身正中一个叉。
 *
 * ## 怎么与 `trash` 区分（刻意的，不是画重了）
 *
 * - `trash` 是**位置**："回收站"这个去处 —— 有提梁（可以提走）、有篾条（一只真的篓子），
 *   桶身瘦高（6.2 → 20.8）；用在侧边栏那一行导航上，点进去看到的是"被删掉的东西待的地方"。
 * - `delete` 是**动作**："把这一项删掉" —— 去掉提梁与篾条，只留一只更矮更宽的浅桶
 *   （6.2 → 18.6），并在桶内画一个叉：叉读作"划掉 / 这一项不要了"，
 *   而不是"搬去某个地方"。
 *
 * 于是两个图形在任何尺寸下都不会认错：`trash` 高瘦有提梁，`delete` 矮宽有叉。
 * 此前全站的"删除"都是纯文字按钮，没有任何图形。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Delete() {
  return (
    <>
      {/* 桶口：比桶身两端各出挑 2.2，把"口"和"身"分开 */}
      <path d="M4.2 6.2h15.6" />
      {/* 桶身：上口 12 宽、下底 7.2 宽，底部两角用 r=1.8 的圆弧收口（浅桶，止于 18.6） */}
      <path d="M6 6.2l.85 10.8a1.8 1.8 0 0 0 1.8 1.6h6.7a1.8 1.8 0 0 0 1.8-1.6L18 6.2" />
      {/* 叉：桶身正中的"划掉"，两笔等长、交点在桶心 */}
      <path d="m10.2 10.8 3.6 3.6M13.8 10.8l-3.6 3.6" />
    </>
  );
}

/**
 * @file 图标图形：relation（关联）
 * @description 图形意图：**一支双头箭** —— 一道横贯内容区的轴线（4 → 20），
 * 两端各一个各 4.5×9 的箭头，分别指向左、右。
 * 双头 = "两边都算数"，对应"这两张卡片之间存在（双向）关联"。
 *
 * 替换的 Unicode：`\u2194` ↔（`GraphSidebar.tsx:438` 的"A ↔ B"）——
 * ⚠️ 这是本批**唯一一处**与「不要改正文里作为连接词的箭头」这条排除规则擦边的地方：
 * 它夹在两个 `<strong>` 标题之间。判断依据是那句排除规则的定语「**正文里**」——
 * 那一行不是说明文字，而是"建议关联"卡片里的**关系主体**（关系两端就是这两个标题），
 * `↔` 在那里承担的是"存在关联"这个语义，不是句读。
 * 于是按"必须处理的"清单把它换成了图形；若后来认定它属排版字符，
 * 回退成本是一行（`<Icon name="relation" size={16} />` → `↔`）。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Relation() {
  return (
    <>
      {/* 轴线 */}
      <path d="M4 12h16" />
      {/* 左箭头：自 (8.5,7.5) 折到左端 (4,12)，再折回 (8.5,16.5) */}
      <path d="m8.5 7.5-4.5 4.5 4.5 4.5" />
      {/* 右箭头：与左箭头逐点镜像 */}
      <path d="m15.5 7.5 4.5 4.5-4.5 4.5" />
    </>
  );
}

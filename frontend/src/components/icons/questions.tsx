/**
 * @file 图标图形：questions（问题集）
 * @description 图形意图：**清单 + 问号** —— 一张横放的清单卡，左半是一个大问号，
 * 右半是三行长短不一的条目。与 `qa`（对话气泡 + 问号）刻意区分：这里没有气泡尖尾，
 * 且多出"清单"的三行条目。
 * 替换的 Unicode：`\u2611` ☑（`Sidebar.tsx:51`）—— 带勾的方框，与 `assessment` 的 ✓ 撞车。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Questions() {
  return (
    <>
      {/* 清单卡：18×14 圆角矩（横放，与 `notes` 的竖长册页形状相反） */}
      <rect x="3" y="5" width="18" height="14" rx="2" />
      {/* 问号的弯钩：近乎半圆（半径约 2.44），右端接一段短竖 */}
      <path d="M6.1 9.4a2.4 2.4 0 0 1 4.8.9c0 1.5-2.4 1.9-2.4 3.3" />
      {/* 问号的点：零长线段 + round cap = 一个圆点（不用 fill） */}
      <path d="M8.5 15.8h.01" />
      {/* 清单三行：末行更短，读作"条目"而不是"表格" */}
      <path d="M14 9h4.5M14 12h4.5M14 15h3" />
    </>
  );
}

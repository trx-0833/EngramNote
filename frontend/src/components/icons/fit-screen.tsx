/**
 * @file 图标图形：fit-screen（适应屏幕）
 * @description 图形意图：**四角框** —— 四段各带一个 r=2 圆角的折线，
 * 分别占住左上、右上、右下、左下四角，围出一块"被框住的范围"。
 * 四角框是"把内容缩到刚好装进视野"的通用图形。
 * 替换的 Unicode：`\u2922` ⤢（`GraphCanvas.tsx:160` 的"适应屏幕"按钮）——
 * 那是"东北向双箭头"，语义是**方向**（往右上），不是"缩放到合适大小"；
 * 而且它是个生僻码点，缺字体的环境会渲染成豆腐块。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function FitScreen() {
  return (
    <>
      {/* 左上角：自 (4,9) 上行到圆角，再右行到 9 */}
      <path d="M4 9V6a2 2 0 0 1 2-2h3" />
      {/* 右上角 */}
      <path d="M15 4h3a2 2 0 0 1 2 2v3" />
      {/* 右下角 */}
      <path d="M20 15v3a2 2 0 0 1-2 2h-3" />
      {/* 左下角 */}
      <path d="M9 20H6a2 2 0 0 1-2-2v-3" />
    </>
  );
}

/**
 * @file 图标图形：menu（菜单 / 移动端导航开关）
 * @description 图形意图：**三道等距横线**（汉堡菜单）—— 手机上打开侧栏抽屉的唯一入口。
 *
 * 替换的 Unicode：`\u2630` ☰（`App.tsx:114`）。这里有一处**同一个码点被用在两个
 * 不同语义上**的问题：`Sidebar.tsx:41` 的「笔记列表」用的也是 `\u2630` ——
 * 于是"菜单"和"笔记列表"在用户眼里是同一个符号，且该字符的宽度还依赖字体
 * （部分字体下它比周围汉字宽出一截，把图标列撑歪）。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Menu() {
  return (
    <>
      <path d="M4 7h16" />
      <path d="M4 12h16" />
      <path d="M4 17h16" />
    </>
  );
}

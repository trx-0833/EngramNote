/**
 * @file 图标图形：logout（退出）
 * @description 图形意图：**从门里走出去** —— 左半是一个半开的门框（右侧开口），
 * 一条横线自门内穿出，末端一个指向右的箭头。
 *
 * 替换的 Unicode：`\u2190` ←（`Sidebar.tsx:170`）。那个字符的语义是
 * "返回上一页"，用在"退出登录"上会把人引向错误的预期 —— 退出是不可逆的会话结束，
 * 不是导航历史里的后退。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Logout() {
  return (
    <>
      {/* 门框：左半边，右侧留出开口（人从这里走出去） */}
      <path d="M10.5 4H6.5a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h4" />
      {/* 出路：自门内穿出的横线 */}
      <path d="M9 12h10" />
      {/* 箭头：指向右（向外） */}
      <path d="M16 9l3 3-3 3" />
    </>
  );
}

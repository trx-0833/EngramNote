/**
 * @file 图标图形：zoom-in（放大）
 * @description 图形意图：**放大镜 + 圈内加号** —— 镜圈 r=7（比 `search` 的 7.5 小半档，
 * 给圈内的加号留出 1 单位空隙）、右下角一段手柄、圈内一横一竖各 6 长。
 * 替换的字形：ASCII `+`（`GraphCanvas.tsx:137` 的图谱缩放控件）——
 * 它与紧挨着的 `\u2212` −、`\u2922` ⤢ 是同一排三个按钮，
 * 一个走字体、两个走字符，三个按钮的视觉重量因此完全不同。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function ZoomIn() {
  return (
    <>
      {/* 镜圈 */}
      <circle cx="10.5" cy="10.5" r="7" />
      {/* 手柄：自 45° 方向切点 (15.45,15.45) 伸到内容区右下角 */}
      <path d="m15.5 15.5 5 5" />
      {/* 圈内加号：横竖各 6 长，交点在镜心 */}
      <path d="M10.5 7.5v6M7.5 10.5h6" />
    </>
  );
}

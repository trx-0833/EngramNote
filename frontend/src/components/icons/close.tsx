/**
 * @file 图标图形：close（关闭）
 * @description 图形意图：**叉** —— 两笔等长（各 12×12 的对角线），交点在几何中心。
 * 此前全站有 `\u2715` ✕ 与 `\u00D7` × 两种字形并存，`Toast.tsx` **同一个文件里**
 * 就各用了一次（`:68` 的提示图标是 ✕、`:208` 的关闭按钮是 ×）——
 * 两者字重、宽高比都不同，并排看就是两个图标。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Close() {
  return (
    <>
      {/* 左上 → 右下 */}
      <path d="m6 6 12 12" />
      {/* 右上 → 左下 */}
      <path d="M18 6 6 18" />
    </>
  );
}

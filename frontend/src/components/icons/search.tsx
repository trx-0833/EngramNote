/**
 * @file 图标图形：search（搜索）
 * @description 图形意图：**放大镜** —— 一个 r=7.5 的镜圈（圆心左移上移，把右下角
 * 让给手柄）+ 一段自镜圈右下切线方向伸出的手柄，收在内容区右下角。
 * 此前只有 `NotesList.tsx` 有一份内联 SVG 放大镜（18 网格、线宽 2），
 * 另外 3 个搜索框（知识卡片 / 题库 / 图谱工具栏）连图形都没有。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Search() {
  return (
    <>
      {/* 镜圈：r=7.5，外沿落在 3.0 → 18.0 */}
      <circle cx="10.5" cy="10.5" r="7.5" />
      {/* 手柄：自镜圈 45° 方向的切点（≈15.8,15.8）伸到内容区右下角 20.5,20.5 */}
      <path d="m15.8 15.8 4.7 4.7" />
    </>
  );
}

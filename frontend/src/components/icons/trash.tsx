/**
 * @file 图标图形：trash（回收站）
 * @description 图形意图：**纸篓** —— 一道桶口横线 + 上方提梁 + 上宽下窄的桶身
 * （底部两角收圆） + 桶内两道略微内倾的篾条。
 * 替换的 Unicode：`\u2672` ♲（`Sidebar.tsx:42`）—— 回收标志，语义是"循环利用"而非"删除"。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Trash() {
  return (
    <>
      {/* 桶口：比桶身两端各出挑 2.2，把"口"和"身"分开 */}
      <path d="M4 6.5h16" />
      {/* 提梁：自桶口起，两侧各一个上圆角收进顶边（孔宽 2，高 3） */}
      <path d="M9.5 6.5V5A1.5 1.5 0 0 1 11 3.5h2A1.5 1.5 0 0 1 14.5 5v1.5" />
      {/* 桶身：上口 11.6 宽、下底 7.4 宽，底部两角用 r=2 的圆弧收口 */}
      <path d="M6.2 6.5l1.1 12.6a2 2 0 0 0 2 1.8h5.4a2 2 0 0 0 2-1.8L17.8 6.5" />
      {/* 篾条：两道随桶身内倾的短竖线（上端距桶口 4，下端距桶底 1.6） */}
      <path d="M10 10.5l.4 7M14 10.5l-.4 7" />
    </>
  );
}

/**
 * @file 图标图形：upload（上传）
 * @description 图形意图：**托盘 + 向上箭头** —— 下方一个敞口托盘（左右两壁 + 圆角底），
 * 正中一支自托盘内升起的箭头，箭头尖顶在内容区上缘附近。
 * 箭头朝上 = "东西离开本地、进入系统"；托盘 = "落进一个容器里"。
 * 此前这一处语义只有纯文字按钮与一块拖拽区，没有任何图形。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Upload() {
  return (
    <>
      {/* 箭杆：自托盘内（y=15.5）升到内容区上缘附近（y=4） */}
      <path d="M12 15.5V4" />
      {/* 箭头尖：两撇对称张开，指向正上方 */}
      <path d="M7.6 8.4 12 4l4.4 4.4" />
      {/* 托盘：两口竖壁 + 圆角底（底角 r=1.5），左右各出挑到 x=4.5 / 19.5 */}
      <path d="M4.5 15v3.5a1.5 1.5 0 0 0 1.5 1.5h12a1.5 1.5 0 0 0 1.5-1.5V15" />
    </>
  );
}

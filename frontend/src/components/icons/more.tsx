/**
 * @file 图标图形：more（更多操作）
 * @description 图形意图：**横排三点** —— 三个等距（间距 7）、同样大的圆点。
 * "还有别的，但这一屏放不下"的最小图形。
 * 替换的 Unicode：`\u22EF` ⋯（`KnowledgeCards.tsx:511` 的"更多操作"按钮）——
 * 它是**数学的居中省略号**，在中文字体里比拉丁省略号更矮、间距更宽，
 * 而且与按钮里另一个状态文案 `...`（ASCII 三点）在同一行上下跳动。
 *
 * 每个点用 `r=1.2` 的圆 + `Icon.tsx` 的 1.5 描边画成（外径 3.9），
 * 于是它在本套图标里仍然是"描边图形"而不是一个填充块 —— 与其余 29 个图标同一种画法。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function More() {
  return (
    <>
      {/* 左点 */}
      <circle cx="5" cy="12" r="1.2" />
      {/* 中点（落在外接矩正中） */}
      <circle cx="12" cy="12" r="1.2" />
      {/* 右点 */}
      <circle cx="19" cy="12" r="1.2" />
    </>
  );
}

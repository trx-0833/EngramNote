/**
 * @file 图标图形：seal（印章 / 已内化）
 * @description 图形意图：**一枚方印** —— 17×17 的方形印面（四角 r=2）+ 印面里再收 4
 * 的内框（9×9，四角 r=1，留出朱文印的边框宽度）+ 内框正中一个「工」形印文
 * （上横 / 中竖 / 下横）。
 *
 * 为什么选「工」：它是篆书里最简单的对称字，三笔就收得住，在 16px 下仍然读得出
 * "这里刻了字"；而且它**不是任何可读文本**，图形文件里因此只有 path，
 * 混不进半个字符（`Icon.test.tsx` 有一条"图标里不含任何文字"的断言）。
 *
 * 这是本项目最有辨识度的一个符号机会：印面的"盖下去就不再改"对应
 * **成就 / 已内化**——一张卡片真的进了长期记忆，才配得上一枚印。
 * 本批**暂无使用者**（同批只把图形备好），登记在册以便后续
 * "掌握度 ≥ 某阈值 / 目标达成"这类位置直接取用。
 *
 * 只提供图形本身：`<svg>` 外壳（viewBox / 线宽 1.5 / currentColor / 尺寸 / 无障碍）
 * 统一由 `Icon.tsx` 负责。
 */
export default function Seal() {
  return (
    <>
      {/* 印面：17×17 的方形外框 */}
      <rect x="3.5" y="3.5" width="17" height="17" rx="2" />
      {/* 内框：四边各离印面 4 */}
      <rect x="7.5" y="7.5" width="9" height="9" rx="1" />
      {/* 印文「工」：上横（9.5→14.5）、中竖（10.2→13.8）、下横（9.5→14.5） */}
      <path d="M9.5 10.2h5M12 10.2v3.6M9.5 13.8h5" />
    </>
  );
}
